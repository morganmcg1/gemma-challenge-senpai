# Large local-only EAGLE-3 drafter artifacts (corpus tensors, checkpoints, logs).
# The PR deliverables are the scripts, arch_notes.md, and reported metrics.
train_data/
checkpoints/
*.log
*.pid
eval_traces/
