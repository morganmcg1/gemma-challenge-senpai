# SENPAI Research State — Fast Gemma Challenge

## ★★★★★★ CYCLE 62 — CURRENT STATE (2026-06-20 ~21:30Z) — BI0 SHIPPED @218; ★ HUMAN TARGET (#784 13:34Z): BREAK 250 TPS in-band + (13:55Z) "keep moving with HIGH URGENCY towards submission"; ★★ INT4HEAD = THE FIRE CANDIDATE — MERGED (#788 ubel) **real served ~253-257 local → projects to roughly break ~250 official (clears 250 MARGINALLY, ~+16% over bi0; a same-day ~321 re-baseline was RETRACTED 20:05Z — it misread stark's oracle SYNTHETIC-imposed r=0.56 ceiling as absolute TPS, see below)**; ★★ **FIRE-PREP PANEL PASSED FIRE-WORTHY ALL AXES (#795 ubel, W&B `57izwrp6`)** — GPQA-Diamond pooled 0.503 ≥ floor 0.471 AND ≥ bi0 0.497, MMLU-Pro 0.692, AIME greedy 0.300 / sampled maj@8 0.400, GSM8K 0.915 — at parity-or-above shipped bi0 every axis → **HF APPROVAL ISSUE #800 OPENED**; ★★★ **FIRE ATTEMPT FAILED ON HOSTING (NOT quality) — re-fire BLOCKED on the human's hosting decision (#800), ~2 fires remaining.** Human APPROVED TWICE on #800 ('fire when ready' 16:31Z + 'fire asap' 17:29Z); land #801 launched it (HF job `6a36d5de3093dba73ce2b016`, 18:03:10Z) but it **ERRORED at server startup 18:09:52Z on a private-repo 401** — the sandboxed runner gives `serve.py`'s vLLM exec (`serve.py:147 execvpe(os.environ)`) no HF token, `MODEL_ID`→private Hub → `RepositoryNotFoundError: 401`, **before serving a single prompt**. Local pre-flight gates ALL passed (PPL 2.00256 / 128-128 / all-4-modalities / panel `57izwrp6` GREEN) → hosting/auth, NOT metric. Escalated #800 with 3 fixes (**A RECOMMEND** token-free serve-time path: serve from PUBLIC `…-qat-w4a16-ct` base + quantize lm_head→int4 g32 AT STARTUP via `build_lmhead_quant.py`, byte-identical to validated int4head, no private repo / no visibility change, only risk = startup-timeout margin; **B** make Hub repo public [visibility tradeoff, human's call]; **C** inject runner HF token [infra]). A re-fire is a NEW HF launch → needs a fresh explicit human go. **land #801 → Plan-C de-risk (Track-1 stay re-fire-ready, Track-2 build+locally-validate token-free serve, NO HF job); lawine #815 = runner-env-parity gate (reproduce the 401 RED + public path GREEN before any future fire).** Pre-fire approval/packaging history (still valid): (ubel #802 MERGED published int4 g32 → PRIVATE Hub `gemma-challenge/gemma-4-e4b-it-int4-mtp-bi0-int4head` @ rev `ad42984`, serve-from-Hub `official_gate=PASS`). land #801 (W&B group `bi0-int4head-firevalidity`): PPL **2.00256** ≤2.42, **128/128** spec-on+off, greedy 19/128 = **A10G FP-noise floor** (AR-vs-AR=22/128 → spec not worse than AR; 512-tok greedy-identity unreachable on A10G), **public TPS 255.34 CLEARS the 250 target** (+16.9% vs bi0-head control 218.38 ≈ bi0 official → rig validated). The private-gap probe read **20.53% > my 5% gate — but it's a MIS-CALIBRATED-PROXY FALSE-RED**: bi0-head control (head swapped to bf16, identical K=6 drafter) reads the **same 20.17%** → int4 lm_head delta = **+0.36 pp (noise)**, int4head ≡ bi0 on the gap; proxy gives 20.2% for bi0 whose REAL private gap is **4.3%** → proxy ~4.7× too harsh; lm_head quant is distribution-independent (+16.9% pub / +16.4% priv) ⇒ **int4head real private gap ≈ ~4.7% ≤5%**. land correctly STOPPED at >5% per instruction + escalated; I posted FIRE-CLEAR disposition (#801) + surfaced decomposition + FIRE recommendation to human (#800). **Human replied 'fire asap' (17:29Z) → STOP LIFTED → I posted FIRE-GO to land #801 (17:30Z) + acked execution on #800 (17:31Z).** land now: verify manifest Hub-pointed (`MODEL_ID`=`gemma-challenge/...int4head` @ `ad42984`, NOT local path) → launch HF submission → post job URL + terminal SENPAI-RESULT (official `summary.json:tps` / PPL / 128-count / modalities). On official TPS return → update BASELINE.md (new official best vs bi0 218.02) + EXPERIMENTS_LOG + board/Morgan report. #784 GATE = quality-in-5%-band (byte-identity NOT sacred); ★ **ACCEPTANCE FRONTIER (config/topology) CLOSED on vLLM 0.22 — but RE-OPENED 19:25Z via the greenlit stark #816 top-k-match kernel (see below)** — depth (#774 K-sweep null) + runtime (#792 CENTROID_TOP_K null) + **topology (#799 land tree/EAGLE CLOSED RED — single-linear-chain only, 4 code-level blockers)** all dead → team redirects fully to WEIGHT-BYTE levers; ★ SURGATTN-3D now **FULLY DEAD** — byte-identity CLOSED (#793/#794) + **wirbel #791 QUALITY-KILLED & CLOSED** (GPQA 0.4646 < 0.472 floor; same-harness force-2D control reproduced 0.5101 = real variant effect, not noise; +5.51% speed real but one axis out-of-band → keep force-2D ON); **kanna #771 CLOSED** (stale pod + loopgraph NULL; fused-argmax +1.95% byte-EXACT PRESERVED as free post-fire micro-lever); **lawine #796 (lm_head BYTE-FLOOR) CLOSED clean null** (byte-floor → TPS REFUTED; channelwise W4 DOES serve on Ampere; weight-MSE ≠ logit-MSE) → reassigned **#804** (STACK private-stability pre-flight — is surgattn-3D's +5.98% prompt-agnostic?); **★ STACK (#797 int4head × surgattn-3D, 272.78 local) DEAD by inheritance** — its only delta over int4head is the now-killed surgattn-3D component → **fern #803 (STACK quality panel) + lawine #804 (STACK private-stability) CLOSED moot**; int4head #788 stands ALONE as the fire candidate. ★ **NEW LEVER (stark #798 re-profile, MERGED): PLE-input-gate de-quant = +5.3% measured** — `per_layer_input_gate` (N=256) runs int4-Marlin 3.6× SLOWER than bf16 (tiny-N tile starvation); de-quant JUST it → ~270.4 local, quality-NEUTRAL (bf16 ⊃ int4); body-MLP is now the kernel WALL (M-flat, no headroom). Active: **land #801 = THE FIRE — FAILED on a private-repo 401 → status:wip on Plan-C de-risk** (HF job `6a36d5de3093dba73ce2b016`, launched 18:03:10Z, ERRORED 18:09:52Z; W&B launch `ftds7gll`; land had re-merged origin to fix a stale local-path manifest [clean merge `ad392c7`] + dry-ran `enforce_launch_gate`=LAUNCH-ELIGIBLE before the POST — but that gate only checks PPL/completion/modalities, NOT runner model-loadability, so it ran GREEN in a credentialed env the runner does not reproduce → the 401. Re-fire blocked on the human's #800 hosting decision; land now stays re-fire-ready [Hub manifest re-pushed `997a16d`] + builds/locally-validates the Plan-C token-free serve path); **ubel #805** (PLE-dequant +5.3%, fire-track) IN-FLIGHT; ★ **stark #806 MERGED — non-GEMM attribution = NO LEVER** (W&B `azlgrinu`: 52.7 ms/step launch+fusion lever ALREADY harvested by shipped FULL@7 CUDA-graph + torch.compile; residual is fixed-floor/count-bound ~302 RMSNorm reductions + 42 launch-free attention → **decode definitively body-MLP-GEMM-bound, kernel side EXHAUSTED**; closes researcher idea #2 megafusion; attention=32.7%/~1.07 ms caps surgattn-3D headroom). **5 active student lanes attacking the post-int4head decode walls** (researcher-agent ranked 7 TPS ideas → `RESEARCH_IDEAS_2026-06-20_16:40.md`): **on the dominant body-MLP GEMM wall (47.6%, bandwidth-bound)** — ~~**wirbel #807**~~ (W4A8, narrower *activations*) **NEGATIVE −16.68% + BANKED (squash `ff6708e`)** — W4A8 SERVES on sm_86 (`VLLM_MARLIN_INPUT_DTYPE=int8`, no requant) + PPL-safe (2.0105) but is a clean DECODE LOSS: at M=1/M≈7 the body is **weight-read-bound**, int8 *activations* save <0.6% of bytes + add a per-token quant kernel (>100σ A/B 507.12→422.52 local; W&B `t2msgab0`). Banked for the harness fix + the W4A8-serves-on-Ampere/quality-safe corollaries + a ready compute-bound reference submission. **Activation-precision axis CLOSED for low-batch decode.** → **wirbel reassigned #814 (body int4 GROUP-SIZE coarsening g32→g128)** (fewer SCALE bytes at same W4 bits: bf16 g32 scale ≈12.5% of body weight bytes → g128 ≈3% → ~9% fewer body-weight HBM reads on the 47.6% verify-GEMM; newly legal under #784's 5% band; stark #810 map says body sensitivity near-uniform → likely in-band); ~~stark #810~~ (per-layer mixed-precision W3/W2, Q-Palette) **MERGED (banked map) + SUB-4-BIT SERVE-KILLED** — map DELIVERED (`research/bi0_int4head_mlp_mixed_precision/`, near-UNIFORM sensitivity, all-42-W3 clears PPL 2.1432 = quality budget WIDE open) but W3/W2 **kernel-blocked on sm_86** (vLLM 0.22 WNA16 = 4/8-bit only, raises at load; Marlin Ampere uint4/uint8 only; Machete sm_90-only) → binding constraint is KERNEL not quality; re-open only on sm_90. ★ Map says **if any legal sub-W4 lever existed, apply it UNIFORMLY (no per-layer sparing) — but W4A8 is now REFUTED as a decode lever (wirbel #807 −16.68%); the map's live use is the new group-size lane (uniform g128)**. W&B `p874uaix`/`ul611iyf`. → **stark #813 (acceptance-ceiling ORACLE) → GREENLIGHT + BANKED (squash `a6b17632`)**: config-leniency is DEAD (vLLM 0.22 v1-engine hard-wires greedy accept to exact-argmax `rejection_sampler.py:743-745`, dropped v0 `TypicalAcceptanceSampler`, only `rejection_sample_method∈{standard,synthetic}`) so stark ran the **synthetic-accept ORACLE** on the TRUE served int4head (impose accept rate; garbage tokens NOT shippable; kernels identical = faithful speed-only ceiling): r=0.56 (IMPOSED — ~29% ABOVE int4head's REAL E_accept 3.379) **309.96 steady / 323.29 wall** → r=1.00 **CEILING 471.09 steady / 506.84 wall** (W&B `myk4s0ft`→`j60r68os`, group `bi0-int4head-accept-oracle`). Ceiling 471 ≫ the +10% band (>282) → **GREENLIT a custom greedy-kernel top-k-match accept-branch = stark #816** (`vllm_topk_accept_patch.py` as a 3rd `sitecustomize` hook on `vllm.v1.sample.rejection_sampler`; `accept iff draft∈topk(target,k)`; `k=TOPK_ACCEPT_K`, k=1⇒byte-identical control; sweep k∈{1,2,4,8}; full quality panel on SERVED outputs + greedy-identity waiver #784; LOCAL-only) → **ACCEPTANCE FRONTIER RE-OPENED** (had been depth #774 / pool #792 / topology #799 / config #813-Step1 all closed). ★★ **int4head RE-BASELINE CORRECTION (20:05Z): the ~321 figure was RETRACTED** — a same-day edit misread stark #813's oracle r=0.56 number (309.96 steady / 323.29 wall) as absolute TPS, but that oracle IMPOSES the accept rate via `rejection_sample_method=synthetic` and r=0.56 (e_accept 4.355) is ~29% ABOVE int4head's REAL E_accept (3.379 / r≈0.397, logged by ubel `7ntx4nrn`). THREE real-served controls cluster at ~253-257 (ubel #805 252.62, fern #811 254.20, fern #797 257.39) + the 256.74 reconstruction → **honest anchor ~253-257 local → roughly break ~250 official, UNCONFIRMED**; the oracle is SHAPE-only, not an absolute proxy ⇒ int4head, if it serves cleanly, clears 250 MARGINALLY (~+16% over bi0); ★ **fern #808 (2:4 structured pruning) VIABILITY-KILLED & MERGED** (W&B `m7y0qg5h`, ~0 GPU: pinned vllm 0.22.0 / compressed-tensors 0.15.0.1 has **sparsity support STRIPPED** — loader hard-rejects `sparsity_config`, no `gptq_marlin_24_gemm` compiled, no Sparse24 class; upstream removal #36799/#2267; g128-only; banked probe for #807/#810 reuse) → **fern reassigned #811** (int4head serve-config **byte-exact** TPS sweep — MAX_MODEL_LEN right-size for the 640-tok workload / CUDA-graph capture sizes / Marlin launch-config knob / batched-tokens; zero quality risk, stacks on the firing winner); **NVFP4/MXFP4 present but ADD microscale bytes vs int4-g128 → NOT a fewer-reads TPS lever** (don't chase for speed); ~~lawine #809~~ (CUDA-graph capture widening to kill the per-token sampling sync stall) **CLOSED CLEAN NULL** — the serial sync doesn't exist: `async_scheduling` is auto-enabled for `gemma4_mtp`, D2H readback overlapped on side streams (0.0µs on compute stream); sampling-sync/capture-scope axis closed (joins #789/#771 + #806 "kernel side exhausted"); W&B `wsr5i6qb`. ~~lawine #812~~ (attribute the bf16 `aten::mm` ~17% of GPU-busy) **CLOSED clean NULL** — PHANTOM lever: the re-profile (W&B `7rvzl0l9`) of the true int4head build reads marlin 290.19ms/70.4%, `ampere_bf16` 2.09ms/0.5%, `aten::mm` 0.13ms/0.0%; the "17% bf16 GEMM" was the **lm_head, already int4 on int4head** → nothing to convert. **★ #809 MISLABEL CORRECTION (banked):** the run **`wsr5i6qb`** cited in #809 as "int4head" was actually `…-qat-w4a16-ct` (int4 body + **bf16 TIED lm_head**), NOT int4head — future "17% bf16 on int4head" reasoning is mislabeled & will misfire. int4head decode is int4 end-to-end on the GEMM path → only weight-BYTE levers live (group-size #814). → **lawine reassigned #815** (runner-env-parity launch gate — catch model-loadability 401s before a fire burns; directly motivated by today's failure). ★ **CYCLE-62 BANKS (squash-merged, NOT official-TPS winners — baseline stays bi0 218.02):** land #801 (Plan-A token-free serve VALIDATED = the 401 hosting fix, byte-identical sha `0f03a7dc`, 470s cold-start margin, gated OFF by default), ubel #805 (PLE-dequant +5.14% ~265 + BOOT-DETERMINISTIC 0/128 vs control's 98/128), fern #811 (serve-config NULL + boot-fragility diagnostic), lawine #815 (runner-parity 401 gate, RED on private + GREEN on public). ★ **4 IDLE → REASSIGNED (zero idle functional GPUs):** land #817 (port Plan-A serve to PLE-dequant → make the BETTER candidate fire-ready), ubel #818 (head-precision × E_accept — does int4 lm_head DEPRESS the surprisingly-low r≈0.397 acceptance? free-win hunt), lawine #819 (token-less `HfApi().model_info(token=False)` probe in `enforce_launch_gate` — hard pre-fire 401 block), fern #820 (reject-rank + verifier-entropy CDF → viability gate for stark #816 uniform-top-k AND FLy entropy-gated accept). ★ **RESEARCHER IDEAS queued** (`RESEARCH_IDEAS_2026-06-20_20:00.md`): #1 FLy entropy-gated accept (+20-40%, arXiv:2511.22972 — de-risks #816's quality, fed to fern #820), #2 FR-Spec/VOCABTRIM draft-lm_head vocab compression (+5-12%, quality-safe, attacks the 20% drafter — ★ ASSIGNED lawine #821: drafter is a SEPARATE public assistant model projecting the full 262,144-token vocab 6×/step [NUM_SPECULATIVE_TOKENS=6], so a frequency-pruned draft head is amortized 6×/step and the only lane touching the DRAFT head; verifier scores full vocab → quality-safe by construction), #5 AdaEDL adaptive draft-length, #6 MTP-loop CUDA-graph (vLLM PR #34880). **In-flight:** stark #816 (top-k accept kernel) + wirbel #814 (body g32→g128, 3 speed arms logged `bi0-int4head-body-groupsize`, quality panel pending) + ubel #818 + fern #820 + lawine #821 (FR-Spec draft head) + **land #822** (AdaEDL entropy-gated early-stop within K=6 — skips #774's reject tail, replaces banked #817). ★ **BANKED this window:** lawine #819 (token-less 401 probe in `enforce_launch_gate`, squash `89bfeb7`, W&B `ady1h9tx` — next fire hard-blocks on an un-fetchable model) + **land #817 (PLE-dequant token-free serve, squash `d3b4e24`, W&B `cwjfuci4`)** → ★★ **PLE-dequant (#805/#817) is now the PREFERRED fire candidate: 265.61 / +5.14% over int4head, boot-deterministic, full-checkpoint sha256 byte-identical to validated #805, parity-GREEN token-less, 625s cold-start margin.** Both PLE-dequant (#817) and int4head (#801) are now token-free fire-ready + byte-identity-proven (primary + fallback); 401 resolved on our side → the ONLY re-fire gate is a fresh human go on #800. denken + kanna pods DOWN (stay down per #784 — no re-ping; triage flags them idle but they are the known-broken pair) — see Fleet status below — authoritative

### ★ NEW GOVERNING HUMAN DIRECTIVE — Issue #784 (2026-06-20 11:24Z, morganmcg1)
**"Push hard on faster TPS within quality guardrails."** Operating target: optimize TPS aggressively; **maintain quality within 5% of the base model on AIME/MMLU/GPQA**; **byte-identical outputs are NOT sacred for this phase** — a non-byte-identical variant that keeps quality inside the 5% band is **worth testing and escalating**. Cadence: pick 2–4 variants, local speed smoke each, cheap quality evidence, keep winners + discard losers fast, **cap analysis per lane** (don't over-analyze a small hypothesis). Prefer local A10G profiling. **Do NOT launch HF benchmark/submission jobs without explicit human approval** (open an Approval-request issue first). → **I have relaxed every in-flight card's "greedy-token-identical MUST match" hard gate to "quality-in-5%-band"** (PPL ≤ 2.42 + 128/128 + all-4-modalities + AIME/MMLU/GPQA within 5%). ACK posted on #784.

### ★ HUMAN SUB-DIRECTIVE — #784 (2026-06-20 13:34Z, morganmcg1)
**"Keep pushing — can we push toward or break 250 TPS while keeping our quality gate?"** + two ops notes: **(1) IGNORE PPL-only competitors** (sparkgemma/ultra-gemma ~490–505 TPS are PPL-only-validated — PPL ~2.39, no AIME/MMLU/GPQA; PPL is an imperfect measure → calibration only, NEVER borrow their W192/CTK/noprecache configs); **(2) broken students stay down for a while → reassign to functional students** (denken's wedged pod is NOT getting fixed soon — stop blocking on #790, no more re-pings; keep the 7 functional students fully loaded, which they are). **My answer (posted #784 ~13:45Z):** 250 = +14.7% over 218.02, reachable by STACKING orthogonal quality-safe levers — surgattn-3D (~+6.7%*) × int8 lm_head (~+4–6%) × acceptance K/CENTROID (~+4–6%) [× fused-argmax +1.95%] → projects ~252–266 TPS. *surgattn is the biggest but **least-certain** (n=1, exceeds static ~2.6% attn ceiling) → being noise-certified + quality/identity-resolved by #791/#793/#794; if it de-rates, the path leans on lm_head + acceptance. No silver bullet; the stack. Will open an HF approval issue the moment a stacked config clears the local gate + 4-axis panel.

### ★ COMPETITIVE BAR (public board, cmpatino-verifier 11:18Z)
A competitor (`sparkgemma-s46b`) had **506.63 TPS re-run private at 490.72 (Δ 3.1%), PPL 2.3931 — VERIFIED VALID.** The frontier is being pushed by aggressive, *non*-byte-identical configs that sit inside the PPL≤2.42 bar. Our shipped bi0 = 218.02 → large headroom; the old byte-identity constraint was leaving TPS on the table (validates #784). NOTE: this is the public leaderboard scoreboard, not an in-scope branch — do NOT inspect/borrow sparkgemma's config; use the number only to calibrate urgency.

### What shipped (2026-06-20 09:32Z)
- `int4_mtp_bi0_surgattn` FIRED + OFFICIAL: **218.02 TPS / PPL 2.0058 / 128/128 VALID** (W&B `s63tb03x`, job `6a3656ef3093dba73ce2ac88`). Human-approved #769. +72.5% over int4_g128_lmhead@126.378. Config: int4 W4A16 + MTP K=6 + VLLM_BATCH_INVARIANT=0 + surgattn + prometheus guard.
- **★ bi0 quality panel NOW COMPLETE (all 4 Morgan axes):** MMLU-Pro **0.644** / GSM8K **0.867** / AIME **10/30** / GPQA-Diamond **0.4970** (#773 wirbel, W&B `kredc30c`, pooled 990 samples). All axes pass the ≤10% degradation bar. Reported to Morgan + dhruv-mishra on board `20260620-111347-905_senpai.md`. **The quality panel requirement for any 300+ candidate is: same bar (all 4 axes ≥90% of base).**

### Current research focus: reach a quality-safe 300+ via PRIVATE-STABLE levers, decomposed by the PROFILER

Morgan's question (board 08:19Z): "≤10% degradation vs base for any 300+ submission." **Status:** bi0 quality panel is complete and PASSES. dhruv-mishra's question ("separate validation other than perplexity?") answered on board 11:13Z — YES: full validity contract = PPL + 128/128 + greedy-identity + 4-modalities + cmpatino private Δ≤5%; PLUS our internal quality panel. The real target = PRIVATE-STABLE prompt-agnostic levers that beat 218 TPS with the same quality gates.

**★ PROFILER REALITY (BASELINE.md) drives the decomposition:** decode at conc=1 is **memory-bandwidth-bound — ~92% weight-GEMM, attn ~2.6%, sampling ~0.2%.** Only two lever FAMILIES have real headroom: **(1) the int4 W4A16 GEMM kernel** (numerics-neutral, greedy-identity) and **(2) drafter acceptance** (amortize the one big GEMM over more accepted tokens, greedy-verified).

**★ PERMANENTLY DEAD LEVERS (confirmed):**
- **int4 Marlin W4A16 kernel-SWAP (#781 ubel, CLOSED 11:55Z):** no numerics-equivalent faster W4A16 kernel exists on sm_86 — Marlin is the SOLE servable kernel (Cutlass/Machete need sm_90, AllSpark W8-only, Conch absent, Exllama rejects bf16); its `atomic_add` toggle is hw-gated off (Ampere no bf16 atomicAdd); no tunable tile/thread/split; CLI rejects quant-method mismatch. **★ The 58% M=1 HBM figure is a RED HERRING — deployed serving runs at M=7 (~79% of peak); spec already amortizes the M=1 dequant slack (13.29ms single-stream → 218.49 deployed = 2.9×).** Kernel-schedule lever closed; the body GEMM is at the wall where it runs. W&B `uaq6btet`.
- **FlashInfer (#779 stark, CLOSED):** architecturally incompatible with Gemma4-E4B on sm86 — head_dim=512 global-attn has no valid paged-prefill kernel in FlashInfer; crashes at warmup `NUM_MMA_D_QK=32→head_dim=512`. Not a config issue; permanent. W&B `6i58gvjk`.
- fp8-KV (#777 ubel, CLOSED): three-layer hardware wall on sm_86. Permanent.
- precache (#775/#778, CLOSED): prompt-replay gaming, fails program.md:325 + private-Δ gate. Permanent.
- osoi5-baked (#772 land, CLOSED): body-level quality collapse. Permanent.
- **drafter-weight-quant (#786 stark, CLOSED 12:35Z):** the bf16 MTP drafter is only **17.2% of GPU-busy + latency-bound at M=1** (weight read ~0.17ms/2.434ms, NOT BW-bound) — no "disproportionate bf16 BW share" to recover; AND vLLM 0.22.0 **silently no-ops** `--speculative-config quantization=` for `gemma4_mtp` (no packed weights to attach; proven 3 ways). Dead two ways. The int4 verifier (82.8%) is the budget. W&B group `bi0-drafter-gemm`.
- **ngram/prompt-lookup drafter (#782 land, CLOSED 13:40Z):** 89.91 vs 218.11 local TPS (2.4× slower) — ngram E[T] 1.99–2.17 vs MTP 3.34; suffix-matching misses on general chat, and num_spec 3→5 *lowers* accept_rate (miss-dominated). bi0's learned MTP head is the better acceptance lever; stacked ngram+MTP architecturally unsupported (single-`method` `SpeculativeConfig`). W&B `7m8eyv0f`/`rv1v3lxi`.
- **★ drafter-dispatch / launch-overhead family (#789 stark + #771-loopgraph kanna, CLOSED 13:40Z):** the 6× M=1 proposer passes are ALREADY PIECEWISE-captured (not eager; no size-1 graph realized — `adjust_cudagraph_sizes_for_spec_decode` rounds [1,2,4,8]→{7}); FULL/whole-loop proposer capture is hard-disabled in 0.22.0 (`llm_base_proposer.py:380`, no config knob); AND kanna's whole-loop LOOPGRAPH measured **+0.037% null**. Joint conclusion: the drafter `cpu 5.6 ≫ gpu 2.5` gap is **async-launch-queue depth hidden behind GPU compute, NOT recoverable wall-clock latency** — closed from both the config-capture (stark) and whole-loop-capture (kanna) angles. Verify-GEMM amortization via acceptance (fern #774 / lawine #792) is the live path. W&B `xn7opsy8`/`wjeykst8`.

#### Private-stable lever decomposition (Cycle 62 — all LOCAL-only, all quality-gated per #784):
| lever | profiler ceiling | quality risk | assigned | status |
|---|---|---|---|---|
| **★★ int4 W4A16 g32 lm_head GEMV = THE FIRE CANDIDATE** (1.342→0.378 GB/tok, 3.56×; per-token 2.777→0.750 ms; MTP does NOT amortize) | **~253-257 local served (ubel 252.62 / fern 254.20 / fern#797 257.39 / recon 256.74) → ~break 250 official; the ~321 from the SYNTHETIC oracle was RETRACTED** | LOW (panel cleared all axes) | ubel #788/#795/#802 → land #801 | **★★★ FIRE FAILED on a private-repo 401 (NOT quality) — re-fire BLOCKED on #800's hosting decision; ~2 fires left.** HF job `6a36d5de3093dba73ce2b016` launched 18:03:10Z, **ERRORED at startup 18:09:52Z** (W&B launch `ftds7gll`): the sandboxed runner gave `serve.py`'s vLLM exec no HF token for the private `MODEL_ID` → `RepositoryNotFoundError: 401`, no prompts served. Pre-fire ALL-GREEN (public TPS 255.34 clears 250, PPL 2.00256, 128/128 spec-on+off, real private gap ~4.7%; Gate 1/2/3 `nidx5trn`/`57izwrp6`/human #800) → hosting/auth not metric. The `enforce_launch_gate` dry-run read LAUNCH-ELIGIBLE because it only checks PPL/completion/modalities, not runner model-loadability. **Escalated #800** (A RECOMMEND token-free serve-time lm_head quant from PUBLIC `…-qat-w4a16-ct`; B public repo; C runner token); a re-fire needs a NEW human go. → **land #801→Plan-C; lawine #815 runner-parity gate** | 
| ~~surgattn +5.35% — quality-gate the 3D variant~~ (`VLLM_SURGATTN=0`, 229.19 local = +5.51% real) | — | killed | wirbel #791 | **CLOSED QUALITY-KILL** (GPQA-Diamond 0.4646 < 0.472 floor; same-harness force-2D control reproduced 0.5101 = real reproducible variant effect; MMLU/GSM8K/AIME pass but hard ALL-axes gate fails → keep force-2D ON. Mechanism head-precision-independent → STACK inherits the fail. W&B `padmkhm7`/`w93pznll`/`f9lrcxm1`) |
| ~~surgattn — WHOLE byte-identical at split-KV combine~~ | — | — | stark #794 | **CLOSED clean null** (the +5.35% IS the parallel reduction reassociation: fp32 accum already optimal=no-op, `triton_attn.py:185-204`; deterministic-combine un-splits KV=removes the win; determinism control noise floor 0 → 4/128 is real, not noise; W&B `kcojzw20`) |
| ~~surgattn — DRAFTER-share byte-identical (caller-gate)~~ | — | — | land #793 | **CLOSED clean null** (caller-gating recovers only a byte-identical subset; residual ~3/128 [gpqa1/mmlu_pro2/aime0] is irreducible drafter ULP-flip cascade via #792 verify-GEMM mechanism; reconciles with #794) |
| **★ post-int4head decode RE-PROFILE — MERGED (evidence)** (body verify-GEMM now dominant 5.92ms/47.6%; lm_head 0.75ms/6%; surfaced PLE-dequant lever) | diagnostic | none | stark #798 | **MERGED** (body-MLP is the Marlin 1-wave WALL — M-flat, no kernel headroom; lm_head saving ~100% realized; ★ FRESH PLE-dequant +5.3% lever; kernel side nearly exhausted, acceptance frontier closed. W&B `dpc36210`/`tgdo0imp`) |
| **★★ PLE-input-gate DE-QUANT = +5.3% (build+serve, FIRE-TRACK)** (`per_layer_input_gate` 2560→256 ×42 runs int4-Marlin 3.6× SLOWER than bf16 — tiny-N tile starvation; de-quant JUST it → bf16, 0.629ms/step saved) | **+5.3% → ~270.4 local (stacks on int4head, orthogonal)** | LOW (bf16 ⊃ int4 = quality-neutral-to-better) | ubel #805 | **IN-FLIGHT** (LOCAL; build int4head+PLE-bf16, serve-verify dispatch=cuBLAS, measure TPS/PPL/GSM8K/greedy; if confirms → publish as fire candidate) |
| **★ NON-GEMM verify attribution** (split the 3.28ms/26.4% residual: attn / RMSNorm ×~250/fwd / RoPE / KV-write / sampling — any fusable/launch-bound lever? bounds surgattn-3D ceiling) | diagnostic (last unattributed slice) | none | stark #806 | **IN-FLIGHT** (LOCAL synthetic microbench, disk-free — fits stark's constrained pod; clean negative = kernel side exhausted, decode is body-MLP-bound) |
| ~~tree/EAGLE multi-candidate drafter FEASIBILITY~~ | — | — | land #799 | **CLOSED RED** (vLLM 0.22 spec-decode single-linear-chain only for gemma4_mtp; 4 blockers: `num_speculative_tokens` scalar int `speculative.py:80`, `rejection_sample` asserts `ndim==1` `rejection_sampler.py:413`, all proposers emit linear `[batch,K]`, no tree-attn kernel; EAGLE separate-weight = retrain epic. W&B `x76anf6u`) → **★ acceptance frontier DEFINITIVELY closed (depth #774 + runtime #792 + topology #799)** |
| **★ int4head FIRE-VALIDITY pre-flight** (within-job spec-on-vs-spec-off greedy-identity [closes stark's stale-ref landmine for the fire candidate] + `private_gap_probe.py` to predict private Δ TPS before quota) | fire gate | none (validation) | land #801 → Plan-C | **⛔ FIRE FAILED on a private-repo 401 → status:wip on Plan-C** (job `6a36d5de3093dba73ce2b016` launched 18:03:10Z, ERRORED 18:09:52Z, launch W&B `ftds7gll`). Pre-flight was FIRE-CLEAR (255.34 pub TPS clears 250; PPL 2.00256; 128/128 spec-on+off; greedy 19/128 = A10G FP-noise floor; real private gap ~4.7% ≤5% [20.53% probe = mis-calibrated proxy, bi0-head control isolates int4 lm_head delta to +0.36pp]; W&B firevalidity `2oqdjcvz`/`8r1bqfur`/`176c2h74`/`vk3i9qja`) — but the runner gave the vLLM serve exec no HF token for the private `MODEL_ID` → `RepositoryNotFoundError: 401`, no prompts served. The pre-fire `enforce_launch_gate` read LAUNCH-ELIGIBLE only because it checks PPL/completion/modalities, NOT runner model-loadability. Re-fire blocked on #800; land→Track-1 re-fire-ready (Hub manifest `997a16d`) + Track-2 token-free serve build (LOCAL, no HF job) | 
| ~~int4head FIRE BLOCKER: publish int4 g32 → private Hub + repoint `MODEL_ID` + serve-verify~~ | fire gate | none (packaging) | ubel #802 | **✅ MERGED GREEN** (published PRIVATE Hub `gemma-challenge/gemma-4-e4b-it-int4-mtp-bi0-int4head` @ rev `ad42984`; manifest repointed MODEL_ID→Hub; clean-env serve-FROM-HUB: PPL 2.00267 / 128-128 / all-4-modalities / guard active / `official_gate=PASS`; W&B `nidx5trn` verified all axes. Pre-fire packaging gate satisfied — fire now 1 step from ready) |
| ~~MTP draft-acceptance RUNTIME knob CENTROID_TOP_K @ fixed K=6~~ | — | — | lawine #792 | **CLOSED clean null** (widening pool 32→128 moves accept +1.5%, TPS net-zero, breaks 128/128→119/118 via ULP flips; runtime acceptance axis exhausted on bi0 — DEPTH still live via #774) |
| ~~int4 lm_head BYTE-FLOOR: channelwise (`-1`) + MSE/clip scales~~ | — | — | lawine #796 | **CLOSED clean null** (byte-floor → TPS REFUTED: 4 arms 255.5–256.3 all ≤ control 256.74; ~41 MB scale saving <2% traffic + amortized across M=7 verify; ★ channelwise W4 g=−1 DOES serve on Ampere (Marlin `CompressedTensorsWNA16`), ★ weight-MSE ≠ logit-MSE (g32 MSE RAISED PPL); g32 #788 is the sweet spot. W&B `kxkdixob`) |
| ~~STACK private-stability pre-flight~~ | — | moot | lawine #804 | **CLOSED moot** (STACK quality-dead via wirbel #791; surgattn-3D prompt-agnosticism settled anyway by wirbel's +5.51% @ ≈15× stdev) → lawine reassigning |
| ~~MTP K-depth sweep {0,2,4,6,8}~~ | — | — | fern #774 | **CLOSED clean null** (K=6 is the TPS knee; K=8 regresses −2.0%; per-position accept collapses 0.66→0.32 by horizon 7-8 — depth lever dead) |
| ~~STACK: int4head × surgattn-3D — the two levers COMPOUND~~ (272.78 local = +5.98% on int4head; banked `int4_mtp_bi0_int4head_surgattn3d`, W&B `n84urlmc`) | — | **QUALITY-DEAD** | fern #797 MERGED | **candidate SHELVED** — wirbel #791 killed surgattn-3D on GPQA and the STACK inherits it (head-precision-independent). The merged code/submission stays as evidence; int4head #788 stands ALONE as the fire candidate |
| ~~STACK fire-prep quality panel~~ | — | moot | fern #803 | **CLOSED moot** (STACK quality-dead by inheritance from wirbel #791's surgattn-3D GPQA kill — no quality-safe gain over int4head to panel) → fern reassigning |
| ~~fused-sparse-argmax centroid kernel~~ (byte-EXACT +1.95% @ ~1.8σ — loopgraph itself = +0.037% null) | low | none | kanna #771 | **CLOSED** (stale pod + loopgraph structurally dead `llm_base_proposer.py:380`; fused-argmax +1.95% byte-EXACT but marginal/unconfirmed N=3 + dwarfed by int4head/stack — PRESERVED as a free post-fire micro-lever. W&B `xn7opsy8`) |
| ~~Marlin kernel-swap~~ / ~~locus~~ / ~~FlashInfer~~ / ~~fp8-KV~~ / ~~precache~~ / ~~osoi5~~ / ~~drafter-quant~~ / ~~verifier-M=7-graph~~ / ~~ngram (2.4× slower)~~ / ~~drafter-dispatch capture~~ | — | killed/dead | — | CLOSED |

### Key constraints
- **Quality gate (Morgan):** MMLU-Pro ≥ 0.572, GPQA ≥ 0.471, GSM8K ≥ 0.807, AIME ≥ 0.090. Greedy-identity to the bi0 control is the quality proof for numerics-neutral levers.
- **Full bi0 quality panel (reference):** MMLU-Pro 0.644 / GSM8K 0.867 / AIME 10/30 / GPQA-Diamond 0.4970. W&B `kredc30c` (GPQA), `s63tb03x` (TPS).
- **Serving fix:** prometheus guard ALONE. fastapi pin = INCOMPATIBLE with vLLM 0.22.0. Never use the pin.
- **No autonomous HF launch.** Open approval issue; fire only after human approves.
- **Private-stable mandate:** any fire MUST be prompt-agnostic — Δ TPS ≤ 5% private re-run gate. Precache / prompt-replay is permanently OFF (program.md:325).
- **★ Stale-greedy-reference landmine (stark #794):** the cached LOCAL spec-off greedy reference for bi0 (`research/greedy_reference/…int4_mtp_bi0_surgattn…`, Jun-19) is STALE/cross-job — a naive `greedy_gate --model-id` against it reads ~99/128 DIVERGENT for the **shipped bi0 itself** (not a bi0 bug; bi0 passes the official *within-job* gate). Before trusting `greedy_gate` for ANY bi0-derived submission, regenerate the reference WITHIN-JOB. (Lower priority since #784 makes byte-identity non-sacred, but a real gating gotcha.)

### Fleet status (2026-06-20 ~21:30Z) — 6 functional active (ZERO idle) + 2 down (denken, kanna)
> **★ Current lanes (Cycle-62 late):** land #822 (AdaEDL entropy-gated early-stop within K=6 draft — skip the #774 reject tail) · ubel #818 (head-precision × E_accept free-win hunt) · lawine #821 (FR-Spec frequency-pruned draft lm_head — 6×/step draft GEMV, the only lane touching the DRAFT head) · fern #820 (reject-rank + entropy CDF → #816/FLy viability) · wirbel #814 (body g32→g128, 3 speed arms logged, quality panel pending) · stark #816 (top-k accept kernel) — all WIP. Banked this cycle: land #801 + ubel #805 + fern #811 + lawine #815 + lawine #819 (token-less 401 probe, squash `89bfeb7`) + **land #817** (PLE-dequant token-free serve, squash `d3b4e24`, W&B `cwjfuci4` — full-checkpoint sha256 byte-identical to #805, parity GREEN no-token, 625s cold-start margin). ★★ **PLE-dequant is now the PREFERRED fire candidate** (265.61 / +5.14% over int4head, boot-deterministic, AND token-free-serveable) — both it (#817) and int4head (#801) are token-free fire-ready + byte-identity-proven (primary + fallback); the 401 blocker is resolved on our side. **The ONLY remaining gate on a re-fire is a fresh human go on #800.** denken+kanna DOWN (stay down per #784; triage flags them idle but they are the known-broken pair — do NOT re-assign/re-ping). wirbel #814 nudged 20:47Z (proof-of-life: 3 speed arms in W&B, panel likely mid-flight; escalate only if dark past ~21:45Z). The bullets below are the Cycle-62-MID fire-failure + bank history that produced these lanes.
- **land #801 (CRITICAL PATH — int4head re-fire de-risk):** the int4head fire FAILED on a private-repo 401 (runner has no HF token; `serve.py:147 execvpe(os.environ)` against the private `MODEL_ID`). status:wip on two tracks: **Track-1** stay re-fire-ready (Hub-pointed manifest re-pushed durable `997a16d`, build staged) — re-fire is a NEW HF launch, needs a fresh human go on #800; **Track-2** build + LOCALLY validate the Plan-C token-free serve path (serve from PUBLIC `…-qat-w4a16-ct` + quantize lm_head→int4 g32 at startup via `build_lmhead_quant.py`; prove byte-identity to validated int4head + cold-start wall-clock margin under the job timeout + local gate parity; NO HF job).
- **lawine #815 (runner-env-parity launch gate — NEW, motivated by today's failure):** build `scripts/local_validation/runner_env_parity_gate.py` that runs `serve.py` in a scrubbed token-less / fresh-`HF_HOME` runner-parity env; MUST go RED `failure_class=401_private_repo` on the int4head Hub manifest (reproduce the fire failure) + GREEN on the public bi0 path (no false positive). Protects the ~2 remaining fires. LOCAL-only. (Replaces #812, CLOSED clean NULL — the bf16 `aten::mm` was the lm_head, already int4 on int4head; #809's `wsr5i6qb` was mislabeled w4a16-ct.)
- **ubel #805 (PLE-input-gate DE-QUANT — fire-track +5.3%):** delivered #788 (int4 g32 lm_head) + #795 (panel) + ✅ **#802 MERGED** (published int4head to private Hub) → productionize stark's #798 finding: build int4head + de-quant `per_layer_input_gate` to bf16 (int4-Marlin 3.6× slower than bf16 at N=256), serve-verify dispatch=cuBLAS, measure decode TPS (expect ~270 / +5.3%) + PPL/GSM8K/greedy. If confirms → next fire candidate. IN-FLIGHT.
- **wirbel #814 (body int4 GROUP-SIZE coarsening g32→g128):** fewer SCALE bytes at same W4 bits — bf16 g32 group-scale ≈12.5% of body weight bytes → g128 ≈3% → ~9% fewer body-weight HBM reads on the dominant 47.6% verify-GEMM; newly legal under #784's 5% band; stark #810 map says body sensitivity near-uniform → likely in-band. Step-1 viability + Step-2 PTQ g32/g64/g128 A/B + Step-3 quality panel. LOCAL-only. (Replaces #807 W4A8, BANKED NEGATIVE −16.68%.)
- **fern #811 (int4head serve-config byte-exact TPS sweep):** MAX_MODEL_LEN right-size for the ~640-tok workload / CUDA-graph capture sizes / Marlin launch-config / batched-tokens — zero quality risk, stacks on the firing winner. LOCAL-only.
- **stark #813 (acceptance-ceiling ORACLE) → GREENLIGHT + BANKED `a6b17632` → reassigned stark #816:** oracle ceiling@r=1.00 = 471 steady / 507 wall ≫ +10% band (282) → GREENLIT the custom greedy-kernel top-k-match accept-branch (**stark #816**: `vllm_topk_accept_patch.py`, `TOPK_ACCEPT_K` sweep k∈{1,2,4,8}, quality panel on SERVED outputs + #784 greedy-identity waiver, LOCAL). Also CORRECTED the int4head baseline (20:05Z): the oracle's 309.96 steady / 323.29 wall is the SYNTHETIC-imposed r=0.56 ceiling (~29% above real E_accept 3.379), NOT absolute TPS — a ~321 mis-edit was RETRACTED; honest anchor ~253-257 local (three real controls: ubel `7ntx4nrn` 252.62 / fern `zzd2u9uf` 254.20 / fern #797 257.39).
- **DOWN (stay down per #784, no re-ping):** denken (wedged pod, branch `denken/fire-bi0-surgattn-guarded`), kanna (repeatedly silent/never-logs — see [[project-kanna-pod-liveness]]).
- **kanna #771:** CLOSED — pod stuck (last commit 13:04Z predates my 13:23Z send-back, CONFLICTING, no response — known kanna liveness pattern); loopgraph structurally dead + fused-argmax +1.95% byte-exact PRESERVED as a free post-fire micro-lever. kanna stays DOWN per #784 (not reassigned).
- **★ The surgattn +5.35% is now a SINGLE-LANE decision:** #793 (caller-gate) + #794 (combine kernel) both proved the speedup is intrinsically non-byte-identical (parallel KV-reduction reassociation + drafter ULP cascade, ~3-4/128 reasoning-only, PPL bit-identical, zero on AIME). Under #784 (byte-identity not sacred) the lever ships *only* via **wirbel #791's** quality gate — if its panel clears, ~+5.35% stacks on int4head.
- **★ Pod infra:** denken WEDGED + kanna STUCK (both stay down per #784 ops note — NOT re-pinging; kanna's last commit 13:04Z, no response to 13:23Z send-back). Other "worker unhealthy" flags (fern/lawine/wirbel) = stale-heartbeat false alarms (branches match + dirty>0). Reliable liveness = branch-match + commits, NOT heartbeat age.
- **MERGED this cycle:** **#788** (ubel int4 g32 lm_head — +17.0% local, 256.74 TPS, THE FIRE CANDIDATE) + **#795** (ubel int4head fire-prep panel — FIRE-WORTHY all axes) + **#797** (fern int4head × surgattn-3D STACK — levers COMPOUND, 272.78 local rung-2 candidate) + **#802** (ubel int4head publish-to-Hub + repoint — ✅ fire blocker CLEARED, serve-from-Hub `official_gate=PASS`) + **#798** (stark post-int4head re-profile — body-MLP is the kernel WALL, surfaced PLE-dequant +5.3% lever). **CLOSED this cycle:** **#799 (land tree/EAGLE — RED, acceptance topology dead → acceptance frontier DEFINITIVELY closed)**; **#796 (lawine lm_head byte-floor — clean null, byte-floor ≠ TPS, channelwise W4 serves on Ampere, weight-MSE ≠ logit-MSE)**; **#771 (kanna loopgraph+fused-argmax — stale pod + loopgraph dead; fused-argmax +1.95% byte-exact PRESERVED post-fire)**; #792 (lawine acceptance-runtime CENTROID_TOP_K) + #774 (fern acceptance-depth K-sweep) — acceptance family tuned-out; **#794 (stark surgattn byte-identical-combine) + #793 (land surgattn byte-identical drafter-share)** — both clean nulls, surgattn-3D proven intrinsically non-byte-identical → wirbel #791 sole decider. **OPENED this cycle:** HF approval issue **#800** (int4head fire); assignments **#801** (land fire-validity) + **#802** (ubel publish/repoint) + **#803** (fern STACK quality panel) + **#804** (lawine STACK private-stability) + **#805** (ubel PLE-dequant +5.3% fire-track) + **#806** (stark non-GEMM attribution). **CLOSED prior cycles:** #789/#771-lg (drafter-dispatch family), #782 (ngram 2.4× slower), #781 (Marlin kernel), #776 (locus), #779 (FlashInfer), #786 (drafter-quant), #787 (verifier-graph), #777 (fp8-KV), #775/#778 (precache), #772 (osoi5).
- **★ CRITICAL PATH TO 250 (int4head fire — STAGED, 1 step from ready):** int4 g32 lm_head ALONE projects to roughly break ~250 official (real served ~253-257 local across four controls; a ~321 mis-edit from the SYNTHETIC oracle was RETRACTED 20:05Z). Panel done (#795 FIRE-WORTHY). Pre-fire gate **1 of 2 GREEN: ✅ ubel #802 MERGED** (published private Hub + serve-from-Hub `official_gate=PASS`, W&B `nidx5trn`); **⏳ remaining = land #801** (within-job greedy-identity + private-gap). When #801 GREEN → I post the full "pre-fire checklist GREEN" on **#800** → fire on human `approved` (per #784, no autonomous launch; per human 13:55Z "high urgency towards submission"; publish-half progress already posted). If official confirms ≥250 → DONE (single lever). **Candidate ladder if int4head lands 240–250** (rungs are orthogonal & quality-safe; acceptance frontier DEFINITIVELY closed — depth #774 + runtime #792 + topology #799 all dead): **(rung-2) PLE-input-gate de-quant +5.3% → ~270 (ubel #805, fire-track, quality-neutral bf16⊃int4)**; **(rung-2') int4head × surgattn-3D stack 272.78 local (fern #797 MERGED, gated on fern #803 + lawine #804 + wirbel #791)**; **(rung-3) the triple int4head × PLE-dequant × surgattn-3D** if both confirm. [lm_head byte-floor #796 CLOSED — exhausted; non-GEMM lever TBD stark #806.] No HF launch without #800 human `approved`.

---

