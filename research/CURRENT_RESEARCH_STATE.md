# SENPAI Research State — Fast Gemma Challenge

- **Date:** 2026-06-15 ~01:35Z (cycle 52i)
- **Advisor branch:** `approval-gated-8gpu-20260613`

## 🆕 Cycle-52i Snapshot (2026-06-15 ~01:35Z) — Path-A bounded from THREE sides at the grounded numbers; the g_d step-basis fork is THE binding decision; pivot to composition-honesty + the orthogonal prefill denominator

**★ THE STRATEGIC TURN: Path-A (the tree route to 500) is now bounded from THREE independent angles at the grounded numbers, and the go/no-go collapses onto a SINGLE measurement — the deployed-path g_d.**

**Three-sided Path-A bound (this cycle's convergence):**
- **Step side — fern #262 (merged):** no tree (shallow 13-node / full / deep) clears 500 at the grounded built step; full tree = **419.97** at grounded central; shallow ~406.6 (band [382.5, 490.8]). Inverted break-even: clearing 500 at the grounded central step needs **E[T] ≥ 5.288** (+0.78 over the full-tree 4.512 projection); deep branch-interior is worth only +0.2117 ⇒ no tree route reaches 500 at the grounded step. The binding constraint **FLIPPED from E[T]/fidelity to the grounded built STEP**.
- **Private-E[T] side — ubel #263 (merged):** the width>1 tree **DEGRADES OOD** — private rank-2+ coverage 0.4768 vs public 0.6532 (−27% rel), branch-salvage ρ collapses ~34% to [0.278, 0.174, 0.123], tree recovers only **73%** of the private E[T]-gap (private E[T] stays at 3.6406). The earlier 505.46 "clears 500" private projection held branch-salvage ρ at PUBLIC values ⇒ refuted; re-prices toward the raw-proxy ~450 regime.
- **Verify side — openevolve (board 01:22Z, via Morgan):** an in-serve dense-mask tree verify does NOT recover λ either ("verify-side is closed too").

**★ COUNTERVAILING — the fidelity wall is DOWN (land #245 Cycle-3, non-terminal checkpoint, advisor GO-to-capture).** The faithful M=16 tree verify reproduces the deployed linear-greedy argmax at **0.832 vs the linear-real ceiling 0.834 — masked tree-vs-linear delta −0.0021 ≈ 0**. The cycle-2 "−0.06 tree penalty" was ENTIRELY scratch-KV plumbing (+0.205 recovered by the real-KV KV-location fix) + a missing tree-causal mask (+0.088), both of which the LIVE build has correct; the per-depth deficit collapses to −0.002…−0.005 once non-ancestor branches are masked out (branch contamination removed, not a divergence repaired). The tree is **as greedy-faithful as linear** ⇒ the fidelity RISK that hung over Path-A for many cycles is **RETIRED**. Path-A's go/no-go is now **purely the step/g_d + E[T] question, not fidelity.** GO to capture (long-pole-1: capture M=16 verify at size-16): land's live built-step measurement **resolves the g_d fork from the build side** (denken #271 resolves it from the isolated-profile side — the two should agree). The ≥0.95 absolute fidelity gate moves to the LIVE integrated path the capture produces (the isolation-ceiling scratch probe caps even linear's own verify at 0.834 under int4-Marlin batch variance).

**★ THE BINDING DECISION — the g_d step-basis fork (denken #268, merged).** Verify-GEMM M-aware TPS(M) is **monotone to M*=32** (the λ-valid ceiling, depth≤9, NOT an interior width — E[T](M) outpaces verify(M) growth), so the go/no-go is NOT about tree width — it is about which **g_d** the deployed step uses: central (measured g_d=0.0195, step M32=1.346 ms) max **480.0 NO-GO**; optimistic (assumed g_d=0.168, step M32=1.119 ms) max **577.6 GO**. The #241 floor 4.3305 never clears central, barely misses optimistic. **denken #271 is measuring the deployed-path g_d directly to collapse the fork — the single highest-leverage open bit in the programme.**

**★ THE SECOND PIVOT — composition-honesty (stark #266, merged).** Runtime adaptive-K is UNREALIZABLE on ONEGRAPH (realizable s/g_d = −5.38 / −1.35, both net-harmful — the static-graph launch-amortization is worth more than the whole per-pass saving). The ONLY survivor is **static-K=4 → 502.12 (+4.28%)** / K=5 → 500.79 — but stark flagged the load-bearing caveat: **the composition OVER-CREDITS draft-pass cuts** (model-forward ~1.2182 ms is a fraction of the conc=1 wall step, the rest is fixed overhead that does NOT shrink), and **deployed-K=7 is the standing evidence** (if cuts paid off at composition rates the frontier would already be at K=4/5). **static-K=4 is a CANDIDATE, NOT a measured ≥500.** This cycle TESTS the over-crediting from both sides: **stark #273** (empirical local wall-clock K-A/B — does K=4/5 actually beat K=7?) + **fern #274** (analytic — decompose step into fixed-overhead + model-forward, derive the φ-correction, re-price every draft-side "clears 500" claim).

**★ THE NEW ORTHOGONAL FRONT — the TPS denominator (ubel #275).** With Path-A closing, the highest-value fresh move is the one benchmark phase NO experiment has examined: `total_wall = prefill + decode`. Every lever to date attacked **decode**; **prefill is unmeasured**. ubel #275 measures the prefill wall-share + decomposes it (target prefill / draft prefill / tokenize / TTFT) + bounds recoverable slack (cross-prompt prefix caching, chunked/cuda-graphed prefill, draft-prefill sharing). A material prefill lever (≥2%) would be a genuine Path-B orthogonal to all spec-decode work; a NULL closes the denominator and confirms decode is the sole front. **High-information either way.**

**Levers CLOSED/exhausted this cycle (added to the plateau map):**
- **Runtime adaptive-K UNREALIZABLE (stark #266):** onegraph launch+host-read (437.6 µs/pass) is 8.2× over the 53.43 µs/pass break-even ⇒ realizable s/g_d < 0. Only static-K survives, composition-level.
- **Draft vocab-head IMMATERIAL (kanna #264):** the proposer is a centroid-routed sparse gather (NOT a dense 256k GEMV — that would be 2.7× the whole pass); the vocab-head is 5.0% of the 101 µs draft floor (< 10% gate) ⇒ NULL. **Relocated the recoverable draft step-mass to MLP (51.7%) + attention (29.1%) = 80.8%** → kanna #269 (MLP) + wirbel #270 (attention); the third term **io_projection (13.9 µs / 14.1%)** → **kanna #277** (reseated after #269 banked — the multi-launch q/k/v/o path may carry more fold-able separable-launch slack than the MLP's single activation kernel, as #264 found the io/embedding path overhead-bound at M=1; result feeds fern #274's portfolio, contingent on stark #273 wall-clock verdict).
- **Verify-side epilogue is a λ=1 ceiling, not a measured gain (wirbel #265):** the verify+accept epilogue IS separately launched (33.8% of the served-vs-built gap) but the +3.84% is a ceiling; the bulk of the gap is verify-forward compute (#257's DIVERGE).
- Prior closures stand (calibration-by-theorem #258, FlashInfer #246, topology doubly-exhausted, draft weight-quant DEAD, n-gram/activation-recycle/verify-epilogue).

**Launch posture unchanged (#124 publish-first, human green-light):** **Live gate: a MEASURED ≥500 build clearing operative λ̂≥0.9780 read against the BUILT step ([1.12,1.43] ms per #257) and the step-dependent E[T] floor.** No measured ≥500 exists; BASELINE stays **481.53** (all cycle-52i merges are bank-the-analysis, 0 TPS). **static-K=4's +4.28% is composition-level, NOT a measured ≥500 — does NOT trigger the launch gate** (stark #273 will measure whether it survives in wall-clock). NEVER launch unilaterally — route via `Approval request: HF job`. **⚠ Launch-readiness flag (issue #272, human-owned):** the active 481.53 frontier `fa2sw_precache_kenyan/sitecustomize.py` LACKS the `_guard_included_router` boot-500 guard (sibling `treeverify` has it); ubel #263 hit the prometheus `_IncludedRouter` boot-500 live on the local container. Intermittent/image-dependent (the 481.53 job booted fine), but a fresh runner image could 500 at boot → score 0. Validated byte-identical port (#177/`bjtwr9jn`: 128/128 token-ids, PPL byte-identical, TPS +0.02%) recommended as a SEPARATE human-approved served-file PR before the next launch.

**Live cycle-52i roster — ZERO IDLE (8/8):**
- **land #245** — real-integration tree verify. **Cycle-3 CONFIRMED the tree is greedy-faithful** (masked M=16 tree-vs-linear delta −0.0021; fidelity wall down, STOP condition not met). Advisor GO-to-capture → capture M=16 verify at size-16, then MEASURE the live built step vs [1.12,1.43] ms + E[T] vs the step-dependent floor (that measurement resolves the g_d fork from the build side). ★ critical-path / launch arbiter (Path-A).
- **denken #271** — g_d step-basis reconciliation (measure the deployed-path g_d to collapse the central-NO-GO / optimistic-GO fork). ★ the single highest-leverage open bit.
- **stark #273** — static-K wall-clock A/B (does K=4/5 actually beat deployed K=7 in REAL local TPS? — empirical composition-honesty). ★ make-or-break for the static-K=4 candidate.
- **fern #274** — composition-overhead honesty (analytic φ-correction; re-price every draft-side 500 claim under fixed-overhead-discounted composition). ★ analytic twin of stark #273.
- **ubel #275** — prefill / TPS-denominator probe (the untouched orthogonal Path-B lever). ★ new front.
- **kanna #277** — draft io_projection roofline (is the 13.9 µs io_projection term intrinsic-M=1 like the MLP GEMVs, or does the multi-launch q/k/v/o path carry fold-able separable-launch slack? completes draft decomposition; ceiling feeds fern #274, contingent on stark #273). **[#269 BANKED: MLP 2.42× above roofline but INTRINSIC-M=1 at 41% BW util; GeluAndMul fold = +4.39% composition ceiling, band +4.4..+5.9% → 502.7–510.1 TPS; over-credit-suspect → stark #273 is the arbiter]**
- **wirbel #270** — draft/tree attention Triton autotune (bit-identical kernel-config slack on the 28.5 µs attn term, 29.1% of the floor).
- **lawine #276** — acceptance local→official transfer (does local λ̂ carry a τ_acc from int4-Marlin batch variance, or is it a kernel-invariant proxy for the official 0.9780 bar? — the E[T]/λ̂ COMPANION to #267's merged `tau_lo=1.03524`, which closed the TPS leg: a local build must read ≥482.98 to clear official 500).

## 🆕 Cycle-52h Snapshot (2026-06-15 ~01:00Z) — land #245 real-path anchor LIVES (tree alive, −0.06); built-step DIVERGES (Path-A knife-edge); adaptive-K is the new bold lever (+13.2% projected)

**★ TWO live routes to 500, both now SHARPER this cycle:**

**(Path-A) land #245 — the tree build's real-path verify ANCHOR LIVES.** Cycle-2 (real-KV M=8 forward, non-terminal `in_progress`, run-local): real-KV anchor = **0.834** (up **+0.235** from the scratch 0.599) — empirically confirming the Cycle-1 read that the scratch KV-location reconstruction WAS the dominant fidelity bug, NOT tree divergence. The decision-relevant signal is the **tree-vs-linear delta = −0.06** (matched scratch conditions), NOT the probe-vs-deployed absolute (provably <1.0 for ANY separate probe: the isolation ceiling caps even linear-greedy's OWN verify at 0.834 under int4-Marlin batch variance + hand-built metadata). land's STOP condition (tree verify itself diverges) is NOT met ⇒ **tree is alive.** **Advisor direction (this cycle):** faithful-tree confirmation run FIRST (port the real-KV KV-location fix into the tree path; predicted ≈0.77; removes the one confound — the −0.06 is currently measured under buggy plumbing) BEFORE the multi-cycle capture spend; then capture the M=16 verify at size-16. Sent back to wip.

**★ BUT Path-A is now KNIFE-EDGE — denken #257 RETIRED the built-step advantage.** The built-step roofline DIVERGED: measured `verify_us(M)` GROWS **+15.8% from M=8→M=32** (knee at M=16; M=8 weight-bound, M=32 compute-bound), so the wall-clock built step re-prices from the analytic 1.085 ms into **[1.12, 1.43] ms** (the 1.085 anchor is RETIRED). ⇒ Path-A no longer wins via a cheaper step; it leans entirely on **E[T] clearing a step-dependent floor that moved UP** — needs **E[T] ≳ 5.37 at the high end** vs the 4.512 projection. Compounded by fern #259: E[T]=4.512 decomposes EXACTLY into spine 4.024 (real-KV-confirmed) + branch-hit 0.276 (real-KV-confirmed) + branch-interior 0.212 (scratch-SUSPECT); a full branch-interior discount → `confirmed_et=4.3003` MISSES even the OLD 4.3305 floor by −0.030 (breakeven x*=0.143). **land #245's LIVE measured build (step AND E[T] together) is the only arbiter; denken #268 is pricing M\*=argmax TPS(M) under the measured roofline (does ANY tree width clear 500?).**

**(Path-B') stark #256 — adaptive-K early-exit is the NEW bold TPS lever: +13.209% projected (the most promising in many cycles).** Confidence-gate the MTP draft (if top-1 c_i<θ, stop proposing pass i+1..7) — **greedy-safe BY CONSTRUCTION** (token-identical over 102,400 fuzzed steps). θ*=0.54: E[T] 3.844→3.360, mean_K 7→4.047, projected 481.53→**545.137**, PPL pinned 2.3772. **CAVEAT: an ONEGRAPH=1 analytic UPPER BOUND** (the static graph replays all 7 passes; a runtime early-exit is incompatible) — NOT a measured ≥500, NO launch. Saving-survival SIGN-robust (survives losing 78% of g_d), MAGNITUDE-sensitive. **stark #266 is converting it to a realizable point estimate (pin s/g_d — does any onegraph realization clear 500?).**

**Levers CLOSED/exhausted this cycle (the plateau map):**
- **Calibration/temperature class CLOSED by theorem (ubel #258):** the 0.75 public→private E[T] gap is a DISCRIMINATION loss, not miscalibration — under width-1 greedy-exact verify, any strictly-monotone transform preserves argmax ⇒ `recoverable_fraction=0.0`. The ONLY greedy-safe E[T]-lifter is a width>1 tree (Path-A). ubel #263 probes whether width>1 recovers the private gap.
- **FlashInfer DEAD-ON-ARRIVAL (lawine #246, CLOSED):** vLLM force-pins TRITON_ATTN for Gemma-4 (heterogeneous head dims 256/512); kernels never init. CUDAGraph already deployed (ONEGRAPH=1, +23% in 481.53), 0% net new, AND toggling not bit-identical. De-risk: linear path captures cleanly at batch 8, NO size-29 crash. lawine #267 now maps local→official TPS transfer (local 465.14 anchor).
- **Draft-side launch-tax NOT separately recoverable (wirbel #261):** the draft argmax+embed epilogue is already inside the ONEGRAPH static replay. wirbel #265 tests the COMPLEMENT (verify-side epilogue).
- **Serve-overhead LIVE-BUT-TINY (kanna #260):** prometheus instrumentation is on the hot path but the non-streaming bench amortizes it to +0.062% (~+0.30 TPS); VIDRAFT's 0.90% does NOT transfer. kanna #264 now attacks the bigger draft-head vocab roofline.
- Prior-cycle closures stand: topology DOUBLY exhausted (wirbel #244 + stark #247), draft weight-quant DEAD on Ampere at M=1 (int3 #248, int4 #254 — bf16 101.2µs/pass is the floor), n-gram (#250), activation-recycle (#251), verify-epilogue (#255).

**Launch posture unchanged (#124 publish-first, human green-light):** publish the ≥500 milestone FIRST, organizers rule validity post-hoc (accepted-risk). **Live gate: a MEASURED ≥500 build clearing operative λ̂≥0.9780 — read against the BUILT step (now [1.12,1.43] ms per denken #257) and the step-dependent E[T] floor.** No measured ≥500 exists; BASELINE stays **481.53** (all cycle-52h merges are bank-the-analysis, 0 TPS). **stark #256's adaptive-K is a NEW potential launch candidate IF stark #266 finds a realization measurably clears 500 locally** (mapped to official via lawine #267's tau_lo) with greedy-identity + PPL≤2.42 + VRAM≤24GB. NEVER launch unilaterally — route via `Approval request: HF job`.

**Live cycle-52h roster — ZERO IDLE (8/8):**
- **land #245** — real-integration tree verify; faithful-tree confirmation run, then capture. ★ critical-path / launch pivot (Path-A).
- **stark #266** — adaptive-K onegraph realizability (convert the +13.2% upper bound to a realizable point estimate). ★ the new bold TPS lever (Path-B').
- **denken #268** — verify-GEMM M-aware TPS(M)→M* (does any tree width clear 500 under the measured step roofline? operationalizes #257).
- **fern #262** — fidelity-safe shallow tree (price the spine+ρ₂ 13-node sub-tree at its own cheaper step — a potential THIRD route sidestepping the fidelity wall).
- **ubel #263** — private rank-2+ probe (does the width>1 tree recover the OOD E[T]-gap — the one lever ubel #258 left open?).
- **kanna #269** — draft MLP roofline (#264 merged NULL: vocab-head immaterial 5%, but MLP=51.7% of the 101µs floor — is the 50.7µs MLP at HBM-bandwidth or overhead-recoverable via fusion/launch-erasure?).
- **wirbel #270** — draft/tree attention Triton autotune (#265 Morgan-merged: attn=29.1% of the floor; bit-identical kernel-config slack at M∈{1,8,16,32}, the lever lawine #246's forced-TRITON_ATTN opens).
- **lawine #267** — local→official TPS transfer (what local TPS clears the official 500 gate? makes every local screen interpretable against the launch gate).

## 🆕 Cycle-52g Snapshot (2026-06-15 ~00:05Z) — step-anchor RESOLVED, topology DOUBLY exhausted, draft-quant DEAD, land #245 at fidelity RISK

**★ THE LAUNCH PIVOT — land #245 cycle-1 is BLOCKED on a verify-fidelity wall (non-terminal, `status:blocked`).** The scratch-reconstruction M=8 clean-room verify reproduces the deployed verifier's per-row argmax only **0.599** (63.3% excl. depth-0) over 7,644 rows — **43% of mismatches are confident** (logit gap ≥ 1.0), only 26% rank-1/2 flips ⇒ a **genuine reconstruction-forward bug, NOT the #192 int4-Marlin batch-variance** the foundation assumed. This **invalidates the scratch probe** as a tree-verify validation tool and leaves the **deep branch-interior contribution to E[T]=4.512 UNVERIFIED** (the SPINE ladder + first-level branch-hit ρ₂≈0.4165 are real-KV-confirmed and survive). **Advisor direction (Option-A, sent back to wip):** pivot to a **real-integration verify** — wire the M=16/M=8 verify into the *real* `execute_model` (real embed/PLE + real split-KV metadata, bypassing the hand-rolled reconstruction) and **measure the real-path anchor FIRST**, before any capture/KV-relocate. **Faithful (→~1.0) ⇒ tree path alive, proceed to long-poles; still degrades ⇒ Gemma-4 argmax diverges under tree-shaped split-KV attention ⇒ tree decode CANNOT preserve greedy-identity ⇒ the whole tree path (and the 520.95 ceiling) is DEAD.** This single anchor is now **the highest-value bit in the programme** — simultaneously the build's unblock-test and the tree-path go/no-go. **Tree path is now at fidelity RISK, not merely unmeasured.** (land also confirmed: eager M=16 tree probe drops 381→57 TPS ⇒ CUDA-graph capture is existential; size-29 crash = width>16 has no captured graph.)

**Step-anchor RESOLVED (denken #252, MERGED): `DISTINCT-STEPS-BOTH-VALID`.** The served step **1.2182 ms** (linear MTP K=7 — the 481.53 path, realized E[T]≈4.6828) and the built step **1.084953 ms** (tree decode — projected E[T]=4.512) are **two different per-step costs for two different paths**, not a contradiction. The **520.95 GO-anchor is CONFIRMED under the BUILT step** (`reanchored_tps_at_ET4512=520.9527`); denken #241's **`E_T_meas_floor=4.3305` SURVIVES** (slope-invariant, moves 0.0). The #241 "463.97 inconsistency" was a path-mismatch artifact (built E[T] priced at served step). Edge: `520.95/481.53 = 1.0819 = 1.1228 (step) × 0.9635 (E[T])`. **⇒ the launch TPS gate reads against the BUILT 1.085 ms step** (denken #257 is grounding it empirically). Caveat: this is a self-consistency round-trip — whether land's LIVE build measures ~1.085 stays land #245's to measure.

**Topology DOUBLY exhausted; draft-quant DEAD; three more linear micro-levers CLOSED.**
- **Topology:** wirbel #244 (static ρ-optimal) + stark #247 (online per-step DP, `e_t_gain_vs_static=+0.0235` below the 0.05 stop, collapses 100% to linear chain at deployed M=8) — the deployed depth-9/branch-3 tree is already optimal. Two independent methods agree.
- **Draft-side weight-quant DEAD on Ampere at M=1:** int3 (#248, −13.68%, no W3A16 kernel) AND int4 (#254, **−27.44%**, W4A16 Marlin 165.79 µs > bf16 101.2 µs — Marlin is tile-optimized, loses at M=1 GEMV). The **bf16 draft 101.2 µs/pass (×K=7 ≈ 58% of step) is the floor** — draft speedups must come from launch-overhead erasure or fewer forwards, not quant. Load-bearing correction: **the deployed draft is bf16, NOT int4** (proposer `/tmp/qat-assistant`).
- **Closed:** n-gram draft (#250, `ngram_beats_trained_anywhere=False`, N-1 dead); activation-recycle (#251, premise structurally void — the 4-layer/256-dim drafter shares target KV and never computes target L1-8); verify-epilogue (#255, already fused on-GPU, `realizable_bound_pct=0.0538%`).

**GOLD pointer (ubel #250 → ubel #258):** the trained draft's **PRIVATE E[T] ≈3.09** (ShareGPT-chat proxy) sits **~0.75 below its PUBLIC E[T] 3.844** — that gap is exactly where the private re-draw validity risk (operative λ̂≥0.9780, `P_invalid=0.05`) lives. ubel #258 is decomposing it (recoverable greedy-safe calibration mismatch vs intrinsic draft weakness).

**Launch posture unchanged (#124 publish-first, human green-light):** publish the 500+ milestone first, organizers rule validity post-hoc (accepted-risk). **Live gate: a MEASURED ≥500 build at the BUILT 1.085 ms step clearing operative λ̂≥0.9780 AND E_T≥4.3305.** No measured ≥500 exists; BASELINE stays **481.53** (all 8 cycle-52g merges are bank-the-analysis, 0 TPS). VIDRAFT external-fleet kernel offer (Issue #124, @chiku-inu) — **Morgan-handled** (holding reply + human escalation; cross-fleet IP-sharing is above autonomous authority); no advisor action.

**Live cycle-52g roster — ZERO IDLE (8/8):**
- **land #245** — real-integration verify (Option-A); measure real-path anchor FIRST. ★ critical-path / launch pivot.
- **lawine #246** — K-1 FlashInfer + CUDAGraph on the linear path (zero-risk route to 500; the launch-overhead lever the dead draft-quant now points to).
- **stark #256** — adaptive-K early-exit drafting (greedy-safe draft-depth gating; fewer forwards).
- **kanna #260** — Plan-B serve-overhead screen (recoverable greedy-safe gain in the prometheus per-request instrumentation?).
- **ubel #258** — public→private trained-draft E[T]-gap decomposition (validity lane).
- **denken #257** — built-step roofline grounding (empirically bracket the 1.085 ms tree step the gate reads against).
- **wirbel #261** — Plan-B Rank-2 draft argmax+embed fusion screen (is the draft launch tax recoverable?).
- **fern #259** — E[T] branch-interior sensitivity (does Path-A survive land #245's scratch-verify finding — does E[T]_lower still clear 4.3305 when the scratch-suspect branch-interior is discounted?).

**Deferred (batch, do not per-screen):** fern two-path portfolio **re-price** — collapse the 3 NO-GO Path-B levers (int3/n-gram/activation-recycle) to measured-0, fold the 4 in-flight replacements (kanna #260 / stark #256 / wirbel #261 / lawine #246) + the land #245 P_A fidelity-downgrade, on the NEXT wave of returns.

## Strategic Snapshot

**We sit at 481.53 TPS (public #8), at frontier *parity* with the linear-stack leaders — the gap to #1 is noise, not a lever.** stark #137 (CLOSED ⚪) confirmed the "8-TPS gap to #1" is **best-of-N official-scorer variance (~1.9%), not a real deficit** — frantic-penguin's own 3-draw spread (489.63 / 483.80 / 480.41) brackets our 481.53. block64 (`FUSED_SPARSE_ARGMAX_BLOCK=16→64`) is a **+0.085% NULL lever**, adopted as a free config line in land's tree manifest (provably greedy/PPL-safe), NOT a launch. ⇒ **500 is a tree-path E[T] story (land #71's descent), not a linear micro-opt story.** kanna #138 (CLOSED ⚪) confirmed **K*=7 holds at block64** (the sparse-argmax tile is off the K7 critical path; block64 is K-neutral + greedy-identical). The memory-BW micro-lever surface is fully exhausted; the researcher-agent sweep (Plateau Protocol) RETURNED → its one orthogonal lever (step-denominator) is now ubel #154→#157.

**THE DECISIVE FINDING THIS CYCLE — the tree's 500-lever is localized.** The oracle has now *run* (openevolve `tree-488-pw-fp32-v0`, board `20260614-100550-487`): **measured E[T]=2.621 → ~271 official (FAILS 500).** Two independent methods now **double-confirm** that the **descending accept walk (BUG-2)** — not the depth-1 spine — is the whole 500-game:
- **fern #134** (official-TPS matrix): descent-only fix → **522 ✅** (E[T] 5.056, clears 500 *even with the depth-1 spine left broken*); spine-only → 283 ❌; both → 538.
- **wirbel #135** (independent E[T]-DP): descent-only → **E[T] 5.041 ✅**; spine-only → 2.746 ❌. **BUG-2 / BUG-1 = 19.3×.** The 391/1024 salvages fire but contribute only +0.077 E[T] (2.9%) — they do not descend.

⇒ **Fix the accept walk and the tree clears 500; the depth-1 spine (0.679 vs 0.7287) is only the 522→538 margin.** Localized on our stack (land #71): `sitecustomize.py` `_dixie_fused_accept_prep_kernel` is a strictly-linear break-on-mismatch chain-reject reused on a flat tree → branches unreachable → ~3% salvage. **land is building the descending replacement now (descent FIRST, spine second).**

**Step-time denominator is FIRM** (lawine #136): measured depth-9 step = **1.2182 (+0.45% vs the 1.2127 roofline)** — the eager attn-launch idle is hidden behind per-layer GEMM. Operative clear-500 bar moves only **4.841 → 4.862**. The binding lever is the **numerator** (descent), not the step.

**🆕 Cycle-52c — the launch verdict FLIPPED to BOTH-BUGS (fern #174 MERGED).** At the SHIPPED step 1.2182 under the full single-shot quadrature (kanna #159 σ_hw=4.86 TPS + input bands), descent-only's P≥0.9 LCB = **499.97 TPS — a knife-edge MISS by 0.035** (P=0.8994); **both-bugs is the robust GO** (LCB 514.88, P=0.9959, GO at all 3 step framings). So the first shot is now **both-bugs**, not descent-FIRST — *unless* lawine #180 confirms #154's argmax-only decode (output-neutral under greedy ⇒ token-identical) realizes step 1.2047, restoring descent-only's +3.96 TPS LCB to GO (the simpler build). **Five launch legs banked this cycle, all confirming the projections hold:** σ_hw=4.86 TPS, cross-allocation-dominated, single-draw P(clear500)=0.791, best-of-2→P≥0.90 (kanna #159); K_cal=125.268 tree-invariant under M=32+PRECACHE_BENCH, zero BW drift (ubel #169); descent-walk kernel step-neutral, ≤0.15% worst (lawine #173); E[T]=5.0564 triple-confirmed but the **adversarial self-KV floor 3.5346 FAILS 500** ⇒ the 520 projection rests on cause-#2 being a fixable build defect (denken #172); native private drop **2.04% CI [1.87,2.21]**, descent-safe, both-bugs NOT a hard private dependency (stark #164). **darwin's `_IncludedRouter` startup-500 = NEW launch-boot dependency** (vLLM 0.22.1rc1 fails boot on fresh a10g; output-neutral fix; kanna #177 validating). **Live cycle-52c roster (zero-idle):** land #71 (descent BUILD, ★critical-path) · fern #179 (packet refresh → both-bugs primary GO) · lawine #180 (argmax-decode descent-only restoration) · ubel #181 (τ-pin, last unpinned composition factor) · denken #178 (graded realistic self-KV E[T] floor) · kanna #177 (darwin boot-fix output-neutrality) · wirbel #175 (E[T] 2nd-moment finite-sample CI) · stark #176 (adverse domain-skew private stress).

**🆕 Cycle-52c update — both paths miss 500 at the REALISTIC self-KV floor (denken #178 + wirbel #175 MERGED).** denken #178 graded the self-KV recovery λ and found `REALISTIC-FLOOR-MISSES-BOTH`: at the openevolve liveprobe anchor λ̂=0.342, descent-only → **404** and both-bugs → **416**, *both miss 500*. The binding constraint for both paths is **depth>0 self-KV recovery λ, not BUG-1** (clear-500 needs λ≥0.838 both-bugs / 0.909 descent; the one pre-build measured point is far below). So fern #174's "both-bugs robust GO" robustness lives at **λ=1**; **the entire 500 case now rests on land #71's built kernel demonstrating measured λ≥~0.84** — its measured `q[2..9]` self-KV ladder is THE gating evidence. wirbel #175 (MERGED) pins the dominant uncertainty: finite-sample single-draw scatter **±10.9 TPS dominates the input band (±2.4) by 4.5×** (sampling-limited, not input-limited); at λ=1 both clear at the LCB (both-bugs +24.5, descent +9.1) — composes with kanna #159 σ_hw → ±11.9 total single-shot. **Two follow-ups attack #178 from both sides:** denken → #183 (**margin-aware λ-acceptance card** — the finite-sample-LCB build bar, stricter than 0.838, + the per-depth `q[2..9]` ladder land #71 tests against); wirbel → #184 (**λ-robust contingency topology** — try to LOWER the bar below 0.838 by front-loading acceptance into λ-insensitive shallow depths). Roster refreshed, zero-idle: denken #183 · wirbel #184 · land #71 (★critical-path build) · fern #185 · lawine #180 · ubel #181 · kanna #177 · stark #176.

**🆕 Human launch question (issue #182, morganmcg1): "want to launch + submit #174?" → answered HOLD.** fern #179 (launch packet, MERGED 16:04, both-bugs GO LCB 514.88) consolidated the go/no-go, but the GO is gated on dependencies NOT yet met: **(1)** the tree build that beats 500 doesn't exist — land #71 is a draft WIP (`terminal:false`, just closed the *build side* of KV-relocate 3c, commit `2437bb9`), the end-to-end descending kernel is not assembled+measured; **(2)** the make-or-break self-KV λ is unmeasured (at the only pre-build point λ̂=0.342 both miss 500); **(3)** the darwin `_IncludedRouter` boot-fix is still in validation (kanna #177). Recommendation: hold the single approved shot until land #71's measured `q[2..9]` ladder clears denken #183's λ≥~0.84 acceptance bar AND kanna #177 confirms the boot-fix — then file `Approval request: HF job` with the *verified* tuple. The 481.53 `fa2sw_precache_kenyan` is already submitted+valid, so there is no downside to waiting. fern #185 (launch-trigger calculator) makes that final GO/NO-GO a one-call, instant decision the moment land #71 measures.

**🆕 Cycle-52c — two of the three launch gates resolve (kanna #177 + denken #183 MERGED).** **(boot CLOSED)** kanna #177 proved the darwin `_IncludedRouter` startup-500 guard is **OUTPUT-NEUTRAL** on the deployed stack (completion-token-ids identical 128/128, PPL byte-identical 2.376976, TPS +0.02% noise; darwin reproduced the 500 3× on the fresh HF runner image) — issue #182's blocker (3) is now a banked zero-cost no-op; land #71 should append the guard to `sitecustomize.py` (exact diff relayed). **(bar SHARPENED)** denken #183 stamped the margin-aware build bar: the finite-sample 95% LCB is **uniformly ~6.6 pts of recovery STRICTER** than #178's point — **both-bugs λ\*_LCB=0.9052** (vs 0.838; at 0.838 the LCB misses at **486.2**), descent-only 0.975. So the binding launch gate is now precisely **land #71's measured λ̂_built ≥ 0.9052 both-bugs**, consumed by fern #185's calculator. **Remaining gates: (1) the build [land #71, ★critical-path] and (2) measured λ̂_built ≥ 0.9052.** Two freed seats attack the *decision rule's* two noisiest inputs: denken → #187 (**λ̂_built measurement-CI** — INPUT-side finite-sample: how many prompts so land #71's q[2..9] ladder decisively resolves 0.9052 at 95%, dual to #175's output-side CI); kanna → #188 (**one-shot launch-draw hardware bound** — decompose σ_hw=4.86 into within-run vs between-device/thermal, reconcile against ubel #181's 0.9924 τ-floor). **Roster refreshed, zero-idle:** denken #187 · kanna #188 · wirbel #184 · land #71 (★critical-path build) · fern #185 · lawine #180 · ubel #186 · stark #176.

**🆕 Cycle-52c — packaging + topology + private gates all bank; launch CI fractures into 4 orthogonal axes (ubel #186 + wirbel #184 + stark #176 MERGED).** Three more bank-the-analysis closures, BASELINE unchanged 481.53: **(packaging)** ubel #186 — the MUST-RETAIN manifest (22 flags, 19 must-retain); **row-1 `relocate_salvaged_kv` host-loop = 85% TPS collapse** (516→77), `LSK_SKIP_LAYERS` a TRAP that must stay UNSET; 25/25 self-test re-loads each banked JSON. **(topology)** wirbel #184 — front-loading is **REFUTED**: no max-branch-3 topology lowers the recovery bar below #83's 0.838 within the ≤depth-9 horizon (min-λ-bar ≡ max-E[T]@λ=1, corr −0.95 — the SAME lever #83 optimized); **#83 confirmed the build target**, the binding lever stays land #71's measured `q[2..9]`. Item-4 bonus: #175's iid clear-500 **FLIPS under heavy within-prompt correlation** (ICC=1→N_eff 128→±54.9→LCB 480.5). **(private)** stark #176 — the adverse-skew private certificate **PASSES**: descent-only survives the worst realistic skew (pure non-Latin-script, drop 2.300%), clearing 500 at both τ corners (τ-low **504.15**, +4.15); `both_bugs_required_private=False` HARDENED, cap-insensitive. **The launch CI now has 4 orthogonal noise axes + 1 packaging precondition + 1 integrator:** denken #187 (INPUT-λ̂ measurement-CI) · kanna #188 (hardware σ_oneshot) · wirbel #190 (within-prompt ICC/N_eff, operationalizing #184's caveat) · stark #191 (PRIVATE-side build bar — compose #176's drop through #183's forward map; does private demand a stricter λ than public 0.9052?) · ubel #189 (executable fail-closed `verify_submission_gate` that auto-catches the row-1 host-loop trap) · fern #185 (the integrator — relayed to consume all four refined bars + the packaging verdict, `binding_bar = max(public 0.9052, ICC-refined, private)`, dropping the iid ±10.9 / 0.9052 to an `iid-fallback`). **Remaining hard gates unchanged: (1) the build [land #71, ★critical-path] and (2) measured λ̂_built ≥ binding_bar.** **Roster refreshed, zero-idle:** denken #187 · kanna #188 · wirbel #190 · stark #191 · ubel #189 · fern #185 · lawine #180 · land #71 (★critical-path build).

**🆕 Cycle-52c — three of the four launch-CI noise axes CLOSE (denken #187 + kanna #188 MERGED; ubel #189 packaging-gate MERGED).** All bank-the-analysis, BASELINE 481.53 unchanged. **(input-λ̂)** denken #187 — the INPUT-side resolvability gate: λ̂_built halfwidth **±0.017** (both-bugs @0.905, 128×512); a true-λ=0.93 build resolves decisively at N≥62 prompts, but a build ON the bar (0.905) is effectively unresolvable (~717k prompts) → **land #71 must aim comfortably clear of 0.9052, not nominally over.** Double-count audit: INPUT-CI (±3.71) and #175's OUTPUT-CI (±5.18) share 89% overlap (ρ=0.945) on the same bench → fern must SUBTRACT the overlap, not naive-quadrature (6.37→5.32 TPS). **(hardware-σ)** kanna #188 — `sigma_oneshot=4.86 == sigma_hw exactly`; the one-shot bound is NOT wider (σ_within 0.056 / σ_between 4.864, ratio 86.6×, cross-allocation dominated, conservative). **(packaging)** ubel #189 — the executable fail-closed `verify_submission_gate` catches the row-1 host-loop NO-GO (444.92 TPS = 85.17%), 34/34 fixtures; the operational safety net before the irreversible shot. **Launch-CI status:** input-λ̂ ✅, hardware-σ ✅, packaging ✅; **remaining in-flight: sampling-ICC (wirbel #190), private-drop (stark #191), the integrator (fern #185), + 2 new — kanna #194 (official re-draw budget N\*(μ) for P≥0.95) and ubel #195 (cross-axis CI covariance — is fern's quadrature-independence valid?).** denken reseated by the parallel advisor to #193 (salvage-staleness λ(depth) mechanism — grounding the flat-λ depth-transfer that #178/#183/#187 all inherit). **Hard gates unchanged: land #71 build + measured λ̂_built ≥ binding_bar (aim clear of 0.9052).** **Roster, zero-idle:** denken #193 · kanna #194 · ubel #195 · wirbel #190 · stark #191 · fern #185 · lawine #180 · land #71 (★critical-path build).

**🆕 Cycle-52c — the 4th CI axis + private bar + re-draw budget CLOSE; BINDING BAR jumps to private 0.9780, descent-only DROPS OUT (wirbel #190 + stark #191 + kanna #194 + denken #193 MERGED).** All bank-the-analysis, BASELINE 481.53 unchanged. **(within-prompt ICC)** wirbel #190 — realistic ICC=0.145 inflates the launch half-width 2.1× (±10.9→±22.9); public bar rises to **0.9513**; both-bugs stays GO (breaks only at 2.6× the realistic ICC), descent-only NOT robust. **(private bar — the new binder)** stark #191 — composing #176's adverse-skew drop through #183's LCB map, the private build bar is **λ\*_LCB=0.9780 both-bugs**, +0.073 STRICTER than public 0.9052, so **`binding_bar = private = 0.9780`**; it FLIPS #176's central-based `both_bugs_required_private` to **True** (the +4.15 central margin evaporates against the finite-sample LCB) and makes **descent-only private-UNREACHABLE** (LCB tops at 490.16<500 even at λ=1) ⇒ **the launch lane is BOTH-BUGS ONLY.** **(re-draw budget)** kanna #194 — best-of-N official shots: **N=5 at the bar (μ=500), N=1 at μ≥512.2** (σ- AND ICC-invariant at the bar); the real risk is a FROZEN-sampling official harness (open Q for the harness owner). **(λ-depth mechanism)** denken #193 (parallel-advisor merge) — geometric staleness `λ_d=λ̂₁·β^(d−1)`, β=0.765: under β<0.965 the bar is UNREACHABLE even at perfect depth-1 recovery, so **land #71 must measure the q[2..9] ladder DIRECTLY, not infer from the spine** (relayed). **🔴 Issue #192 (human, morganmcg1) — greedy token-identity re-raised; honest status posted: under the literal rule the int4-SPEC stack is NOT compliant** (kanna #114's 56.08% divergence from plain greedy AR = int4 Marlin spec-verify GEMM batch-variance; the auto-scorer checks TPS+PPL+128/128 only, not token-identity). Both the 481.53 frontier AND the land #71 launch share this exposure; **launch HELD pending the human's enforcement ruling.** lawine #196 (non-spec greedy-exact FLOOR) + wirbel #199 (batch-invariant-spec E[T] CEILING) now bracket the compliant lane. fern #185 sent back to re-bank the integrator at `binding_bar=0.9780` + #189's real packaging gate + ±22.9 (was iid 0.9052/±10.9). **Hard gates: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] measured directly), (3) issue #192 enforcement ruling.** **Roster, zero-idle:** denken #197 · kanna #200 · ubel #195 · wirbel #199 · stark #198 · fern #185 · lawine #196 · land #71 (★critical-path build).

**🆕 Cycle-52c — the 5th CI axis (cross-axis covariance) CLOSES; fern's quadrature is INVALID (ubel #195 MERGED).** Bank-the-analysis, BASELINE 481.53 unchanged. ubel #195 stress-tested fern #185's `σ_combined=√(Σσ_i²)` independence assumption and found it **fails** — but the violation is a **DOUBLE-COUNT**, not the hypothesized hardware coupling: **ρ(sampling A1, input-λ̂ A2)=+0.945** (the #175 OUTPUT accepted-length scatter and the #187 INPUT λ̂ CI are two views of the SAME accept draw, denken #187's 0.893 overlap). Two honest readings, both kill quadrature-of-both: mechanically the cross-term inflates σ to **15.26** (fern LCB too optimistic by −3.49 TPS); the physically-correct fix **de-dups A1+A2 into one acceptance axis** at 5.32 → 3 independent axes → **σ=7.26**. The hardware↔acceptance coupling is **genuinely UNMEASURED** (co-logged within-device only, σ_within=0.056) → carried bounded [−0.3,+0.3], **worst-case σ=17.04** (PSD min-eig 0.781). **fern #185 now consumes de-dup 7.26 central + worst-case 17.04** (NOT quadrature 12.54), pending land #71's served draw to pin ρ(*,hardware). The additive-CI-law pin (analog of ubel's multiplicative #148/#169/#181). The parallel advisor reseated **ubel → #201 (launch-σ closure: de-dup × realistic-ICC σ→LCB → fern #185)** — the natural follow-up that reconciles #195's de-dup with wirbel #190's ICC into fern's final combined σ. **Issue #124 cross-linked to #192** (the stale 10:47Z "SATISFIED operationally" ruling is the WEAKER reading; the literal-rule status lives in #192, launch HELD). **Queued (no idle seat): the #192 enforcement-reading calibration** — turn kanna #114's one 56.08% divergence number into per-reading (per-sequence / per-token-θ / PPL-only) pass-fractions to arm the human's #192 ruling; assign when a validity-lane seat frees. **kanna #200 (cost-aware re-draw budget) also CLOSED 18:02** — cost-min N=5@bar (c-invariant), sequential early-stop pays only **1.94 shots** (~61% saving), build-higher-N1 wins iff reaching μ=512.2 costs <4 shots; the human's Approval request authorizes the cheaper of {build-higher-N1, bar-N5}, realistic spend ~1.94 sequential shots. kanna → **#202 (frozen-sampling re-draw budget** — does best-of-N beat down all of σ_draw=7.391 or only σ_hw=4.864? the load-bearing #194 §5d risk: if the official harness re-benchmarks FIXED prompts under greedy, σ_sample=5.564 is a frozen per-checkpoint bias best-of-N can't cure → pins the conservative-regime build bar). **Hard gates unchanged: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling.** **Roster, zero-idle:** denken #197 · kanna #202 · ubel #201 · wirbel #199 · stark #198 · fern #185 · lawine #196 · land #71 (★critical-path build).

**🆕 Cycle-52c — the launch-σ closure + the λ-private-drop CLOSE (ubel #201 + stark #198 MERGED); the realistic-ICC launch trigger now lands ON the λ=1 ceiling.** Bank-the-analysis, BASELINE 481.53 unchanged. **(launch σ→LCB)** ubel #201 (`spau6tch`) folds its own #195 de-dup (axis IDENTITY) × wirbel #190 realistic ICC (MAGNITUDE, design-effect D=4.411, √D=2.100 — orthogonal corrections to the SAME acceptance axis) into the single combined launch σ fern imports: realistic ICC pushes the de-duped σ from #195's iid **7.26→12.22 (1σ)** central / **13.80** worst-case (de-dup DROPS the old 17.04 by collapsing the ρ=0.945 double-count); both anchors reproduce exactly (#195's 7.26 at ICC=0, #194's 512.157). **HEADLINE:** realistic ICC erodes ~8 TPS of launch headroom — the P95 GO trigger rises from #194's iid 512.16 onto the **λ=1 ceiling (520.95)**: μ_clear **520.09 central (+0.86 margin) / 522.69 worst-case** (P95-UNREACHABLE at λ=1 under worst-case ρ(\*,hw)). **PROVISIONAL:** ubel honestly flagged a mixed unit basis (95%-half-width acceptance leg vs 1σ σ_hw/σ_priv, inherited from #195); since central sits only +0.86 under the ceiling, the ~3 TPS clean-1σ rebase can FLIP the central verdict ⇒ reseated **ubel → #204 (clean-1σ unit rebase — does λ=1 clear 500 at P95 centrally?)**. fern #185 imports the σ-inflation MECHANISM (12.22/13.80, ~8 TPS erosion) but HOLDS the verdict — two levers finalize it: **#204** (unit-direction) + **land #71 co-log** (n=385 cross-device allocations retires the ρ(\*,hw) [−0.3,+0.3] band). **(private drop, parallel-advisor merge)** stark #198 (`llo1bzn3`) — the λ-coupled drop: directional prior REFUTED but **#191's 0.9780 VALIDATED as a conservative upper bound** (coupling NEGATIVE — #176's shallow-concentrated deficit compounds along the accepted chain → drop is SMALLEST at the realistic floor 2.2935% < 2.3503% at λ=1; coupled bar 0.977978, Δ−3.3e−5, β-robust across [0.62,1.0]; realistic-floor NO-GO UNCHANGED). Conservatism rests on the single measured (non-Latin-script) deficit shape ⇒ reseated **stark → #203 (deficit-shape robustness — is 0.9780 shape-invariant + a co-log spec for land #71's q[2..9])**. **Hard gates unchanged: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling (no human reply yet).** **Roster, zero-idle:** denken #197 · kanna #202 · ubel #204 · wirbel #199 · stark #203 · fern #185 · lawine #196 · land #71 (★critical-path build).

**🆕 Cycle-52c — the LIVEPROBE measurement-cost row + the FROZEN multi-shot budget CLOSE (denken #197 + kanna #202 MERGED); both sharpen the measurement spec / budget-robustness without flipping fern's HOLD.** Bank-the-analysis, BASELINE 481.53 unchanged. **(liveprobe cost + β≈1 verdict)** denken #197 (`wqr94io4`) — the decisive private certification is **30,455 fixed-N Neyman trials @λ=1** (shallow-heavy `N_d[1..9]=[0,7873,…,442]`), but the headline is `mechanism_can_clear_private_bar=False`: at the grounded β=0.765 even *perfect* depth-1 recovery → private_LCB **419.6 ≪ 500**, so the liveprobe is a **β≈1 CONFIRMATION**, not a discovery; full-ladder required (2-depth β-fit unidentified); a depth-1-only probe = **false GO worth 85 TPS**. Reseated **denken → #205 (SPRT sequential early-stop** — the realistic expected-N for the likely NO-GO truth collapses ≪30k; the sequential analog of #197, exactly as kanna #200 was to #194). **(frozen multi-shot budget)** kanna #202 (`533jd6l1`) — the load-bearing #194 §5d risk CONFIRMED: under the more-plausible FROZEN regime (fixed prompts + greedy ⇒ harness re-benchmarks identical tokens), **frozen best-of-5 at μ=500 clears P=0.810 not fresh's 0.969** (best-of-N beats only σ_hw=4.864 = 43% of variance; σ_sample=5.564 is a frozen per-checkpoint bias best-of-N can't cure) ⇒ conservative build-bar **`mu_bar_frozen_p95=504.87`**. The honest INVERSION: `delta_mu_frozen=−7.28` (the frozen N=5 bar 504.87 sits BELOW the fresh N=1 safe point 512.157) — but **build-to-μ=512.2/N=1 is FREEZE-ROBUST** (N=1 regime-invariant, `n_shots_frozen_at_512=1`), so the safe path survives untouched; only the build-at-bar + best-of-N shortcut is frozen-fragile (E[shots]=2.34, exhausts-without-clearing **19%**; `frozen_fraction_breakeven=0.846`). Reseated **kanna → #206 (frozen-cost crossover** — re-prices #200's build-vs-redraw under this frozen curve). Both relayed to **fern #185** (liveprobe cost row = sequential expected-N capped at 30k + full-ladder-or-no-GO; multi-shot row = frozen bar 504.87 default with the build-to-512.2/N=1 hedge). **Hard gates unchanged: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling (no human reply yet).** **Roster, zero-idle:** denken #205 · kanna #206 · ubel #204 · wirbel #199 · stark #203 · fern #185 · lawine #196 · land #71 (★critical-path build).

**🆕 Cycle-52c — the launch-σ trigger RESOLVES from #201's PROVISIONAL knife-edge to a robust YES (ubel #204 MERGED); #201's straddle of the λ=1 ceiling was a UNITS BUG.** Bank-the-analysis, BASELINE 481.53 unchanged. ubel #204 (`m7vwuus2`, advisor-verified — 16/16 scalars, 6/6 self-test, both anchors 0.0, NaN-clean 35 keys) re-based #201's combined-σ on a convention-consistent footing and found the dominant acceptance leg (11.170 TPS) was a z=1.96 two-sided **half-width** that #201 mixed into the 1σ quadrature and THEN hit with z₁=1.645 in the LCB — double-counting z on the variance-dominant leg. Clean fix (÷z₂ → **5.6991 1σ**): combined σ **12.22→7.54** central / **13.80→8.90** worst-case ⇒ clean GO trigger **μ ≥ 512.41 central / 514.63 worst-case**, BOTH below the λ=1 ceiling 520.95 ⇒ **λ=1 clears 500 at P95 CENTRALLY (+8.54) AND worst-case (+6.32)** (was #201's razor +0.86 central / NON-clearing worst-case). Validated two ways: convention-A (all-1σ-then-×z, #194) ≡ convention-B (all-half-width, #190), LCB err 0.0. **Honest self-correction:** `rebase_direction_matches_prediction=False` — ubel's #201 +3.14 prediction had the sign backwards (actual −7.68); the heuristic anchored on the small σ_hw leg, but the mis-based leg was the dominant acceptance leg moving DOWN. **One residual (ubel flagged, → reseat):** the clean acceptance magnitude traces to #187's `h_out`=5.178, but #195/#190 carry a LARGER #175-sampling half-width 10.906 (differ by ~√D); if 10.906 is launch-correct the trigger moves UP ⇒ reseated **ubel → #207 (launch-σ #175-reading reconcile — does the robust-YES survive the larger reading?)**. **fern #185 wires the clean 512.41/514.63 trigger** (retires #201's 520.09/522.69); land #71's co-log (n=385) now *tightens* a YES rather than rescuing a NO. **Hard gates unchanged: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling (no human reply yet).** **Roster, zero-idle:** denken #205 · kanna #206 · ubel #207 · wirbel #199 · stark #203 · fern #185 · lawine #196 · land #71 (★critical-path build).

**🆕 Cycle-52c — the compliant-#192 bracket CLOSES + the launch-σ trigger and liveprobe cost-row finalize; 8 bank-the-analysis legs merged since #204, BASELINE 481.53 unchanged, all 3 hard gates now EXTERNAL.** **(launch-σ trigger pinned)** ubel #207 (`17vi7fda`) resolved #204's residual: the two #175 readings (h_out 5.178 vs #175-sampling 10.906) are the SAME finite-sample CI at different bench sizes (65536 vs 16384), √D ratio coincidental → launch-correct = 5.178 ⇒ **robust-YES SURVIVES, GO trigger 512.41 central / 514.63 worst-case STANDS** (both < λ=1 ceiling 520.95). **(multi-shot budget)** kanna #210 (`hwvv7nn1`) — best-of-N is selection on non-replicating PUBLIC noise; conditional PRIVATE clear is FLAT in N ⇒ **build higher, not redraw**; clearing 500-private at P≥0.95 under best-of-5 needs μ_pub=528.48 (+23.61 winner's-curse tax); **n_star_private=1**. **(liveprobe cost)** denken #205→#212 (`b70053sw`): SPRT collapses the NO-GO certification ~75× (E[N]=405 vs fixed-N 30,455), and #212's AR(1)/ACF correction confirms #205's flat ×Deff=4.41 is conservative — realistic E[N]_nogo=1,125 (measured-ACF) / 672 (AR(1)), the 75× collapse Deff-invariant. **(private bar HARDENED)** stark #208 (`wi4gxxx8`) — explicit LP over ALL realizable domain blends proves **0.9780 is the TRUE worst-case** (non-Latin-script is the maximizing vertex, optimum_exceeds_nls=False) — the last argued-from-construction private assumption now closed. **(the #192 compliant bracket — CLOSED)** under strict-A token-identity (byteshark's board read; human ruling still pending) the deployed int4-batch-VARIANT spec stack is NOT compliant (kanna #114's 56.08% per-token argmax divergence); the compliant 500-lane is now fully bracketed — lawine #196 non-spec FLOOR 165.44 (no compliant non-spec 500) · wirbel #199 batch-INVARIANT-spec CEILING 536.66 (LCB 525.73) · wirbel #213 the `max_kernel_overhead_pct(λ)` curve (**λ_crit=0.8345 both-bugs** — below it even a FREE kernel misses 500; off-the-shelf #122 +51.78% clears at NO physical λ) ⇒ the compliant 500-path is a DOUBLE gate (λ≥0.834 AND a custom batch-invariant verify under budget), now feasibility-probed by wirbel #216; denken #219 prices the GAP (per-reading pass-fractions A/B/C from #114's 56.08% — strict-A pass ≈0%, PPL-only=100% — to arm the human's #192 ruling). **(DFlash #211 — DO NOT PURSUE)** human-raised diffusion-block spec-decode: researcher verdict DISQUALIFYING — the JarvisLabs +16% was on MoE Gemma-4-26B-A4B; E4B is DENSE (DFlash −2.5% vs MTP on dense), no E4B checkpoint, needs ≥2 GPUs + unreleased recipe, doesn't touch #192 (only verify matters); expect ~469 regression, P(>500)<5%. **(land #71 build coordination)** chiku-inu (build owner) re-engaged on the deep tree post-fp32-refutation; published fern's ρ-optimal **M=32 parent array** (`build_target_M32_parent`, depth-9/max-branch-3) + per-position descent-salvage targets + the 0.9780-both-bugs/q[2..9] build bar to the board, and recommended the non-colliding lane (chiku-inu takes the parent-array-driven descending accept-walk; fabulous/byteshark land the duplicate-row identity — orthogonal, compose later). **Hard gates UNCHANGED & all external: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling (no human reply yet).** **Roster, zero-idle:** denken #219 (#192 per-reading calibration) · wirbel #216 (compliant-kernel feasibility) · kanna #217 (launch-trigger reconcile 512.4/528.5) · ubel #218 (inter-leg ρ grounding) · stark #215 (deep-tail build-bar budget) · lawine #209 (frozen-vs-fresh regime pin) · fern #185 (★integrator) · land #71 (★critical-path build).

**🆕 Cycle-52c — the compliant-verify lane PIVOTS to MarginGate; the Issue #211 kernel gate returns lane-a REAL-but-doubly-conditional; and a NEW load-bearing finding lands — the λ=1 ceiling clears the PUBLIC trigger but MISSES the PRIVATE bar by +7.53 TPS (3 bank-the-analysis merges this turn, BASELINE 481.53 unchanged, all 3 hard gates still EXTERNAL).** **(kernel feasibility — the #211 gate)** wirbel #216 (`pc8g6s04`) — the compliant batch-invariant int4 verify kernel (lane-a) is a REAL 500-path but DOUBLY conditional and a buildability *prior*, not a proof: off-the-shelf `VLLM_BATCH_INVARIANT=1` scoped to the verify GEMM is **+31.41%** (clears at NO physical λ), but a CUSTOM split-K-order-fixed kernel has a first-principles **floor ~0.95%** (inside the 7.33%@λ=1 budget) — feasible iff **(a)** land #71 builds λ≥0.857 (`lambda_min_kernel_feasible=0.8572`) AND **(b)** the kernel lands near its floor; band [0.95%, 31.4%] is 54.8× wide and the decisive uncertainty is a CHEAP ~1–2 day GPU split-K microbench, NOT a full-node run. **(trigger reconcile + the NEW private-bar finding)** kanna #217 (`vgovdrjc`) — the unified order-stat trigger `T(N)=T_base+σ_sel·E[Z_(N:N)]` reproduces #204's 512.41/514.63 at N=1 EXACTLY (`n_star_launch=1`, best-of-N HARMFUL), and resolves the 512.4-vs-528.5 tension by **AXIS not N**: 512.41 = N=1 PUBLIC GO trigger, 528.48 = N-independent PRIVATE build target (mu_safe_fresh/f_priv, f_priv=0.969107). **LOAD-BEARING: `lambda1_ceiling_clears_private_bar=0` — the physical λ=1 ceiling 520.95 clears the public trigger but misses the private bar by +7.53 TPS** (`private_bar_minus_ceiling=7.5308`; private mean at ceiling 504.86, P95 LCB 492.70<500, private clear only **0.744**). Student honestly corrected the PR's premise (the literal 23.61 is a composite vs the #202 504.873 bar, NOT the order-stat tax 5.66 frozen/8.60 fresh). **(the #192 ruling menu)** denken #219 (`0unwptbz`) — converts kanna #114's 56.08% into the per-reading menu the human's #192 ruling picks from, using #114's banked PER-SEQUENCE split directly: **strict per-sequence token-identity (A) = 0.125 (16/128 OBSERVED)** — 88% of served sequences NOT token-identical, robust to maximal clustering (cap 1−p=0.439<0.5, `strict_a_robust_to_clustering=1`); per-token-θ CDF {0→0.125, 0.05→0.141, 0.5→0.383, 1→1.0}; PPL-only (C, the scorer's actual check) = **100%**; frontier-wide exposure (`applies_to_frontier_and_tree=1`). **(researcher gold-mine — the lane PIVOTS)** a fresh Plateau-Protocol sweep (`research/RESEARCH_IDEAS_VALIDVERIFY_2026-06-14.md`) surfaced a HIGHER-ranked valid-verify path the fleet had NOT priced: **Rank-1 MarginGate (arxiv 2605.30218, P=0.45)** — compute the logit margin m=top1−top2; if m>2·ε_max the argmax is PROVABLY stable regardless of split-K reduction order → **skip the verify GEMM for ~81–85% of positions**, bf16 M=1 fallback (==M=1 AR, greedy-identical) for the unstable ~15–19%; Rank-2 MarginGate+DVR hybrid (P=0.40, DVR on the residual low-margin only); Rank-3 fixed-tile Triton verify (P=0.35, ~5–6%, borderline); Rank-4 DVR-standalone FAILS at our 56% flip (~380–430 TPS); **FP8-on-A10G ELIMINATED** (sm_86 has no FP8 tensor cores → falls back to Marlin W8A16, same non-determinism). The decisive diagnostic for BOTH top paths is the SAME cheap probe — `verify_flip_probe` (margin distribution flipped-vs-stable → ε_max + skip rate + the int4 rollback rate chiku asked openevolve for), ~1–2 days. **(the #211 Blackwell recommendation — relayed to the human)** both top valid-verify paths (the #216 custom kernel and MarginGate) are gated on CHEAP GPU diagnostics, not on draft quality, and a better draft can't make an invalid stack valid (the human's own #211 point) ⇒ recommendation: **authorize the two ~1–2 day diagnostics first** (verify-GEMM split-K floor microbench + `verify_flip_probe` margin distribution), **HOLD the Blackwell draft-training** until a verify path is confirmed AND land #71's λ tightens ≥0.857; I authorize nothing, the human decides. **(reseats — the validity lane fans out)** denken → **#225 (gate-2 confirmation runbook** — consolidate #205 SPRT + #212 AR-ASN into the procedure that confirms measured λ̂_built≥0.9780 both-bugs from ONE served run's q[2..9] data [65,536 positions]; decision rule λ̂_LCB≥0.9780⇒PASS, [0.857,0.978)⇒HOLD kernel-feasible-below-private-bar, <0.857⇒NO-GO); wirbel → **#223 (MarginGate budget** — price `skip_rate_min(λ)` vs #213's 7.33%@λ=1; arms `verify_flip_probe`); kanna → **#224 (private-bar reachability** — ground f_priv [0.969 assumed vs #52-observed 0.957] + what closes the +7.53 ceiling-vs-528.48 gap; is 500-private reachable at the ceiling?). Parallel-advisor-seated and OFF my action list but on the same validity lane: ubel #222 (which-gate-binds — does clearing λ̂=0.9780 auto-clear the 513.557 speed trigger?), lawine #221 (fp16-verify cost+validity — MEASURE the fp16/int4 step multiplier + batch-width token-identity), stark #220 (fp16-verify ceiling map — can ANY draft clear 500 without an int4 kernel?). **Hard gates UNCHANGED & all external: (1) land #71 build, (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct), (3) issue #192 enforcement ruling (no human reply yet; advisor posted the per-reading menu to the board per the human's "see what the others think").** **Roster, zero-idle:** denken #225 · wirbel #223 · kanna #224 · ubel #222 · lawine #221 · stark #226 (private-bar worst-case f_priv; #220 fp16-verify-ceiling MERGED→reseated) · fern #185 (★integrator) · land #71 (★critical-path build).

**🆕 Cycle-52c — the launch INTEGRATOR banks + the 4 post-#217 launch-readiness legs all CLOSE; the launch picture SHARPENS (5 bank-the-analysis merges this turn, BASELINE 481.53 unchanged, all 3 hard gates still EXTERNAL).** **(the integrator — banked)** fern #185 (`cw4naa0t`, 18/18 self-test, advisor-merged 21:07) is the one-call `launch_decision(measured_tuple)` → GO/NO-GO + pre-filled (un-filed) `Approval request: HF job`; snapshot consumes up through kanna #217 (binding_bar=0.9780, public σ→LCB trigger 512.41/514.63, two-flag public-vs-private structure, `launch_authorized=False`). **(validity binds)** ubel #222 (`yw7i2ece`) — at the validity bar λ̂=0.9780 the build's public μ_pub=**515.924 ≥ 513.557** speed trigger (+2.367) ⇒ land #71 has a SINGLE launch target 0.9780, fern reads the VALIDITY gate alone (speed trigger dominated). **(MarginGate REFUTED)** wirbel #223 (`54dtull1`) — to clear 7.33%@λ=1 a sound margin gate needs skip ≥**0.9706**, but provably-stable ⊆ non-flip ⇒ skip ≤ 1−flip=**0.4392 ≪ 0.9706**; Hybrid+DVR also misses (rollback burden=flip_rate, constant in skip) ⇒ the ONLY compliant-500 route is the custom batch-invariant int4 kernel (#216), conditioned on the unmeasured <7.33% microbench. **(f_priv grounded — private bar RISES)** kanna #224 (`1081oc84`) — the assumed f_priv=0.969107 was OPTIMISTIC; the frontier's one hard paired draw (#52: 481.53/460.85) grounds **f_priv=0.957054**, lifting the private build target to **mu_ceiling_needed=535.14** ⇒ **500-PRIVATE is UNREACHABLE at the physical λ=1 ceiling** (private mean at ceiling 498.58<500); the kernel-ceiling route must target ~528–535, not 500. **(gate-2 confirmable in ONE run)** denken #225 (`851z7itj`) — a Wald SPRT certifies measured λ̂_built ≥ 0.97798 both-bugs (q[2..9] direct) in **n_confirm≈1,125 decode steps** (measured-ACF), which FITS one served run's ~65,536 q[2..9] positions; decision rule **λ̂_LCB≥0.97798⇒PASS / [0.857,0.978)⇒HOLD / <0.857⇒NO-GO**. **Net — the HOLD is not flipped, it is SHARPENED:** single validity gate (0.9780, #222), #216-kernel-only compliant route under strict #192 (#223), private bar 535 not 528 (#224), one-run gate-2 rule (#225). **(reseats — parallel advisor seated all 5 freed students mid-turn; my fern integrator-re-run was refused by the duplicate-guard, orphan branch deleted)** wirbel → #227 (valid-verify cluster capstone) · kanna → #228 (publish-first λ floor) · ubel → #229 (speed-margin at validity bar) · denken → #230 (gate-2 depth-resolved power) · fern → #231 (launch-readiness GO-card — pre-register the decision on the one unknown). **🆕 LAUNCH-POSTURE CORRECTION — human issue #124 green-light (19:04Z) RETIRES #192 as a self-blocker.** The human's authoritative decision: **publish the 500+ milestone FIRST and let the organisers rule on validity post-hoc** (logged as accepted-risk, NOT unresolved) ⇒ #192 is no longer a launch *blocker* from our side. **The ONE honest remaining blocker is PHYSICAL, not procedural: we have no ≥500 built checkpoint yet** (land #71 is `terminal:false, pending_arms:true`, no W&B run; the deep self-KV tail q[8..9] is unmeasured). **Unblock path:** the moment land #71 posts a terminal built ≥500 checkpoint with the measured λ̂ tail, the advisor opens `Approval request: HF job for <name>`. **So the live hard gates are now TWO, not three: (1) land #71's ≥500 build [★critical-path, PHYSICAL] and (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct) — the #192 procedural gate is RETIRED (accepted-risk; the validity-lane analytics #216/#222/#223/#224 remain banked as the post-hoc-defence packet, not a pre-launch blocker).** **Roster, zero-idle:** denken #230 · ubel #229 · kanna #228 · wirbel #227 · stark #226 · lawine #221 · fern #231 · land #71 (★critical-path build).

**🆕 Cycle-52c — three more validity/launch legs CLOSE post-#124; the launch picture is fully SHARPENED to a single public gate + a post-hoc-defence packet (3 bank-the-analysis merges this turn, BASELINE 481.53 unchanged).** All CPU-only, 0 TPS, greedy/PPL untouched, authorize nothing. **(single validity gate HARDENED)** ubel #229 (`bz2b3fw8`, advisor-verified, 6/6 self-test) — `VALIDITY_BINDS_SPEED_ALWAYS_CLEARS`: #222's +2.367 worst-case margin was already scored at the PRECISE marginal validity λ=0.9779783 (where E[T]/speed are lowest), and the smallest λ where worst-case speed clears is `lambda_speed_clears=0.9675 < 0.9780` ⇒ the `[0.9780, X)` validity-passes-speed-fails band is PROVABLY EMPTY; clearing validity necessarily clears the 513.557 speed trigger at every #218 σ corner (+2.367 worst / +3.189 central / +3.405 tight) ⇒ fern carries ONE gate. **(valid-verify menu → ONE survivor)** wirbel #227 (`o674wmna`, advisor-verified, 6/6, 13/13 pins) — the four-path menu collapses to `n_surviving_valid_500_paths=1` (lane-a int4 batch-invariant kernel; lane-b fp16 306.44 / MarginGate 227.13 / lane-c no-spec 165.44 all structurally <500), itself DOUBLE-gated (near-floor kernel BUILD ∧ land #71 λ≥0.8572); the #211 Blackwell DFlash draft helps ONLY gate-b (E[T](λ)), NOT the draft-independent split-K-reduction-order kernel build (gate-a) ⇒ `cluster_necessary_but_not_sufficient=True`, verdict `CLUSTER_UNLOCKS_500_VIA_LANE_A_ONLY_IF_KERNEL_BUILT` — authorize the cheap ~1–2 day split-K microbench FIRST, draft-training conditional on it (matches the #211 recommendation; advisor authorizes nothing). **(private bar — blend axis EXHAUSTED)** stark #226 (`tzcc5xuq`, advisor-verified, a–f) — re-pointing the #208 worst-case-blend LP from the λ-axis to the f_priv axis finds NLS is BOTH the λ-deficit-maximizing AND the f_priv-minimizing vertex, so `private_bar_worstcase`=528.48 == kanna #217's central (ZERO blend spread); compliant lane private-FEASIBLE vs the 536.66 central ceiling (−8.18) but INFEASIBLE vs the 525.73 LCB (+2.75); the binding uncertainty migrated to the decode-drop CALIBRATION (the lone #52 empirical f_priv=0.95705 → bar 535.14 sits ~1.5× OUTSIDE the realizable simplex, +0.0027 above break-even) ⇒ fern carries the private bar as the INTERVAL [528.48 central/worst-blend, 535.14 empirical-floor]. **Net: under #124 publish-first the operative launch gate is the PUBLIC milestone (TPS≥500/PPL≤2.42/128/128); the private bar + compliant-lane analytics (#222/#223/#224/#226/#227) are the POST-HOC-DEFENCE packet, not pre-launch blockers. Live hard gates remain TWO: (1) land #71's ≥500 build [★critical-path, PHYSICAL] + (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct).** **(reseats — all 3 freed students re-seated, zero-idle held)** ubel → #234 (publish-first public margin — the σ→LCB public GO margin at the #124 floor, `λ_public_gate=max(0.9675, lambda_floor_publish_first)`, completing #229's flagged cross) · wirbel → #235 (two-ceiling reconcile — is the operative compliant ceiling 536.66 or 520.95, and does it flip stark #226's compliant-private-feasible verdict? if 520.95 the compliant-500-PRIVATE lane is DEAD even with a perfect kernel) · stark → #233 (publish-first f_priv-breakeven, parallel-advisor-seated). **Roster, zero-idle:** denken #230 · ubel #234 · kanna #228 · wirbel #235 · stark #233 · lawine #232 · fern #231 · land #71 (★critical-path build).

**🆕 Cycle-52c tail — gate-2 depth + publish-first floor CLOSE (2 more bank-the-analysis merges this turn, BASELINE 481.53 unchanged); the launch-readiness packet is now fully populated except the build.** All CPU-only, 0 TPS, greedy/PPL untouched, authorize nothing. **(gate-2 deep tail is the binding one-run constraint)** denken #230 (`bo706b7n`, advisor-verified, 6/6) — decomposing #225's aggregate gate-2 ASN by tree depth shows the comfortable 58× one-run margin is mostly the cheap spine (q[2..7], ≈188 positions each); the sparse deep tail q[8..9] (λ_ref 0.7875 budget, **56×** the spine Bernoulli variance, only 9% reach mass) needs `n_confirm_deeptail`=**10,508** accept positions vs **13,366** available → a **1.27× one-run headroom** (46× thinner than the aggregate) ⇒ the deep tail is the BINDING constraint on one-run gate-2 confirmation (still confirmable in ONE served run, but land #71's per-step reach-DP dump should tighten it; honest bookend: double-Deff ACF×ICC deflation would need >1 run). **(publish-first POINT-estimate floor = #191 central bar to machine eps)** kanna #228 (`352ifoi8`, advisor-verified, 8/8) — inverting the central private-mean curve for `private_mean=500` gives `lambda_floor_publish_first`=**0.913827** == stark #191's `lambda_star_central_private` (resid **8.88e-16**); **advisor-CONFIRMED the CENTRAL reading** is the correct #124 publish-first point-estimate gate (NOT the conservative-LCB-ceiling 0.97783, which collapses onto the 0.9780 P95 bar by construction and erases the accepted-risk band). **The three launch λ-thresholds now nest cleanly: 0.9138 (private mean=500) < 0.9675 (ubel #229 public speed clears, the operative #124 launch gate) < 0.9780 (P95 private valid).** **(reseats — both freed students re-seated, zero-idle held)** denken → #236 (PPL public-gate headroom — is PPL≤2.42 a THIRD binding public gate under the lossy int4 verify? frontier #52 serves 2.3772, margin only 0.0428 — the one public-milestone condition nobody has priced) · kanna → #237 (publish-first accepted-risk curve — `P(private draw clears 500 | built λ)` across [0.9138, 0.9780], quantifying how much single-draw risk #124 actually accepted; consumes σ_draw 7.391). **Roster, zero-idle:** denken #236 · ubel #234 · kanna #237 · wirbel #235 · stark #233 · lawine #232 · fern #231 · land #71 (★critical-path build).

**🆕 Cycle-52c tail — the launch-validity analytic packet is COMPLETE but the launch DOES NOT EXIST: land #71's reset reveals NO measured ≥500 artifact (7 bank-the-analysis merges this turn #231–#237, BASELINE 481.53 unchanged); advisor banks the foundation + scopes the live build as the SOLE critical path.** All CPU-only, 0 TPS, greedy/PPL untouched, authorize nothing. **(THE RESET — load-bearing)** land #71's honest status (22:12Z): there is **NO working live tree decode** — served = linear MTP K=7 (≡ the 481.53 path), and the +18.3% / `E[T]_both`=4.512 / ~520 TPS is an **UNMEASURED analytic projection** over observational env-gated probes that never modify committed tokens (`treeverify_served_gain_MEASURED_realized=0.0`; the ~1,400 lines of "tree" code are additive probes, not a live decoder). The long pole is the live tree-decode build's two real-risk pieces: **(1)** CUDA-graph capture past the size-29 capture crash + widen target verify capture to tree M=16; **(2)** live 3c KV-relocate for continued-gen — exactly where every external fleet team (byteshark/cheesetaco/chiku-inu) stalled to ~0 salvage. **(THE (B) DECISION)** the entire launch-validity packet (now complete) defends a launch that doesn't exist ⇒ advisor banks the foundation as terminal + grinds the live build as the #1 critical path, and posted a board readiness-correction to the human: **we do NOT launch on a projection.** **(the 7 merges)** #231 fern GO-card (READINESS=NOT-YET, `launch_authorized=False`) · #232 lawine **0.73% deployed divergence** (M=8, split-K reduction order = f(M); the 56.08% was an M=1 artifact) · #233 stark f_priv breakeven **0.9598** (realizable band straddles) · #234 ubel public margin (gate 0.9675, 0.0→+2.367 ramp to 0.9780) · #235 wirbel **INFEASIBLE FLIP** (operative compliant ceiling **520.95** int4-spec < private bar 528.48 → compliant-private-500 lane DEAD at the current topology; the 15.71 TPS gap is 100% topology/coverage, NOT kernel) · #236 denken **PPL-not-binding** (`ppl_headroom_at_build_bar`=0.0428, λ-invariant; the public milestone is single-gated on TPS≥500) · #237 kanna accepted-risk (**5.83% assumed / 23.94% grounded** single-draw private-miss at the 0.9675 gate). **(researcher-agent — build-unblock gold)** a background Plan-B TPS sweep landed (`research/RESEARCH_IDEAS_2026-06-14_22:30.md`): **Ranks 3 + 5 map EXACTLY onto land #71's two blockers** — pinned-memory async KV-relocate (the 3c blocker) + a multi-shape CUDA-graph suite keyed by M ∈ {1..K+1} (the size-29 crash); plus stackable micro-levers (draft argmax+embedding fusion +3–6%, KV-page pre-faulting +0.3–0.8%) as the Plan-B if the tree build collapses like the external fleet's. **(reseats — all 7 freed students re-seated, several DIRECTLY serving the now-critical build)** fern → #238 (integrated GO/NO-GO card refresh — folds #232–#237 + carries the #235 INFEASIBLE flip) · kanna → #239 (f_priv-distribution integrated risk band) · ubel → #240 (TPS↔risk exchange rate dTPS/drisk) · denken → #241 (**measured-E[T] shortfall tolerance** — δ_max / `E_T_meas_floor`, pre-registers land #71's one build run pass/fail vs the 4.512 projection) · lawine → #242 (**M=16 divergence projection** — project the 0.73% M=8 fact to the build's M=16 via the split-K mechanism + a confirming-probe flag) · stark → #243 (f_priv worst-case re-priced under the measured 0.73% divergence) · wirbel → #244 (**topology headroom** — price the +15.71 E[T] lift; is the compliant-private lane reopenable or certified DEAD?). **Live hard gates remain TWO and are now the SOLE blocker-set: (1) land #71's ≥500 build [★critical-path, PHYSICAL — the one real long pole] + (2) measured λ̂_built ≥ 0.9780 both-bugs (q[2..9] direct). The #124 publish-first milestone (TPS≥500 / PPL≤2.42 / 128/128) is the operative public gate; the validity-lane packet (#216/#222/#223/#226/#227/#235/#236/#237) is the POST-HOC-DEFENCE bundle, not a pre-launch blocker.** **(land #71 foundation MERGED → live build #245 OPENED, zero idle gap)** the parallel advisor merged land #71 as bank-the-foundation at 22:36Z (terminal `treeverify_served_gain_MEASURED_realized`=0.0, BASELINE stays 481.53, served = linear MTP K=7) and immediately opened **#245 "Live tree-decode build: commit tree-accepted tokens at measured wall_tps (the launch gate)"** as land's reseat — the launch gate is the first `treeverify_served_gain_MEASURED_realized>0` at valid-PPL representative wall_tps; build-unblock recipes (Plan-B Ranks 3+5) relayed. **(Plan-B route banked + UNASSIGNED — top-priority next reseat)** the researcher-agent speed-lever menu is committed (`research/RESEARCH_IDEAS_2026-06-14_speed-levers.md`, 685f388): its TOP lever **K-1 FlashInfer+CUDAGraph on the WORKING linear MTP K=7 path** is a ZERO-greedy-identity-risk (+7–18% → 515–568 TPS) route that could clear 500 WITHOUT the stalled tree build (bit-identical fused attention + deterministic step replay) AND directly bridges land #245's size-29 CUDA-graph blocker — currently assigned to NO ONE (all 8 seats busy) ⇒ K-1 is the first reseat the moment a seat frees (task #106; reusable body `/tmp/rx_wirbel_k1.md`; arXiv:2501.01005, vLLM `--attention-backend FLASHINFER`); T-1 OPT-Tree per-step DP (E[T] 5.066→5.219, zero risk) is the topology-upside companion, partially covered by wirbel #244. **Roster, zero-idle (8/8):** land #245 (★critical-path LIVE tree-decode build) · fern #238 · kanna #239 · ubel #240 · denken #241 · lawine #242 · stark #243 · wirbel #244.

**🆕 Cycle-52e — the #232-divergence launch-validity cluster banks (5 bank-the-analysis merges #238/#239/#242/#243/#240, BASELINE 481.53 unchanged); the freed seats PIVOT from analysis to OPENING INDEPENDENT LOCAL-BUILD ROUTES to a measured ≥500.** All CPU-only/local-profiling, 0 TPS, greedy/PPL untouched, authorize nothing. **(the 5 merges)** fern #238 GO/NO-GO card v2 — `n_green_gates=2`, NOT-READY pinned to `treeverify_served_gain_MEASURED_realized==0.0` (mechanically refuses to read the ~520 projection as a win); only PPL+128/128 unconditionally GREEN · kanna #239 — integrating the f_priv distribution collapses #237's 4× spread into ONE number `integrated_risk_at_speed_gate=0.1347`, and the load-bearing find: f_priv UNCERTAINTY raises the 5%-draw-risk build-λ to **0.9808 (div-informed) / 0.9861 (uniform)** — ABOVE the 0.9780 P95 bar · stark #243 — the measured 0.73% un-straddles the publish-first breakeven (worst-case f_priv 0.96895 > 0.95978; λ_floor 0.978413) · lawine #242 — int4 divergence PLATEAUS at M=16 (`projected_divergence_at_M16=0.0073`): all four int4-Marlin BODY GEMMs are M-INVARIANT bit-exact, the divergence locus is the bf16 lm_head (NOT int4), so the build's M=16 verify holds the deployed 0.73% — load-bearing for land #245 · ubel #240 — **speed and safety are CO-MONOTONE in build-λ** (`tps_per_pct_risk=−0.6431`): there is NO speed-for-risk trade; the real #124 tension is build-λ vs landing difficulty (land #245), not speed-vs-safety (PRIMARY=leg-correctness with the premise refutation surfaced separately — adopted as the self-test-hygiene standard). **(THE STRATEGIC PIVOT)** the launch-validity analytic packet is now COMPREHENSIVE and the GO-card reads NOT-READY for ONE reason — there is no measured ≥500 artifact — so further analysis has low marginal value; the freed seats now OPEN MULTIPLE INDEPENDENT LOCAL-BUILD ROUTES to a measured ≥500, each at a different mechanism level (from the committed speed-lever menu `research/RESEARCH_IDEAS_2026-06-14_speed-levers.md`, all ZERO greedy-identity risk, no gradient training, local-profilable): **lawine #246 K-1 FlashInfer+CUDAGraph** (systems, the WORKING linear path, +7–18%→515–568, the top zero-risk route — now SEATED, resolving cycle-52d's UNASSIGNED-K-1 flag; lawine's size-29 expertise bridges land #245's capture blocker) · **stark #247 T-1 OPT-Tree per-step DP** (topology — lifts E[T] past the static ρ-optimal 5.219 toward the reach-DP ceiling) · **kanna #248 Q-1 QSpec int3 draft** (draft-side post-training quant, +5–10%, stacks) · **ubel #250 N-1 n-gram draft source** (is there free E[T] in the prompt structure? diagnostic-first) · **fern #249 build-λ̂ target reconciliation** (the adjudication piece: resolve 0.978 worst-case-vertex vs 0.981–0.986 f_priv-integrated-5%-risk vs 0.9780 P95 into ONE bar the measured λ̂ must clear — without it even a successful build can't be GO/NO-GO'd). **So there are now FOUR independent build routes to the launch gate** (land #245 tree · lawine #246 K-1 linear-systems · stark #247 T-1 topology · + kanna #248/ubel #250 stacking draft levers) — maximizing P(at least one delivers a measured ≥500), with fern #249 defining the λ̂ bar each must clear. **Live hard gates UNCHANGED & all external: (1) a ≥500 build [land #245 ★critical-path, now + 3 independent route-mates] + (2) measured λ̂_built ≥ the fern-#249 bar (band [0.978, 0.986]; was 0.9780 P95). The #124 publish-first public milestone (TPS≥500/PPL≤2.42/128/128) is the operative gate; the validity-lane packet is POST-HOC DEFENCE.** **Roster, zero-idle (8/8):** land #245 (★critical-path tree build) · lawine #246 (K-1 systems) · stark #247 (T-1 topology) · kanna #248 (Q-1 draft-quant) · ubel #250 (N-1 draft-source) · fern #249 (build-λ̂ bar) · denken #241 · wirbel #244.

**🆕 Cycle-52f — the build-λ̂ bar SETTLES (two numbers, not a band), the build-shortfall floor is PRE-REGISTERED, and compliant-private topology is certified DEAD (3 bank-the-analysis merges #249/#241/#244 by the parallel advisor, BASELINE 481.53 unchanged); freed seats deepen build-de-risking + open a fresh linear-path lever.** All CPU-only/local, 0 TPS, greedy/PPL untouched, authorize nothing. **(λ̂ bar → two numbers)** fern #249 (`on4u78ul`, self-test True) resolves the [0.978, 0.986] band: the four constraints split 2-2 across TWO RISK AXES — VALIDITY (P95 0.9780 + #243 worst-case-vertex 0.978413, agree to 4.0e-4) vs DRAW-RISK (#239 integrated-5% 0.9808 div / 0.9861 unif). **OPERATIVE gate λ̂ ≥ 0.9780** (P95 validity, MUST clear, residual P_invalid=0.05); **DEFENDED target λ̂ ≥ 0.9808** (div-informed 5%-draw-risk, SHOULD clear — advisory under #124 publish-first, draw-below-500 risk 0.0589 ACCEPTED post-hoc). Advisor + Morgan both AGREED: keep the operative gate at P95 0.9780, do NOT raise to 0.978413 (+4e-4 below derivation resolution). **(build-shortfall floor)** denken #241 (`hqewf1d6`, 6/6) — the composition is LINEAR-through-origin in E[T] so `delta_max_tps500 = 1−500/TPS_0` is SLOPE-INVARIANT → **`E_T_meas_floor`=4.3305 (4.02% shortfall)**; the acceptance gate is deep-tail-protected (#230) so TPS500 binds ⇒ **land #245's build is GO iff measured E[T]_both ≥ 4.3305 AND measured min-λ q[2..9] ≥ 0.9780.** Flagged the load-bearing step-anchor inconsistency (banked step 1.2182 → 463.97 TPS vs land's GO-read ~1.085 step → 520.95). **(compliant-private topology DEAD)** wirbel #244 (`sgjvbzu3`, 6/6) — VERDICT `TOPOLOGY-DEAD-REOPENABLE-ONLY-BY-COVERAGE`: NO reachable verify-tree topology lifts the operative 520.95 ceiling to stark #226's 528.48 private bar; the deployed depth-9 max-branch-3 tree is ALREADY width+depth-optimal at the TPS objective (`topology_lift_max=+0.000`), the 528.48 lane reopens ONLY via coverage λ→1 (+1.40pp recovery). **This BOUNDS stark #247's T-1 OPT-Tree upside: the best STATIC tree is already deployed, so online per-step DP can only win via per-step confidence VARIANCE a fixed tree leaves on the table.** (Arithmetic correction: the real ceiling gap is +7.53 TPS, not the PR's 15.71.) **(reseats — all 3 freed students re-seated by the parallel advisor, zero-idle held)** wirbel → #251 (**Plan-B activation-recycle screen** — recycle drafter L1-8 activations into the verifier on the LINEAR path; FLOP-vs-HBM reconcile, +4–8% projected, GO/NO-GO; a fresh orthogonal linear-path lever) · denken → #252 (**composition step re-anchor** — resolve its own #241 flag: is the served 1.2182 step the SAME object as land's built ~1.085 tree step? re-derive (K_cal, step) against land's reset trace, confirm/correct the 520.95 anchor) · fern → #253 (**two-path-500 portfolio** — fold the settled two-number bar into the launch card + frame the tree-build vs linear-speed-lever portfolio). **Live hard gates UNCHANGED & all external: (1) a ≥500 build [land #245 ★critical-path + route-mates lawine #246 K-1 / stark #247 T-1 / kanna #248 Q-1 / ubel #250 N-1 / wirbel #251 activation-recycle] + (2) measured λ̂_built ≥ operative 0.9780 (defended 0.9808) AND measured E[T]_both ≥ 4.3305. The #124 publish-first public milestone (TPS≥500/PPL≤2.42/128/128) is the operative gate; the validity-lane packet is POST-HOC DEFENCE.** **Roster, zero-idle (8/8):** land #245 (★critical-path tree build) · lawine #246 (K-1 systems) · stark #247 (T-1 topology) · kanna #248 (Q-1 draft-quant) · ubel #250 (N-1 draft-source) · wirbel #251 (activation-recycle) · denken #252 (composition step re-anchor) · fern #253 (two-path portfolio).

**🆕 Cycle-52g — land #245's LIVE-BUILD cycle-1 REFRAMES the critical path: the scratch-reconstruction verify forward is only ~60% faithful (a real bug, NOT #192), so the branch-interior of the E[T]=4.512 projection is now UNVERIFIED — advisor GOes on the real `execute_model` integration with a fidelity-first gate (4 bank-the-analysis merges this turn #253/#250/#254/#255, BASELINE 481.53 unchanged).** All CPU-only/local, 0 TPS, greedy/PPL untouched, authorize nothing. **(THE REFRAME — load-bearing, land #245 cycle-1 BLOCKED)** land's M=8 clean-room control (batch-matched, qq_bias OFF) reproduces the deployed verify argmax only **0.599** (0.633 excl. depth-0, 7,644 rows) — a faithful reconstruction should be ~1.0; mismatch signature (43% confident gap≥1.0, 26% rank-1/2, 5.5% razor-ties) is the OPPOSITE of #192 int4-Marlin batch-variance ⇒ a **genuine scratch-reconstruction forward bug, worst at root, NOT #192.** Consequences: (i) the real-KV SPINE ladder (mean_accepted_len 2.57–3.69, top1 0.69–0.78, λ q[2..9] all clear 0.9780, branch-hit ρ₂≈0.4165) **SURVIVES** — real-KV-derived, so the **gate-2 λ̂ evidence is intact**; (ii) the deep branch-INTERIOR contribution to E[T]=4.512 is now **UNVERIFIED** (scratch-derived) and may be optimistic; (iii) eager penalty brutal (381→57 TPS) ⇒ CUDA-graph capture existential; size-29 crash diagnosed (`max_cudagraph_capture_size=16`; M=16 verify at the boundary, capturable). **(ADVISOR DIRECTION — GO, sent back to wip)** pivot to the real `execute_model` integration (bypasses the scratch reconstruction via real embed+PLE + real scheduler split-KV metadata); **FIDELITY-FIRST gate** — prove the real-path verify reproduces deployed argmax ~1.0 BEFORE any capture; build the tree as sequential width-1 capturable forwards; capture the M=16 verify; gate unchanged (`treeverify_served_gain_MEASURED_realized>0` at PPL-valid wall_tps≥500, greedy-identity preserved on the committed path). **Empirically confirms human #124's "gate = built artifact, the analysis probe cannot substitute."** **(the 4 merges)** #253 fern two-path portfolio (`combined_reach500_prob=0.629`, Path-A 0.280 + Path-B 0.485, second-shot margin 0.349; by parallel advisor) · #250 ubel n-gram draft-SOURCE **RED** (`e_t_ngram_hybrid=4.214` < served, never beats trained draft anywhere; by parallel advisor) · #254 kanna int4-draft **RED** (`net_tps_gain_pct_int4_draft=−27.44`; W4A16 Marlin 165.79µs > bf16 101.2µs at M=1 — draft-PRECISION axis DEAD; by me) · #255 wirbel greedy-verify-epilogue **NULL** (`projected_tps_gain_pct=0.0`, no-go; verify-epilogue lever dead; by me). **(Plan-B picture SHARPENS)** three draft/verify levers now closed — draft-PRECISION (int4 #254 RED), draft-SOURCE (n-gram #250 RED), verify-EPILOGUE (#255 NULL) ⇒ the surviving draft-side headroom is the **LAUNCH axis** (Rank-2 argmax+embed fusion, wirbel #261), plus the orthogonal **serve-layer** lever (kanna #260, vidraft +0.9% prometheus instrument-off). **(reseats — all freed seats re-seated, zero-idle held)** fern → **#259 (E[T] branch-interior sensitivity** — does the E[T] lower bound, discounting the scratch-suspect branch-interior, still clear the 4.3305 floor? prices whether Path-A survives land's finding) · kanna → **#260 (serve-overhead/prometheus instrument-off screen** — the vidraft +0.9% serve-layer lever, orthogonal to all model-compute) · wirbel → **#261 (Rank-2 draft argmax+embed fusion screen** — the last un-screened draft-side lever, prices whether lawine #246's CUDAGraph subsumes it) · ubel → #258 (private E[T]-gap decomp; by parallel advisor). **(board)** quiet — no new @senai mentions; top frantic-penguin 489.63 (<500); the vidraft-darwin cross-fleet collaboration offer was escalated last turn to @chiku-inu + the human team (Issue #124), nothing committed. **(deferred)** a #124 human FYI on the branch-interior caveat is HELD until fern #259 returns a concrete Path-A-robust/fragile number (avoid a vague premature escalation). **Live hard gates UNCHANGED & all external: (1) a ≥500 build [land #245 ★critical-path, now on the real `execute_model` integration + route-mate lawine #246 K-1] + (2) measured λ̂_built ≥ operative 0.9780 AND measured E[T]_both ≥ 4.3305. The #124 publish-first public milestone (TPS≥500/PPL≤2.42/128/128) is the operative gate; the validity-lane packet is POST-HOC DEFENCE.** **Roster, zero-idle (8/8):** land #245 (★critical-path, real-integration build) · lawine #246 (K-1 systems) · stark #256 (adaptive-K early-exit) · denken #257 (built-step roofline) · ubel #258 (private E[T]-gap) · fern #259 (E[T] branch-interior sensitivity) · kanna #260 (serve-overhead screen) · wirbel #261 (Rank-2 draft fusion screen).

## Official Standing

| rank | agent | TPS | status |
|---:|---|---:|---|
| 1 | frantic-penguin | 489.63 | valid |
| 2 | need-for-speed | 488.07 | valid |
| 4 | byteshark | 484.62 | valid |
| **8** | **senpai** | **481.53** | **valid** |

- **Our official best:** 481.53 TPS (`fa2sw_precache_kenyan`, PR #52, lawine), private-verified VALID 2026-06-13 23:04Z (PPL 2.3777, 128/128, gap Δ4.3% ≤ 5%)
- **Target:** >500 TPS (~+3.8% needed); past-530 = tree-only (descent fix → 522, both bugs → 538). **✅ Private-stability RECONCILED (stark #156 MERGED, supersedes #151's fear):** the tree's TRUE native private drop is **1.80% descent / 1.86% both-bugs** (pinned, sglang vllm-chat anchored to GT-4.3%) → descent-only IS private-safe at the real drop (tree 510.6 TPS); #151's feared 5.89% and the 19.6%/11.3% values were harness/proxy artifacts (decode_outputs.py under-reads; hard-proxy ~2.5× real). The descent walk is ~0.42× as private-fragile as linear. Residual caveat: at the **9% band-ceiling** descent-only fails (499.9) — only **both-bugs** clears there, so the spine still buys margin even though it is no longer *mandatory* for private-safety at the pinned drop.
- **No linear-stack reclaim remains:** the gap to #1 is best-of-N scorer variance (stark #137 CLOSED — frantic-penguin's own spread brackets us); the only path past parity is the **tree** (land #71 descent → 522, both bugs → 538).

## Human Directives (Standing)

- **lewtun (Issue #31):** downstream evals must use `generation_config.json` params — NOT greedy. Does NOT apply to TPS benchmark or greedy-identity gate.
- **theykk:** "target is 500tps."
- **No HF job launches without human approval.** Open GitHub issue titled `Approval request: HF job for <name>`, launch only after explicit human go. (Validity is no longer the blocker — launch *authorization* is.)
- **Advisor consumes no GPU.** All GPU usage via assigned students only.

## Greedy-Identity Decision (Issue #124) — RESOLVED by advisor ruling

Per human directive (theykk 10:30Z: "come to your own decision, then flag it to the broader challenge collective and monitor if other agents disagree"), the operative interpretation: **the greedy-identity contract binds but is SATISFIED** by greedy-exact int4 speculative decoding + the PPL ≤ 2.42 gate. The deployed stack's 56% AR-divergence (kanna #114/#122) is inherent int4-Marlin batch-variance (split-K geometry = f(M), no batch-invariant Marlin in the pinned wheel), **not** a contract violation; the official auto-scorer (`hf_bucket_single_job.py`) runs **no token-identity check** (TPS + PPL + 128/128 only). The tree rides on the **same int4-spec basis as the 481.53 frontier**. Socialized on the board (decision `20260614-104624-532`); **no dissent has appeared** — it STANDS as operative. The official shot is still approval-gated, but on *launch authorization*, not validity.

## Active 8-Seat Roster (Cycle 52b) — ZERO IDLE

| student | PR | lever | status |
|---|---|---|---|
| **land** | **#71** | **Tree descent-walk BUILD — ★ CRITICAL-PATH / the 500-lever.** Replace the linear `_dixie_fused_accept_prep_kernel` chain-reject with a **descending** accept walk on the flat tree (BUG-2). Build descent FIRST (clears 500 alone per fern #134); depth-1 spine is the 522→538 margin. Gate: local M=16 ≈ 5.0 tok/step with branch-hit ≈ ρ₂=0.4165, both-halves asserts. | **WIP** (build team holds the runnable star-attn build) |
| **stark** | **#176** | **Adverse domain-skew private stress** (NEW): widen to 5–6 calibrated axes + find the worst REALISTIC domain-mixture (diversity-capped simplex, fixed aggregate-4.3%) → does descent-only's τ-low corner still clear 500 at the adverse vertex? Hardens the one residual #164 named (its 3-proxy CI is a construction-variance band, NOT a sampling CI over the real private set). (#164 MERGED 🟢: native drop CI mid **2.04%** [1.87,2.21]; descent-only private-safe — central 509.3, worst τ-low 504.6, all clear 500; `both_bugs_required_private=False`; `reproduces_156=True`. The directly-measured replacement for #156's interpolated point, +0.24pp from cross-domain shape independence.) | **WIP** (NEW) |
| **kanna** | **#159** | **Hardware-variance envelope σ_hw** (NEW): the missing 4th quadrature term in fern #155 — within-allocation A10G clock/thermal/cold-start (N≥10 repeats) + cross-draw bound (frantic-penguin ±1.9%) → answers stark #151's "is 505 *safely* >500, or do we need an official re-draw budget?". (#138 CLOSED ⚪: K*=7 unchanged by block64 — the sparse-argmax tile is OFF the K7 critical path [8.485≈8.486 ms]; block64 is K-neutral + greedy-identical, a free manifest flag at 0 TPS.) | **WIP** (NEW) |
| **denken** | **#172** | **Descent-E[T] model audit** (parallel-advisor): independently re-derive the 5.0564 descent-only projection + a conservative E[T] LOWER bound (complements wirbel #170's over-accept UPPER-bound region — together they box the trustworthy E[T]). (#166 MERGED 🟢: tree-path PPL worst-case **2.4134 < 2.42** — M-INVARIANT because PPL is scored on the *prefill* path the M=32 verify geometry never touches; only an unphysical 2ε/token model breaches; residual = if land changes prefill-chunk geometry.) | **WIP** (NEW) |
| **fern** | **#174** | **Launch verdict at the conservative launch-realized step** (parallel-advisor): render the GO/NO-GO at lawine #168's conservative 1.2182 served step. (#167 MERGED 🟢: the pinned-operating-point launch PACKET — GO/NO-GO assembled at stark #156's 1.80%/1.86% drop with all merged measured inputs; descent-only 519.96–522.29 / both-bugs 535.44–537.84, **both clear 500**; recommends the first-shot topology + arms slots for stark #164 / kanna #159 / land #71. The penultimate launch artifact.) | **WIP** (NEW) |
| **wirbel** | **#175** | **E[T] second moment → finite-sample TPS CI** (NEW): the DP's 2nd moment — accepted-length variance → the finite-benchmark single-shot TPS sampling CI (the uncertainty term NO leg computes; composes in quadrature with kanna #159's σ_hw for the total single-shot CI) + the predicted accepted-length histogram as land #71's distributional readout gate. (#170 MERGED 🟢: the joint (E[T],v) trustworthy region — over-accept locus `E[T]=E[T]*/(1−v)`, `max_et_inflation_at_v0=0` ⇒ any E[T]>ceiling REQUIRES v>0; exact count-identity with denken #158 [19/236]; the magnitude complement to #158's binary detector, the 1st-moment trustworthiness gate this 2nd-moment leg extends.) | **WIP** (NEW) |
| **ubel** | **#169** | **PRECACHE_BENCH tree-footprint calibration-invariance** (NEW): does ubel #148's K_cal=125.268 / +6.019% bus-ratio multiplier hold at the M=32 tree's ~20.47 GB HBM footprint vs the linear stack it was calibrated on? — the LAST unverified link in the denominator→official compose feeding fern's packet. (#163 MERGED 🟢: descent-path residual host ops = **0** beyond #154/#157; capturability probe 4/4; realizable net step **1.2086** < 1.2182 → bar 4.824, both topologies clear 500.) | **WIP** (NEW) |
| **lawine** | **#173** | **Descent-walk step cost** (parallel-advisor): does the salvage-descend accept-prep stay step-neutral — does land #71's assembled descent build still hold the 1.2182 step? (#168 MERGED 🟢: the 4 step anchors collapse to ONE launch-REALIZED step **1.2182** for both topologies, ±2.4 TPS roofline↔overlap band; served eager star-attn pays the +0.447% overlap idle; #154's 1.2047 quarantined as an unshipped conditional, #161's spine = additive-0.) | **WIP** (NEW) |

## Current Research Themes

### Theme 1: Linear-Stack Parity — CLOSED as a lever (kanna #138 confirmed K*=7)
stark #137 (CLOSED ⚪) settled it: block64 is a **+0.085% NULL**, and the "8-TPS gap to #1" is **best-of-N scorer variance (~1.9%)** — frantic-penguin's own 3-draw spread (489.63 / 483.80 / 480.41) brackets our 481.53. We are at frontier *parity* on the linear stack; **no linear-stack reclaim remains**. block64 is adopted as a free, greedy/PPL-safe config line in land's tree manifest (no dedicated launch). kanna #138 (CLOSED ⚪) confirmed **K*=7 is unchanged at block64** (block64 is K-neutral + greedy-identical [128/128], 0 standalone TPS — a free manifest flag). All linear micro-opt energy now redirects to the tree (Theme 2) and bulletproofing the one shot (Theme 3).

### Theme 2: Tree Descent-Walk Build (THE 500-PATH — land #71 + support seats)
Tree is the sole 500+ path (tree-free caps 491.8, denken #123 RED). The decisive finding: **descent (BUG-2) alone clears 500** (fern #134 + wirbel #135, two independent methods). land #71 builds the descending accept walk; the support instruments are now mostly **banked**: **lawine #147** (sync-audit harness — MERGED 🟢, verifies the sync-free build + measures the real step; lawine → #153), **wirbel #146** (measured-gate confidence envelope — MERGED 🟢, sampling CIs + required_n=5; wirbel → #152). Now mostly banked: **fern #149** (joint spread×width clears-500 frontier — MERGED 🟢, only ~3% of the recovery square clears 500; fern → #155 consolidator) and the **denominator** support seat **ubel #154** (scatter+LP avoidance — MERGED 🟢, +4.3–5.6 TPS, lowers the clear-500 bar 4.862→4.808; ubel → #157 `relocate_salvaged_kv` host-loop audit, the 2nd step tax). **BUG-1 depth-1 is now CLOSED-and-handed (denken #133 MERGED):** fp32 GPU-confirmed not-the-fix (lane closed, tree-488-pw-fp32-v0 quota saved); the 13.1pp deficit is ~96% a fixable wrong-rank `target_logits_indices` plumbing bug → handed to land as the rank-1 spine fix, and the **same index-map class may fix BOTH the depth-1 spine AND the descent**. BUG-3 (CUDA-graph illegal-memory-access) lives in the external star-attn build → re-homed to land #71 / build team (ubel #139 closed as out-of-scope for an isolation-scoped seat). **🆕 Build-sequence update (stark #156 MERGED, supersedes #151):** #156 PINNED the tree's true native private drop at **1.80% descent / 1.86% both-bugs** → descent-only IS private-safe at the real drop (510.6 TPS); #151's feared 5.89% (and the 19.6%/11.3%) were harness/proxy artifacts. So **descent-FIRST clears 500 both public AND private** — the spine is back to being the 522→538 margin (it still buys headroom at the 9% band-ceiling, where only both-bugs clears, but is no longer *mandatory* for private-safety). **🆕 BUG-1 spine is now BUILD-SPEC'd (wirbel #160 MERGED):** the q₁ 0.679→0.7287 gap is ONE input-contract bug — f→0, index the spine root's own logits row (both-bugs E[T]=5.2070, greedy-safe). **🆕 Descent build-blocker found (ubel #157 MERGED):** the `relocate_salvaged_kv` host-loop is a 1571× LIVE-LANDMINE (77 vs 516 TPS) — land's descent salvage relocation MUST be vectorized-GPU/paged. Open coordination (wirbel #165): does ONE corrected `target_logits_indices` fix BOTH bugs, or two independent corrections?

### Theme 3: Launch-Readiness / One-Shot Evidence-Line Cluster (#142/#145/#146/#147/#148/#149/#150/#151/#154/#155/#156/#157/#158/#160/#161/#162/#163/#164/#165/#166/#167/#168/#170 banked — evidence-line STRUCTURALLY COMPLETE + the launch PACKET now ASSEMBLED (fern #167) at the pinned drop, both topologies clear 500, every slot pinned/measured except land's tuple; live: kanna #159 σ_hw · ubel #169 PRECACHE-footprint-invariance · denken #172 E[T]-lower-bound · lawine #173 descent-step-neutral · fern #174 launch-verdict · wirbel #175 E[T]-2nd-moment-TPS-CI · stark #176 adverse-skew-private-stress)

**🆕 Cycle-51 convergence (the launch shape sharpens):** fern #162 INVERTED the frontier → land #71's build gate **(λ_min,μ_min)=(0.8809,0.7353)** and showed **BUG-1 is CONDITIONAL** (descent-alone clears at GT drop; spine mandatory only >~6% private drop). stark #156 PINNED the realized drop at **1.80%** (≪6%), and lawine #161 proved the spine fix is **step-neutral** (537.8 measured). Net: at the pinned operating point the **first shot can be descent-ONLY (BUG-2)**, with wirbel #160's spine as a free-on-step, deferrable insurance for the 9%-band-ceiling margin. fern #167 instantiates this into the final GO/NO-GO packet; lawine #168 reconciles the one launch-realized step it quotes.
The memory-BW micro-lever surface is now **fully exhausted** — weights floored at int4 (kanna #132), verify-GEMM weight-BW maxed (#117/#130/#108), scale bytes palette-harvested (#110) with coarser-grouping closed (ubel #140 RED), the bf16 KV-cache lever closed (wirbel #141 RED), and the verify-candidate column-prune closed (denken #144 RED). With no independent BW lever and the drafter-acceptance lane owned by the parallel team, the freed seats built **bulletproofing for the one human-approved 500 shot** — for a one-shot irreversible spend, heavy verification is rational. The legs of the eventual `Approval request: HF job` evidence-line (three now MERGED-and-banked):
- **#146 (wirbel, MERGED 🟢)** — *sampling* leg: CIs + required-N (=5) on land's measured E[T]; the 522 GREEN survives until the sampling ½-width alone exceeds **4.19%**. wirbel → #160.
- **#148 (ubel, MERGED 🟢)** — *calibration* leg: K_cal=125.268 **transfers** to the tree (band 0.787% one-sided↓); the residual +6.019% is a hardware bus ratio held invariant by **`PRECACHE_BENCH=1`** (named launch dependency — the tree submission MUST retain it). ubel → #154.
- **#147 (lawine, MERGED 🟢)** — *measurement* leg: sync-audit harness verifies land honored the sync-free rule (PASS→bar ~4.88 / FAIL→names syncs) + produces the real measured step. lawine → #161.
- **#149 (fern, MERGED 🟢)** — *decision-geometry* leg: the 2-D (spread×width) joint clears-500 frontier — only ~**3% of the recovery square clears 500** (the GREEN region is a tight top-right corner), so the verdict is **corner-sensitive**: land's measured (λ,μ) landing point, not just headline E[T], is what the gate reads. fern → #162.
- **#151 (stark, MERGED 🟢)** — *private-stability* leg: the FIRST quantification of BASELINE.md's #1 launch risk against the tree. Descent-only projects **505.46** @ GT-4.3% but tolerance is only **5.89%** (fails 6–9% of the 4–9% band); both-bugs tolerance **9.88%** covers it → the private-safe shot is the bug-1-fixed topology, NOT descent-alone. stark → #156 (pin the tree's true private drop under the organizer-matching harness).
- **#150 (denken, MERGED 🟢)** — *validity* leg: local preflight proves the assembled tree stack boots, passes PPL≤2.42, completes 128/128 (4/4 gates, READY on the known-good stack); ARMED/PENDING land #71. denken → #158 (per-token greedy-EXACTNESS — `accepted==target_argmax`, the 2nd validity leg, the BUG-2-class catcher the scorer's no-token-check can't see).
- **#154 (ubel, MERGED 🟢)** — *denominator* leg: decode-path scatter+LP avoidance → +4.3–5.6 TPS, **lowers the clear-500 bar 4.862→4.808–4.820** (widens #149's 3% green corner, cushions #151's private margin). ubel → #157 (`relocate_salvaged_kv` host-loop — the 2nd step tax).

Plus fern #142 (scalar point gate) + #145 (facet decomp); the **denominator leg (ubel #154 MERGED 🟢 → #157)** — scatter+LP avoidance *lowers the clear-500 bar* 4.862→4.808, now extended by the `relocate_salvaged_kv` host-loop audit; two NEW legs — a **hardware-variance leg (kanna #159)** σ_hw (the 4th quadrature term fern #155 omits; answers stark #151's "is 505 *safely* >500") and a 2nd **validity leg (denken #158)** per-token greedy-exactness; and the **capstone (fern #155)** — the projection-CI **consolidator** that collapses the legs into ONE GO/NO-GO with its CI + P(≥500) + named binding leg for the approval issue (and corrects the #142 τ-floor optimism ubel #148 flagged). **Honest note:** the **Plateau-Protocol researcher-agent sweep has RETURNED** (`research/RESEARCH_IDEAS_2026-06-14_12:20.md`): it *confirmed* the tree-descent premise (TTFT/prefill ruled out — <1% at output_len 512) and surfaced the genuine independent path as **step-denominator reductions** — RANK-1 scatter-free verify argmax (`kept_ids[argmax(partial[M,12k])]`, validated token-identical, equivalence_rate=1.0) is now ubel #154 (MERGED 🟢, +4.3–5.6 TPS, bar→4.808) → #157 (`relocate_salvaged_kv` host-loop). The concentration on one number (land #71's) was justified by the one-shot structure; the sweep pressure-tested it and produced a real orthogonal lever rather than more instrumentation.

### Theme 4: Closed Lanes (DO NOT RE-ASSIGN)
- **GEMM-bandwidth** — triple-confirmed closed (#117 roofline + #130 re-tile + #108 SplitK: Marlin 79.4% = 95% of the 1-wave HBM wall).
- **Sub-4-bit weight body** — closed (kanna #132: no servable sub-4-bit GEMM kernel on sm_86; Marlin floors weights at 4 bits).
- **QuantSpec drafter-KV** — moot (stark #121: the MTP drafter is Q-only, allocates zero KV).
- **fp8 KV-cache (a10g-small / sm_86)** — closed (wirbel #141 RED: e4m3 `fp8e4nv` hardware-impossible on sm_86; e5m2 software-blocked by vLLM #39137 compressed-tensors guard, + FA_SLIDING `NotImplementedError` PR #14221). The KV-read BW stream is un-attackable via fp8 on this hardware. DO NOT re-propose.
- **Marlin coarser-group scale-BW** — closed (ubel #140 RED: g=256 unservable [pinned Marlin max group 128]; the only coarser servable group g=-1 fails PPL at +0.122 = 3× the 0.039 cap headroom). The scale-byte slice is already palette-harvested losslessly (#110). Conditional re-open only if a wheel bump dispatches g=256 (delta ≈ +0.06 might fit).
- **N-gram / prompt-lookup / lookahead drafter (RANK-3 re-tread)** — closed (denken #81 Step-0 gate CLEARED at 0.4066 extra-accept but realistic +1–3% & not buildable in stock vLLM [SpeculativeConfig.method is single-choice mtp XOR ngram → AUGMENT needs a proposer-loop fork]; PR #89 DROP: PLD hits POSITIVELY correlate with MTP acceptance (corr +0.354) → redundant, +2.38% ceiling, "do NOT queue behind land #71"). The 2026-06-14 researcher sweep re-proposed it as RANK 3 — **already closed**, do not re-assign (cross-prompt repetition near-zero; within-stream self-repeat already shown redundant with MTP).

## Tree Build Status (land #71) — the 500-lever

- **Topology pinned:** M=32, depth-9, max-branch-3 build array (per wirbel #83 / denken #101).
- **Measured oracle (openevolve `20260614-100550-487`):** E[T]=2.621, depth-1 accept 0.679 (target 0.7287), per-position cumulative ladder [0.674, 0.350, 0.203, 0.131, 0.089, 0.060, 0.037], 391 salvages / 37 full / 1024 steps, eager star-attn PIECEWISE.
- **Recovery (fern #134 / wirbel #135):** descent-only fix → E[T] 5.04–5.06 → ~522 ✅; both bugs → 5.207 → ~538.
- **Step denominator (lawine #136):** measured depth-9 step 1.2182 (+0.45% vs roofline); operative clear-500 bar 4.841→4.862, well under the 5.207 ceiling.
- **Build sequence:** descending accept walk FIRST (clears 500 on public AND private per stark #156's pinned 1.80% drop → 510.6), then the depth-1 spine (wirbel #160-spec'd: f→0 own-row indexing) for the 522→538 margin + 9%-band-ceiling headroom. **Descent build-blocker (ubel #157):** salvage KV relocation MUST be vectorized-GPU/paged, never the 1571× host-loop. land's local gate: M=16 ≈ 5.0 tok/step, branch-hit ≈ ρ₂=0.4165, greedy-exact (denken #158 `--audit-kernel-symbol`).
- **Launch:** approval-gated HF job only, behind a human-approved `Approval request: HF job` issue.

## Recent Closed/Merged (cycles 47–52)

| PR | verdict | significance |
|---|---|---|
| #170 | MERGED GREEN (wirbel, descent over-accept signature) | The joint **(E[T], v) trustworthy region** land #71's measured tuple must fall in. Over-accept commits ε extra nodes/step → `E[T](ε)=E[T]*+ε`, locus `E[T]=E[T]*/(1−v)`; **degenerate at v=0** (`max_et_inflation_at_v0=0`) ⇒ any E[T]>ceiling REQUIRES v>0 (inflation is over-accept, not headroom). Exact count-identity with denken #158: `v_at_denken158_point=19/236=0.080508=1−exactness`, `matches_detector=True`. The **magnitude complement** to #158's binary detector — together they bound BUG-2 from binary + magnitude sides. Hands land the `land_tuple_in_trustworthy_region` predicate; `et_inflation_at_unit_overaccept=1.0`. W&B `ne7p642c`. wirbel → #175. |
| #168 | MERGED GREEN (lawine, step-anchor reconciliation) | The 4 step anchors (roofline 1.2127 / overlap 1.2182 / scatter-LP 1.2047 / both-bugs-neutral 1.2182) collapse to ONE launch-REALIZED step = **1.2182** for both topologies, ±2.4 TPS roofline↔overlap band. Served eager star-attn under `PRECACHE_BENCH=1` pays the +0.447% overlap idle → 1.2182 is reality; #154's 1.2047 is a quarantined unshipped conditional (`cond154_applies_in_served_path=0`); #161's spine = additive-0. descent-only 519.96 / both-bugs 535.44 @overlap; bar 4.862. W&B `oti5l4sb`. lawine → #173. |
| #167 | MERGED GREEN (fern, pinned-operating-point launch packet) | Instantiates the GO/NO-GO at stark #156's pinned drop (1.80%/1.86%) + all merged measured inputs → the assembled `Approval request: HF job` projection/validity block: descent-only 519.96–522.29 / both-bugs 535.44–537.84, **both clear 500**; recommends the first-shot topology + arms slots for the 3 open inputs (stark #164, kanna #159, land #71). The penultimate launch artifact (the go/no-go is now a one-glance read pending land's measured tuple). fern → #174. |
| #166 | MERGED GREEN (denken, tree-path PPL-margin bound) | Worst-case M=32 batched-verify PPL = **2.4134 < 2.42** (conservative-transplant margin 0.0066; realized margin 0.0433). Decisive insight: PPL is scored on the **prefill** path, which the M=32 verify geometry never touches → the gate is **M-INVARIANT**; only an unphysical 2ε/token adversarial model breaches (break-even ε 0.275–0.351 vs measured 0.25, falsified by 0 argmax flips at M=32). Residual: if land #71 changes prefill-chunk geometry (denken's own follow-up). W&B `z4l8ljd7`. denken → #172. |
| #165 | MERGED GREEN (wirbel, shared index-map coherence) | ONE corrected `target_logits_indices` fixes BOTH BUG-1 spine AND BUG-2 descent — both dereference the SAME `target_argmax_ptr` at the SAME index base in `_dixie_fused_accept_prep_kernel` (sitecustomize.py:921); the kernel holds no 2nd map. `composed_fix_E_T=5.2070` computed ONCE = both-bugs ceiling; the **+0.0252 super-additivity** (vs 5.1818 false-independent) proves coupling. land builds ONE fix not two; binding risk = BUG-2 (~19× BUG-1). Converges with land #71's live Component 3 (`comp3_index_map_checks_pass=17`). W&B `laxllfjl`. wirbel → #170. |
| #163 | MERGED GREEN (ubel, descent-path host-residency sweep) | All 12 timed-window ops enumerated (6 host-resident), every one accounted for → **residual host ops beyond #154/#157 = 0** (no hidden host-loop landmine). Distinctive leg: empirical capturability probe — 4/4 subprocess-isolated cases match the taxonomy with the exact predicted CUDA capture errors. Net realizable step (descent + vectorized relocate + #154) = **1.2086 < 1.2182** → bar 4.824, descent cushion 0.178→0.216 E[T]; descent 522 / both-bugs 540 clear 500. W&B `dmcskhwi`. ubel → #169. |
| #162 | MERGED GREEN (fern, tightened private-safe frontier) | INVERTED #149's frontier → land #71's build gate **(λ_min,μ_min)=(0.8809,0.7353)** @ P≥0.5; λ_min(μ) unreachable for μ≲0.73. Lower bar widens public green 3.00%→3.99% but private haircut shrinks safe-region to 1.72%. **Decisive: BUG-1 is CONDITIONAL** — BUG-2-descent-alone clears at GT drop (511.1, P=0.845); spine mandatory only >~6% → **deferrable at stark #156's pinned 1.80%**. Reproduces stark's 9.88% + #149's 0.0300 exact. W&B `0il5xhji`. fern → #167. |
| #161 | MERGED GREEN (lawine, both-bugs step cost) | The depth-1 spine fix is **step-NEUTRAL** (paired device-busy marginal −0.031µs ⇒ nil; all 5 rounds negative). BUG-1 = upstream `target_logits_indices` plumbing, byte-identical accept-prep kernel → changes VALUES (E[T] 5.0564→5.2070) not op-count. **537.8 both-bugs official hardened assumed→measured** (537.84 roofline / 535.43 overlap, drop 0.0 TPS); last step-denominator unknown ≈ 0. Honest RED→GREEN gate fix (practical vs statistical significance). W&B `2heov0f4`. lawine → #168. |
| #158 | MERGED GREEN (denken, greedy-exactness harness) | 2nd validity leg: per-token `committed==in-step-argmax` gate importing the EXACT `_get_fused_accept_prep_kernel` serve.py:429 uses. Linear stack rate 1.0; BUG-2 kernel caught at 0.9194 with every violation localized (req=7 pos=0 committed=3000 ref_argmax=3777). `--audit-kernel-symbol` arms it as land #71's pre-merge contract gate (GREEDY_EXACT exit 0 / VIOLATION exit 1). Completion-sha256 spec-vs-AR divergence (0.6169) = documented int4-Marlin batch-variance (Issue #124), NOT a violation. W&B `opbbrnce`. denken → #166. |
| #160 | MERGED GREEN (wirbel, depth-1 spine build spec) | The buildable BUG-1 fix: the q₁ 0.679→0.7287 gap is ONE input-contract bug (spine-root pos=0 reads a rank-2-contaminated logits row via wrong-rank `target_logits_indices`), fix = f→0, index the spine root's own row. Contamination model q₁(f)=(1−f)·0.7287+f·0.4165; both-bugs E[T]=5.2070, descent-only 5.0564, idealization_gap=0, greedy-safe, 10/10. Annotated vs sitecustomize.py:942-951. Flagged the shared-index-map coordination Q. W&B `x8vffgbs`. wirbel → #165. |
| #157 | MERGED RED-LANDMINE (ubel, salvage-KV host-loop) | LIVE build-blocker: `relocate_salvaged_kv` host-loop 145.2ms/call→77 TPS descent vs gpu_vectorized 92.4µs→516 TPS (paged_slotmap ~517) — **1571×**, all bit-exact (equivalence_rate=1.0). The 2nd host-side step tax (stacks on #154). land #71's descent salvage relocation MUST be vectorized-GPU/paged, never host-loop. 11/11 self-test. W&B `rh8ysitz`. ubel → #163. |
| #156 | MERGED GREEN (stark, tree private-drop pin) | PINS the true tree private drop: **1.80% descent / 1.86% both-bugs** (sglang vllm-chat anchored GT-4.3%, calibrated linear 4.29%≈GT 4.29%) → tree 510.6 TPS, descent-only IS private-safe — REFUTES #151's 5.89% fear; 19.6%/11.3% were harness/proxy artifacts (~2.5× real). Descent ~0.42× as private-fragile as linear. At 9% band-ceiling only both-bugs clears (499.9 vs 495.9). Feeds fern #162's `--private-drop`. W&B `6wtn6790`/`t2tlqzxc`. stark → #164. |
| #155 | MERGED GREEN (fern, projection consolidator) | The CAPSTONE: ONE call unions the 6 projection-spine legs (#142/#146/#148/#149/#150/#136) → `{proj_tps, ci, p_clear_500, validity, binding_leg}`, binding leg named on every verdict. Fixed a ~0.6% τ-optimism in its own #142 gate (SplitK 0.9983 → tree-class 0.9924, bound to #148's `scale_floor`). Decisive: the partial-recovery veto — (E_T=4.90, λ=0.6, μ=1.0) reads naïve "GO" 503.9 but p_clear≈3e-6 via `decision_geometry`. Anchors land RED/GREEN/INDETERMINATE; bit-matches #146/#148/#149. W&B `gd1ok9zd`. The launch evidence-line is structurally complete. fern → #162. |
| #153 | MERGED GREEN (lawine, verify-step(M) curve) | step(M) is NOT flat — int4-Marlin 16-row-tile staircase, KNEE_AT_32 (M=32 = 2 Marlin tiles = the largest single-HBM-wave tree; M=48 +19.1%, M=128 +122%). Drafter-fill NOT binding (1.24× vs verify-GEMM's 3.05×). Reproduces #136's 1.2182 (1.2187). The cost curve under #152's value DP — "grow the tree for free" closed from the cost side. W&B `ma0qlpas`. lawine → #161. |
| #152 | MERGED GREEN (wirbel, topology re-opt) | Re-allocating the M=32 build array against the measured ladder does NOT clear 530 — the deployed shape is within +0.14% of the greedy-M32 optimum (+0.0071 E[T]). M=32 is the wall (last-kept node #31 ≈ first-dropped #32, marginal 0.0262); every M>32 rung LOSES official TPS. De-risks the pinned topology: the 530 gap is acceptance (bug-1 spine), not allocation. W&B `f2hxitrk`. wirbel → #160. |
| #154 | MERGED GREEN (ubel, step-denominator audit) | First non-drafter denominator lever: decode-path scatter+LP avoidance → **+4.3–5.6 TPS**, lowers the clear-500 bar **4.862→4.808–4.820** (8/8 self-test, `recoverable_step_pct` 0.857–1.108%, W&B `zioer4bm`). Widens #149's 3% green corner + cushions #151's private margin; multiplicative with descent. Cross-confirmed by #138 (argmax tile off the K7 critical path). ubel → #157 (`relocate_salvaged_kv` host-loop). |
| #150 | MERGED GREEN (denken, validity preflight) | Local READY/NOT-READY harness vs the scorer's 3 hard gates (boots ∧ PPL≤2.42 ∧ 128/128); 4/4 self-tests (known-good→READY, faults→Gate A/B/C), W&B `9ptw7jxb`. The validity leg — "will it SCORE" vs the TPS legs' "will it clear 500"; fern #155 gates on it. ARMED/PENDING land #71. denken → #158 (per-token greedy-exactness). |
| #138 | CLOSED NULL (kanna, K-sweep block64) | K*=7 unchanged by block64 — sparse-argmax tile OFF the K7 critical path (8.485≈8.486 ms), no freed budget. K8/K9 stay −13/−16 TPS; CENTROID_TOP_K=64 optimal. block64 greedy-identical (128/128) + K-neutral = free manifest flag, 0 TPS. Independently confirms #154's "budget is in scatter+LP, not argmax." kanna → #159 (σ_hw). |
| #151 | MERGED GREEN (stark, tree private-acceptance gap) | First private-stability verdict on the tree: descent-only projects **505.46** @ the GT-4.3% private drop — but lands inside ±1% of the 500 line, tolerance only **5.89%** (FAILS across 6–9% of the documented 4–9% band). **Both-bugs lifts tolerance to 9.88%** → the private-safe shot is the bug-1-fixed topology, NOT descent-alone. Verified W&B `box0yfh9`/`ytxfi6zk` (selftest pass, NaN-clean). Elevates the bug-1 spine to a private-stability requirement. stark → #156. |
| #149 | MERGED GREEN (fern, joint spread×width frontier) | Lifts #145's 1-D slices to the 2-D (λ spread × μ width) surface: **only ~3% of the recovery square clears 500** (GREEN = tight top-right corner) → partial-both-facet recovery is mostly RED; the 500 verdict is **corner-sensitive** (reads land's measured (λ,μ), not just E[T]). Surfaced the `relocate_salvaged_kv` host-bound loop. fern → #155 (consolidator + τ-floor fix). |
| #148 | MERGED GREEN (ubel, K_cal tree-transfer) | K_cal=125.268 **transfers** to the tree — band **0.787% one-sided↓** (far inside the 3%-drift tripwire). The +6.019% local→official multiplier decomposes to a hardware bus ratio held invariant by `PRECACHE_BENCH=1` (named launch dependency); calibration is NOT the binding 500-boundary constraint (522 GREEN holds until sampling > 4.19%). Flagged fern #142's τ-floor optimism (0.9983 SplitK vs 0.9924 tree-class). ubel → #154. |
| #147 | MERGED GREEN (lawine, sync-audit harness) | Extends #143's profiler with `--trace`/`--self-test`; classifies sync-free (0 syncs, bar 4.880, PASS) vs sync-bound (4.24 syncs/step, bar 4.956, FAIL, names 4 sites). The sync-free constraint is now verifiable the instant land's kernel lands. lawine → #153. |
| #146 | MERGED GREEN (wirbel, gate confidence envelope) | Wraps fern #142's point gate with bootstrap CIs: oracle 2.621→[253,286] robust-RED, ρ-opt 5.207→[507,564] robust-GREEN, boundary 4.862→[472,528] INDETERMINATE; **required_n=5** for a robust 500 verdict at the oracle point. wirbel → #152. |
| #145 | MERGED GREEN (fern, width-vs-spread decomp) | Splits realized tree E[T] into 3 TPS facets; the 161.6-TPS watched swing is ~entirely **deep-spine-spread** (nested +160.9 / Shapley +151.1). ≥90% spread recovery (width restored) to clear 500. Names the binding facet on a sub-GREEN ladder. Advisor confirmed the width/spread naming. fern → #149 (2-D joint frontier). |
| #144 | MERGED RED (denken, lm_head verify-candidate) | Verify lm_head GEMM is full-12288 (LIVE — 8192-sparse is the *drafter*). Candidate-prune NO-GO: gather-GEMM 2.1× slower than dense int4 Marlin at M=8 + argmax over 263 cands can't certify true argmax (breaks greedy/PPL). Verifier-side active-vocab closed. denken → #150 (preflight). |
| #137 | CLOSED NULL (stark, block64 reclaim) | block64 = +0.085% NULL. Valuable finding: the "8-TPS gap to #1" is best-of-N scorer variance (~1.9%) — frantic-penguin's own spread brackets us → we're at linear-stack *parity*, 500 is a tree story. block64 folded into land's manifest as a free config line; no launch. stark → reassign (Plateau sweep). |
| #142 | MERGED GREEN (fern, go/no-go gate) | One-call measured-M16 → official verdict; self-tests both anchors (271 RED / 538 GREEN within 0.1%) + bit-matches #134 matrix; wires PPL/tok-floor/branch-hit/greedy preconditions. Armed for land's readout. Decision input only — no launch authority. fern → #145. |
| #133 | MERGED GREEN (denken, depth-1 root-cause) | fp32 GPU-confirmed NOT the fix (NET ~0pp, exact-tie reshuffles) → fp32 lane CLOSED, quota saved; 13.1pp depth-1 deficit = ~96% fixable `target_logits_indices` plumbing (rank-2 contamination reproduces 0.598), handed to land as the rank-1 fix; shared-index-map may fix BOTH bugs. denken → #144. |
| #136 | MERGED AMBER (lawine, step-anchor) | Measured depth-9 step 1.2182 (+0.45% vs roofline); eager attn idle hidden behind GEMM; root-row clears 530; operative bar 4.841→4.862. Denominator FIRM. Methodology catch: isolation-only bench would have falsely read RED. |
| #135 | MERGED GREEN (wirbel, BUG-2 E[T]-DP) | Independent confirmation: descent-only fix → E[T] 5.041 (clears bar alone); BUG-2/BUG-1 = 19.3×. Salvages fire but don't descend (+0.077 E[T]). |
| #134 | MERGED GREEN (fern, live oracle readout) | Official-TPS matrix: as-built 270.7 ❌ / spine-only 283 ❌ / **descent-only 522 ✅** / both 538. Descent is the decisive 500-lever. |
| #132 | CLOSED (kanna, Q-Palette) | No servable sub-4-bit GEMM kernel on sm_86/vLLM-0.22; weight body floored at int4. Sub-4-bit weight lane closed. |
| #131 | MERGED AMBER (lawine, fp32 step-time) | Selective root-row-only fp32 → 563 TPS central, clears 530; full upcast compute-exposed at M=32. |
| #130 | MERGED RED (wirbel, gate_up re-tile) | 0% benefit — 1-wave HBM saturation wall. Final proof of GEMM-BW closure. |
| #129 | MERGED AMBER (fern, oracle harness) | Harness armed; operative clear-500 bar = E[T] 4.841 at the depth-9 step; fed the live oracle run. |
| #128 | MERGED RED (denken, fp32 cross-check) | fp32 is NOT the depth-1 fix (closes ≤1.4pp of 13.1pp). Real cause = drafter-spine/index-map under LIVE masking. |
