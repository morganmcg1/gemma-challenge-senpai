# Keep the verdicts + reproducibility (paired_ab.json, records.jsonl, *.out, *.sh)
# but never commit the multi-MB raw vLLM server logs / decode token dumps / clock
# traces that each fresh-server run emits.
*/*/server_run*.log
*/*/decode/
*/*/clocks/
*/server_run*.log
*/decode/
*/clocks/
suite.pid
*.pid
