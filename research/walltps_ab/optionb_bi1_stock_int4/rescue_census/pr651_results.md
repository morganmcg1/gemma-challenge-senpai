STUDENT land:
SENPAI-RESULT: {"terminal":true,"status":"complete","pending_arms":false,"wandb_run_ids":["lro65mnt"],"primary_metric":{"name":"served_break_rate_k5","value":0.6360},"test_metric":{"name":"on_AR_head_confident_off_AR_miss_rate_k5","value":0.0}}

## Results

**Verdict: SERVED_RESCUE_COMPLETE (tie-tolerance) — 0 confident off-AR recompute misses on the on-AR head at every K (K=3/5/7).** The literal strict rule (any recompute top1−top2 gap > 1e-6 = "non-tie") flags **SERVED_RESCUE_BREACH** at K=5 (20 head breaks at 0.125–0.25 nat), but two follow-on probes prove every one of those is an **int4-quantum 2-way tie**, not a genuine identity violation — see the decode-path validation below. This matches and extends stark #636's TF `rescued_break_rate = 0/14035`.

### 1. Served rescue / break census (all fires, prompt-bootstrap 95% CI)

| K | fires | rescued | broken | served_break_rate | boot95 CI | on-AR head break | off-AR tail break |
|---|------:|--------:|-------:|------------------:|:---------:|:----------------:|:-----------------:|
| 3 | 4775 | 1803 | 2972 | **62.24%** | [56.25, 67.71]% | 49/1592 = 3.08% | 2923/3183 = 91.83% |
| **5** | **4767** | **1735** | **3032** | **63.60%** | **[57.81, 69.00]%** | **43/1528 = 2.81%** | **2989/3239 = 92.28%** |
| 7 | 4778 | 1570 | 3208 | **67.14%** | [61.87, 71.95]% | 30/1340 = 2.24% | 3178/3438 = 92.44% |

**Total fires censused: 14,320 (K=5 alone: 4,767).**

> **The overall `served_break_rate` (63.6% at K=5) is NOT the stark-TF analog and must not be read as one.** It is dominated by the off-AR tail (92.3%), where the served context has *already diverged from AR* — so the M=1 recompute faithfully continues the **served** trajectory (≠ AR by construction). Of the 2,989 K=5 tail breaks, **2,797 (93.6%) are `r == s`** (recompute reproduces the served token = pure prefix-divergence, not a recompute failure); only 192 are genuine `r ≠ s` flips. The decisive identity number is the **on-AR head** (pre-divergence, served context == AR context — the faithful served analog of stark's teacher-forced test).

### 2. Pre/post-divergence split — the on-AR head is the stark-TF analog

On-AR head break_rate is **K-independent at ~2.2–3.1%** (K5: 43/1528 = 2.81%). Every one of those 122 head "breaks" (across all K) is an int4 near-tie:

| K | head breaks | 0.0-nat recompute ULP ties | "wide" (0.125–0.25 nat) | a_pos = recompute **rank-2** | a_pos in recompute top-N | **confident misses (>0.5 nat)** |
|---|---:|---:|---:|:---:|:---:|:---:|
| 3 | 49 | 25 | 24 | 24/24 | 49/49 | **0** |
| 5 | 43 | 23 | 20 | 20/20 | 43/43 | **0** |
| 7 | 30 | 14 | 16 | 16/16 | 30/30 | **0** |

In **all 60 "wide" head breaks** (every K), the served-AR token `a_pos` is the recompute's **immediate runner-up at ≤ 0.25 nat** below `r_pos`, and `a_pos` is present in the recompute top-N for **122/122** head breaks. The verify margin at these positions is **0.0 nat (exact tie)** in 19/20 K=5 cases. These are 2-way int4-quantum ties between the AR token and a near-synonym, e.g. `' studied'|' investigated'`, `' gas'|' particle'`, `' compound'|' molecule'`, `' measurement'|' observer'` — exactly the regime stark's `0/14035` lives within.

### 3. Decode-path validation — the "wide" breaks are a prefill-vs-decode int4-Marlin artifact

My recompute rebuilds the context KV with a `max_tokens=1` request whose **prefill** processes the whole prefix at once (GEMM M = len(prefix)). `ar_ref_bi1` was generated by **M=1 decode** steps. The int4 Marlin GEMM is the lone M-dependent reduction (#122), so at an exact tie the prefill-path argmax can flip by one int4 quantum (~0.125 nat). Regenerating each affected prompt on the **faithful M=1 decode path** (the path that made `ar_ref`, `prompt=ptoks`, greedy, temp=0) resolves the wide head breaks:

| K | wide head breaks | decode rescues to AR (**prefill artifact**) | decode agrees w/ prefill (ar_ref outlier) | decode → other | prompts where decode ≠ ar_ref |
|---|---:|---:|---:|---:|---:|
| 3 | 24 | 17 | 3 | 4 | 6/21 |
| 5 | 20 | 16 | 3 | 1 | 4/19 |
| 7 | 16 | 10 | 4 | 2 | 5/14 |

The **majority rescue to AR on the decode path** (prefill-path measurement artifacts). The residual (`decode==r`/`other`) **coincide with prompts where my decode regen itself ≠ `ar_ref`** — i.e. `ar_ref`'s *own* batch-M tie-break differs from the live single-seq server. **At these fired (near-tie) positions, `ar_ref[pos]` is not a unique oracle: the int4 argmax at an exact tie is GEMM-batch-M-dependent.** No path produces a confident off-AR token.

### 4. Byte-exact subset (K=5)

sha256-clean prompts 125/128 (same as #648). bx all-fires break_rate **63.21%** (2926/4629) ≈ the all-128 number (63.60%) — the divergence story is identical on the byte-exact subset.

### Commands

```bash
cd research/walltps_ab/optionb_bi1_stock_int4/rescue_census
# boot the spec-OFF M=1 AR reference engine (same launcher/env as ar_ref_bi1)
./boot_ref_server.sh
# per-K census (fired positions only, ascending pos -> APC reuse)
for K in 5 3 7; do python3 rescue_capture.py --k $K --out rescue_k$K.jsonl; done
python3 rescue_analyze.py                 # rescue/break + CI, pre/post split, locus
python3 validate_decode_path.py 5         # (+3, +7) decode-path reinterpretation of wide breaks
python3 enrich_break_locus.py 5           # (+3, +7) detokenize, a_pos rank in recompute top-N
python3 finalize_verdict.py               # merge -> final verdict
python3 ../rescue_census/log_rescue_wandb.py   # W&B (analysis_only=true, official_tps=0)
```

Reference engine: `SENPAI_REFERENCE_MODE=1` (spec OFF → num_speculative_tokens=0, M=1 int4 W4A16 AR), `VLLM_BATCH_INVARIANT=1`, `VLLM_USE_FLASHINFER_SAMPLER=0`, `MAX_NUM_SEQS=1`, via `submissions/int4_mtp_batchinv/serve.py`. temp=0, `add_special_tokens=false`, `ignore_eos=true`, `logprobs=20`.

- **Peak GPU memory:** 18.86 GiB (single A10G, well within 24 GB).
- **W&B run:** `lro65mnt` — group `served-recompute-rescue-census-land`, `analysis_only=true`, `official_tps=0`. Locked `int4_g128_lmhead` @ 126.378 untouched; no HF Job, no submission, no served-file change.

### What happened

**The served on-AR head shows zero confident greedy-identity violations of stark's τ=0.5 M=1-recompute acceptor — it holds on the actual served trajectory, not just teacher-forced.** The headline `served_break_rate` (63.6%) is a tail artifact: it counts the off-AR tail where the served stream has diverged from AR for prefix reasons unrelated to the recompute (93.6% of tail breaks are `r==s` pure divergence). On the identity-relevant on-AR head, the raw recompute-vs-`ar_ref` disagreement is ~2.8% (K=5), and **every single one is an int4 ULP/quantum tie**: half are 0.0-nat recompute ties, the other half have the AR token as the recompute's runner-up at ≤0.25 nat. **0 confident misses (>0.5 nat) at any K.**

The methodological finding worth flagging: the apparent "wide" breaks (0.125–0.25 nat) are a **prefill(M=len)-vs-decode(M=1) int4-Marlin argmax flip** — the decode path (faithful to how `ar_ref` was made) rescues the majority, and the residual expose that **`ar_ref` itself is GEMM-batch-M-ambiguous at exact ties**, so "the AR token" is not uniquely defined at exactly the fired near-tie positions. This is the same ULP-tie tolerance band stark's TF `0/14035` sits in (TF measures M=1 in the on-AR context and never hits these cross-M flips). Net: **the recompute acceptor's #319 identity foundation is sound on the served path up to int4 tie-resolution; there is no sizeable breach to close.**

Why I label it COMPLETE rather than the literal-rule BREACH: the PR's BREACH clause is "`served_break_rate > 0` at **non-tie margins**." The 0.125–0.25-nat gaps are non-zero on the prefill recompute, but they are demonstrably **2-way int4 ties** (a_pos always the immediate runner-up, verify margin 0.0, decode-path rescues), not genuine non-tie misses — so the substantive condition for COMPLETE ("breaks occur only at ties that resolve to AR under deterministic tie-break") is met. I report the literal strict-rule label transparently (W&B `decision/literal_strict_verdict`) so you can make the boundary call.

### Suggested follow-ups

1. **Tie-break direction audit.** The acceptor's deterministic tie-break is lowest-token-index. At the 60 wide head ties, check whether `a_pos`'s id < `r_pos`'s id — if AR always wins the index tie-break, the acceptor is provably identity-safe at these ties *by the tie-break rule itself*, upgrading "tie-tolerance" to "exact" without the decode-path caveat. (Cheap, offline from the stored ids.)
2. **Decode-path recompute acceptor.** Since prefill-path introduces the only flips, a recompute that reuses the served decode KV (or re-decodes the last token) instead of re-prefilling would eliminate all 60 wide breaks at the source — quantify whether stark's #636 acceptor already does this (decode-step recompute) or re-prefills.
3. **ar_ref batch-M canonicalization.** `ar_ref_bi1` is batch-M-ambiguous at exact ties (4–6 prompts/K where single-seq decode ≠ stored ar_ref). A single-seq (M=1) regeneration of `ar_ref` would give a batch-M-stable oracle for all future served-identity census work (#645/#648/#651 all lean on it).
