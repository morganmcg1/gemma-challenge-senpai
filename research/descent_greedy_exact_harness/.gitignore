# Caches
__pycache__/
*.pyc

# Transient top-level run logs
runs/*.log

# Bulky / raw per-run artifacts — keep the small greedy_exact_harness_result.json
# evidence, drop any large raw dumps or server logs a future live leg might write.
runs/**/decode_outputs.jsonl
runs/**/ppl_results.jsonl
runs/**/server.log
