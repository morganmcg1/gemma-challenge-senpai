# Local-only profiler artifacts: multi-MB and fully regeneratable via
#   python -m scripts.local_validation.profile_decode --submission ... (see FINDING.md).
# Committed: FINDING.md, breakdown.md, frontier_decode_profile.json, decode_*.summary.json
trace_*/
server_*.log
decode_*.jsonl
full_run.log
_smoke*/
