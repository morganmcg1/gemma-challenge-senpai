# The converted candidate dir is a regenerable, heavy, LOCAL dry-run artifact:
# model.safetensors is ~2.9 GB (full-size tables) and must never enter git. Both the
# synthetic dry run and the human's real `--in model_best.pt` run write here; the real
# weights belong in the HF DRAFTER_BUCKET, not this repo. Regenerate with:
#   python convert_eagle3_to_safetensors.py --synthetic-shapes   (or --in <model_best.pt>)
_candidate/
