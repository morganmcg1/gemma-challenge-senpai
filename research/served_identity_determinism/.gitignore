# scratch / regenerable — keep the commit lean (code + distilled JSON only)
__pycache__/
*.log
*.pid
# large raw token-id captures — regenerate via determinism_driver.py
decode_*.jsonl
# smoke-test scratch
determinism_smoke.json
