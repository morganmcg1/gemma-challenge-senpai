# Bulky/ephemeral run artifacts — regenerable from run_panel.py. The structured
# JSON results (summary.json, {arm}_mmlu_pro.json/_sampled.json/_aime.json) and
# text logs are the committed audit trail.
*.pid
_inspect_*/
