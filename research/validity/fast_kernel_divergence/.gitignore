# PR #540 fast-kernel-divergence -- keep the card (fast_kernel_divergence.py,
# _forced_fa_patch.py, README.md, fast_kernel_divergence.json); drop the bulky
# per-position arm captures (~1.1 GB), the smoke/scratch captures, trajectories,
# logs, and the run pidfile. The computed divergence + verdict + high-TV reads
# live in fast_kernel_divergence.json and W&B run 1qpd61cb.
_arm_*.json
_sm_*.json
_smoke*
_traj.json
_run_*.log
_run_*.pid
_run_arms.sh
_dtype_probe.py
__pycache__/
