# Keep only the small analysis artifacts (report.json, report.md, measure_*.json, tokenize.json).
# Heavy / transient files below are fully regenerable from the profiler + records.
*.log
wandb/
*smoke*
