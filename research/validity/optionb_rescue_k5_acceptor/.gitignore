# Scratch process logs, pids, draft comment bodies
*.out
*.pid
_pr663_terminal.md
__pycache__/

# Bulky / reproducible per-prompt outputs — records.jsonl + deproject_*.json keep the metrics
**/decode/
**/server_run*.log
# Per-sample GPU clock CSVs — records.jsonl already holds the clock mean/min/max summary
**/clocks/

# Smoke-test scratch dirs (superseded by full_k5 / rate_k5)
smoke_tau/
smoke_tau_v2/
