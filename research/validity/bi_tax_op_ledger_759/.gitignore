__pycache__/
*.pyc
# Raw Kineto device traces (~5.6 MB) are regenerable via run_profile_all.sh; the
# parsed per-kernel data is committed in runs/ledger.json and the human-readable
# runs/trace_bi*/profiler_out_0.txt key_averages dumps.
runs/trace_bi*/*.pt.trace.json.gz
