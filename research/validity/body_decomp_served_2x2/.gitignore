# Keep scripts + result/aggregate/layer-map JSONs + smoke self-tests; exclude bulky reproducible artifacts & scratch.
__pycache__/
*.pyc
wandb/
# raw inspect_ai eval logs (reproducible, ~MB each)
_inspect_logs/
# verbose driver stdout (progress bars; the result JSONs hold the numbers)
_run_*.log
# local draft of the posted PR comment (the comment itself lives on the PR)
_pr_comment.md
