# PR #696 analysis_only: commit code + small aggregate JSONs only.
# Raw per-seed generation dumps (--save-text) and inspect logs are bulky scratch;
# per-seed accuracies are preserved in the aggregate seed_table JSONs and in the
# W&B verdict artifact (run g5lma5qf). Re-scoring needs the GPU+model anyway.
__pycache__/
*.pyc
*.pid
*.log
results/
results_gpqa/
results_aime/
