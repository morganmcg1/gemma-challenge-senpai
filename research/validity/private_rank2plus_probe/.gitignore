# Keep only the small analysis artifacts (*.json, REPORT.md, assemble_verdict.py).
# Everything below is heavy and fully regenerable from the profiler + records.
_scratch_submission/
*.jsonl
*.jsonl.*
*.log
wandb/
smoke/
