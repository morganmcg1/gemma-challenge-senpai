# PR #776 targeted-locus-revert served-determinism captures.
# Keep scripts, verdict JSONs, per-arm decode_outputs / summary / meta / server.log
# (evidence of the served path nondeterminism + batchinv control), and the
# results comment. Drop PIDs and pycache.
__pycache__/
*.pid
