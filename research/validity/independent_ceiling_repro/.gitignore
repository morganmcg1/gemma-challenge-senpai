# PR #665 clean-room un-rescued K-ceiling — transient scaffolding NOT committed.
# Raw per-prompt completion dumps (rep*.jsonl, ~19M) are reproducible from the
# scripts and are excluded. Committed reproducible artifacts: clean_room_kceil.py,
# combine_kceil.py, precheck_identity.py, parse_records.py, step_latency.py,
# accept_metrics.py, truestock_sampler_probe.py, run*/summary.json,
# run*/records.jsonl, run*/**/*.summary.json, run*/**/server.log, *.driver.log.
rep*.jsonl
run_smoke/
__pycache__/
*.pyc
*.pid
*.boot
wandb/
