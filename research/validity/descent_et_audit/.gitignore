# Caches
__pycache__/
*.pyc

# Keep the small descent_et_dp_audit_result.json evidence; drop any bulky raw dumps
# a future live/empirical leg might write.
runs/**/*.jsonl
runs/**/*.npz
runs/**/*.log
