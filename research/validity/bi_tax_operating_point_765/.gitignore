__pycache__/
*.pyc
# Raw Kineto device traces (~5.6 MB each, x10 windows) are regenerable via
# run_sweep_all.sh; the parsed per-window data is committed in runs/sweep_ledger.json
# + the arm_bi*_summary.json window descriptors.
runs/trace_bi*/*.pt.trace.json.gz
*.nohup
