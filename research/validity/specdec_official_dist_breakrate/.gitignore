# Local scratch from the drafter-free spec screen: raw per-request token dumps
# and vLLM server logs are bulky and regenerable. Computed summaries
# (result.json, records.jsonl, token_divergence.json, *.summary.json) are kept.
__pycache__/
*.log
decode_*.jsonl
*.pid
