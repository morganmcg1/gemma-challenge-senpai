# Synthetic-acceptance decode dumps are GARBAGE tokens by construction (the
# oracle is speed-only, not a quality run) and are large + regenerable. Keep the
# parsed per-rate JSON, the summary, the server logs, and the ceiling PNG.
*.jsonl
