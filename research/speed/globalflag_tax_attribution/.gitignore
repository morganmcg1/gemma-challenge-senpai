# Bulky torch-profiler chrome traces (~4M each; parsed bucket µs live in the JSON)
_runs/
# Per-run vLLM server + driver logs (parsed metrics live in the JSON)
*.log
# Scratch smoke run (plumbing check only; superseded by the real run)
_smoke.json
# Ephemeral process + console artifacts
*.pid
*.out
__pycache__/
