STUDENT ubel: proof-of-life + a control-fidelity finding (non-blocking, but this card is load-bearing so flagging early).

**Run is alive.** W&B (resumable — the final verdict lands in this same run): https://wandb.ai/wandb-applied-ai-team/gemma-challenge-senpai/runs/nqk9izab  (group `int4-aime-selective-g32-build-ubel`). The 3-arm sweep (`run_sweep.sh`) is on the **full_g128 control, seed 2/5**; seeds 0,1 done. Protocol verified faithful to the #693 / #650 reference (`config.yaml` of `pr650-harden-aime-sampled-int4ar`): **no-thinking**, T=1.0 / top_p=0.95 / top_k=64, max_tokens=12288, min_tokens=8, sampled, n=60, 5-seed pooled → Wilson z=1.96 on 300.

**Disk:** ~9.5G free, holding. The sweep builds ONE 17G bf16 checkpoint at a time and `rm -rf`s it before the next arm, so the floor is bounded. Will ping if it drops below ~8G as you asked.

**The finding — the full_g128 control is tracking ~0.08 BELOW the 0.347 int4 anchor** (pooled **0.267** over 2 seeds; per-seed 0.250 / 0.283; Wilson-hi 0.352 just grazes 0.347). This is not pure seed noise — the #693 int4 reference per-seed is tightly clustered 0.317–0.367 (pool 0.3467). Two build properties explain the offset, and **neither is a weight-quant bug**:

1. **bf16-dense fake-quant serve, not the int4-Marlin kernel.** The PR's fake-quant approach serves a dequantized bf16 checkpoint (dodges the mixed-grid g128 pruned-lm_head assert, as intended). The #679/#693 anchors were measured on the int4-Marlin served model. Same weights, different matmul path → a systematic AIME offset over the chaotic 12k-token sampled generation is expected (consistent with the int4-AIME session-nondeterminism already on record).
2. **g128 is double-quant (g32→g128).** The served `build_quant.py` single-quantizes the *unquantized* QAT source (`qat_unq`), which is **not on disk** (and disk is full), so my build re-quantizes the on-disk w4a16-ct g32 weights to g128. BUT the double-quant g128 body rel_err mean = **0.1036 ≈ the served single-quant g128 (~0.09–0.10** from the `_probe` / `lmhead12k` build logs), so this adds negligible weight error.

**Why the experiment stays valid:** all three arms share the SAME bf16-fake-quant path, so the inter-arm contrast is clean, and `aggregate.py` computes `selective_recovery_fraction` on the **measured** controls (self-correcting for the offset), not the external anchors. The **full_g32 control is the linchpin** — it uses the official w4a16-ct g32 weights directly (no re-quant), so if it reproduces ~0.438 on the bf16 serve, the serving path is faithful and the g128 gap is a real recipe property; if it lands ~0.08 low too, the offset is uniform and `recovery_fraction` absorbs it. Either way I'll report the verdict on BOTH the absolute 0.420 gate AND the measured-control scale, with these caveats explicit.

**Where I'd want a steer (not blocking — I'm proceeding):** if the eventual deliverable must be a *served-recipe-faithful* held build (int4-Marlin, single-quant g128), that needs `qat_unq` fetched + mixed-grid serialization — which needs disk freed and re-opens the pruned-lm_head assert. My read is the fake-quant bf16 path is the correct instrument for the *quality-recovery question* (does the 48-module subset carry the AIME-critical error), and the speed is already byte-law-projected at 126.275, so I'm continuing. Shout if you'd rather I pause for the faithful-serve variant.
