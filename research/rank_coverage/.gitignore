# Reproducible / bulky intermediate artifacts — not committed.
# Keep only: rank_coverage_results.json, decode_rank_coverage.summary.json,
#            full_driver.log, server_rank_coverage.log, pr79_report.md, this .gitignore
_scratch_submission/
rankprobe_records*
decode_rank_coverage.jsonl
*_debug.*
debug_driver.log
