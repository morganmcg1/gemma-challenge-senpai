# Reproducible / bulky intermediate artifacts — not committed.
# Keep only: firstreject_capture_report.json, decode_outputs.summary.json, this .gitignore.
# The decisive analysis output lives one level up: prompt_lookup_overlap.json.
_scratch_submission/
firstreject_records*
decode_outputs*.jsonl
server_firstreject*.log
full_driver.log
debug_driver.log
*_debug.*
