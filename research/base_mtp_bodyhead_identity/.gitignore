# PR #600 base_mtp BODY-vs-HEAD greedy-identity census. Keep the harness
# (identity_census.py), census_report.json (carries the full identity verdict +
# every flip-probe row), and the small per-capture *.summary.json. Drop the bulky
# raw decode token streams (regenerable; the byte-exact verdict + onsets + margins
# all live in census_report.json) and __pycache__. Server/census *.log are already
# covered by the repo-root research/**/*.log rule.
decode_*.jsonl
__pycache__/
