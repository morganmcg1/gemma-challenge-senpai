# Bulky raw token dumps (reproducible; summaries + aggregated JSON are kept)
**/decode/*.jsonl
**/warmup/*.jsonl
ppl_check.jsonl
# Bulky vLLM server logs (per-run, ~900K each; the parsed metrics live in records.jsonl)
**/server_fresh_run*.log
# Scratch smoke run (4x32 dummy workload — harness plumbing check only)
smoke_n2/
smoke_n2.out
# Ephemeral process + console artifacts
*.pid
*.out
__pycache__/
