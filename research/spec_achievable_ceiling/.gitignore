# PR #728 spec-dec achievable ceiling (analysis-only). Keep the harness scripts, the
# aggregate report.json / report.smoke.json, the per-K rescue/info/summary JSONs, the
# AR-vs-AR control JSON, and the decode_outputs.jsonl evidence. Drop the bulky
# regenerable teacher-forced base-distribution dump (~39 MB, fully summarized into
# *.rescue.json) and intermediate ppl jsonl, plus __pycache__ and transient pids.
runs/**/ref.base_dist.json
runs/**/*.ppl.jsonl
__pycache__/
*.pid
