# scratch / regenerable — keep the commit lean (code + distilled JSON only)
__pycache__/
*.log
*.pid
# large raw token-id captures (~5.5MB each) — regenerate via census_verify.py
decode_*.jsonl
decode_*.summary.json
# smoke-test scratch
census_report.smoke.json
