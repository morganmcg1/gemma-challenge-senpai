# scratch / regenerable — keep the commit lean (code + distilled JSON only)
__pycache__/
_backup_captures/
*.log
*.pid
# large raw token-id captures (~5.5MB) — regenerate via census_driver.py
decode_*.jsonl
# smoke-test scratch
census_smoke.json
