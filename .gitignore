.env
.credentials
.venv/
*.pem
__pycache__/
*.egg-info/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store
.python-version

# Local W&B run-sync dirs. Runs live on the W&B server (we keep the run IDs);
# don't commit the bulky local copies (binary .wandb files + debug logs).
wandb/

# Local working checkout of the Senpai runner fork. The published branch lives
# in wandb/senpai; this repo only stores the challenge workspace.
senpai-aws-compatible/

# Transient local-validation run artifacts (server logs, smoke runs, install logs).
# The canonical greedy reference under research/greedy_reference/ is kept.
research/_localrun/
# Transient serve/profiler logs written beside committed reference artifacts.
research/**/*.log

# Local W&B run cache (runs are synced online; the local dir is regenerable).
wandb/
submissions/*/model/

# Live advisor frontier polling state.
research/frontier_watch/

# Drafter v0 (PR #9): trained checkpoints (100s of MB) and local W&B run dirs
# are reproducible from scripts/drafter/ + the committed corpus, so keep them
# out of git. The large train shard is regenerable from build_corpus.py (fixed
# seed); the held-out shard + composition.json are committed as the gate proof.
research/wide_drafter/ckpt/
research/wide_drafter/corpus/train.jsonl
# v1: target-greedy re-decode of the same prompts (build_greedy_corpus.py, fixed
# seed, deterministic greedy) -- regenerable, so keep the large shard out of git.
research/wide_drafter/corpus/greedy_train.jsonl
